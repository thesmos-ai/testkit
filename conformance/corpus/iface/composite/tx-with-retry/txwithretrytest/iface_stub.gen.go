// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/composite/tx-with-retry/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package txwithretrytest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	txwithretry "go.thesmos.sh/testkit/conformance/corpus/iface/composite/tx-with-retry"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// TxWithRetryBeginCall records one invocation of TxWithRetry.Begin.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type TxWithRetryBeginCall struct {
	Ctx context.Context
	Err error
}

// TxWithRetryCommitCall records one invocation of TxWithRetry.Commit.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type TxWithRetryCommitCall struct {
	Ctx context.Context
	Err error
}

// TxWithRetryRollbackCall records one invocation of TxWithRetry.Rollback.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type TxWithRetryRollbackCall struct {
	Ctx context.Context
	Err error
}

// --- Per-method configuration ---

// TxWithRetryBeginStub controls how the double answers Begin and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type TxWithRetryBeginStub struct {
	*stub.MethodStub[TxWithRetryBeginCall]

	fn       func(context.Context) error
	fallback *TxWithRetryBeginReturn
}

// TxWithRetryBeginReturn holds the fixed answer configured through Returns.
type TxWithRetryBeginReturn struct {
	Err error
}

// Returns pins a fixed result for every call to Begin. A Func
// override and an injected fault both take precedence over it.
func (s *TxWithRetryBeginStub) Returns(err error) *TxWithRetryBeginStub {
	s.fallback = &TxWithRetryBeginReturn{Err: err}
	return s
}

// Func supplies a body for Begin, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *TxWithRetryBeginStub) Func(fn func(context.Context) error) *TxWithRetryBeginStub {
	s.fn = fn
	return s
}

// TxWithRetryCommitStub controls how the double answers Commit and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type TxWithRetryCommitStub struct {
	*stub.MethodStub[TxWithRetryCommitCall]

	fn       func(context.Context) error
	fallback *TxWithRetryCommitReturn
}

// TxWithRetryCommitReturn holds the fixed answer configured through Returns.
type TxWithRetryCommitReturn struct {
	Err error
}

// Returns pins a fixed result for every call to Commit. A Func
// override and an injected fault both take precedence over it.
func (s *TxWithRetryCommitStub) Returns(err error) *TxWithRetryCommitStub {
	s.fallback = &TxWithRetryCommitReturn{Err: err}
	return s
}

// Func supplies a body for Commit, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *TxWithRetryCommitStub) Func(fn func(context.Context) error) *TxWithRetryCommitStub {
	s.fn = fn
	return s
}

// TxWithRetryRollbackStub controls how the double answers Rollback and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type TxWithRetryRollbackStub struct {
	*stub.MethodStub[TxWithRetryRollbackCall]

	fn       func(context.Context) error
	fallback *TxWithRetryRollbackReturn
}

// TxWithRetryRollbackReturn holds the fixed answer configured through Returns.
type TxWithRetryRollbackReturn struct {
	Err error
}

// Returns pins a fixed result for every call to Rollback. A Func
// override and an injected fault both take precedence over it.
func (s *TxWithRetryRollbackStub) Returns(err error) *TxWithRetryRollbackStub {
	s.fallback = &TxWithRetryRollbackReturn{Err: err}
	return s
}

// Func supplies a body for Rollback, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *TxWithRetryRollbackStub) Func(fn func(context.Context) error) *TxWithRetryRollbackStub {
	s.fn = fn
	return s
}

// --- TxWithRetryStub ---

// The double-runtime witness for the surface this double rides. A
// breaking change to the runtime renames it, and this file stops
// compiling with the skew named.
var _ = stub.CompatV1

// TxWithRetryStubOption configures a [TxWithRetryStub] at construction time.
type TxWithRetryStubOption func(*TxWithRetryStub)

// TxWithRetryStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func TxWithRetryStubStrict() TxWithRetryStubOption {
	return func(s *TxWithRetryStub) { s.strict = true }
}

// TxWithRetryStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func TxWithRetryStubDelegateTo(impl txwithretry.TxWithRetry) TxWithRetryStubOption {
	return func(s *TxWithRetryStub) {
		s.OnBegin.Func(impl.Begin)
		s.OnCommit.Func(impl.Commit)
		s.OnRollback.Func(impl.Rollback)
	}
}

// TxWithRetryStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func TxWithRetryStubWithClock(clk clock.Clock) TxWithRetryStubOption {
	return func(s *TxWithRetryStub) { s.group.SetClock(clk) }
}

// TxWithRetryStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func TxWithRetryStubWithRandSource(src rand.Source) TxWithRetryStubOption {
	return func(s *TxWithRetryStub) { s.group.SetRandSource(src) }
}

// TxWithRetryStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func TxWithRetryStubBenchMode() TxWithRetryStubOption {
	return func(s *TxWithRetryStub) { s.group.BenchMode() }
}

// WithTxWithRetryBegin sets Begin's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithTxWithRetryBegin(fn func(context.Context) error) TxWithRetryStubOption {
	return func(s *TxWithRetryStub) { s.OnBegin.Func(fn) }
}

// WithTxWithRetryCommit sets Commit's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithTxWithRetryCommit(fn func(context.Context) error) TxWithRetryStubOption {
	return func(s *TxWithRetryStub) { s.OnCommit.Func(fn) }
}

// WithTxWithRetryRollback sets Rollback's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithTxWithRetryRollback(fn func(context.Context) error) TxWithRetryStubOption {
	return func(s *TxWithRetryStub) { s.OnRollback.Func(fn) }
}

// TxWithRetryStub is a recording test double for TxWithRetry.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type TxWithRetryStub struct {
	OnBegin    *TxWithRetryBeginStub
	OnCommit   *TxWithRetryCommitStub
	OnRollback *TxWithRetryRollbackStub

	// group is every method stub above, viewed through the surface that
	// does not depend on a signature — one home for the settings and the
	// lifecycle that apply to the whole double.
	group  *stub.Group
	strict bool
}

// Compile-time proof that TxWithRetryStub satisfies TxWithRetry.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ txwithretry.TxWithRetry = (*TxWithRetryStub)(nil)

// NewTxWithRetryStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewTxWithRetryStub(tb testing.TB, opts ...TxWithRetryStubOption) *TxWithRetryStub {
	s := &TxWithRetryStub{
		OnBegin:    &TxWithRetryBeginStub{MethodStub: stub.NewMethodStub[TxWithRetryBeginCall](tb, "TxWithRetry.Begin")},
		OnCommit:   &TxWithRetryCommitStub{MethodStub: stub.NewMethodStub[TxWithRetryCommitCall](tb, "TxWithRetry.Commit")},
		OnRollback: &TxWithRetryRollbackStub{MethodStub: stub.NewMethodStub[TxWithRetryRollbackCall](tb, "TxWithRetry.Rollback")},
	}
	s.group = stub.NewGroup(
		s.OnBegin.MethodStub,
		s.OnCommit.MethodStub,
		s.OnRollback.MethodStub,
	)
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.group.StrictAll()
	}
	s.group.Bind(tb)
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *TxWithRetryStub) ResetCalls() {
	s.group.Reset()
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *TxWithRetryBeginStub) invoke(ctx context.Context) func() TxWithRetryBeginReturn {
	if s.fn == nil {
		return nil
	}
	return func() TxWithRetryBeginReturn {
		r0 := s.fn(ctx)
		return TxWithRetryBeginReturn{Err: r0}
	}
}

// Begin records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *TxWithRetryStub) Begin(ctx context.Context) error {
	call := TxWithRetryBeginCall{Ctx: ctx}
	r := stub.Answer(s.OnBegin.MethodStub, &call, stub.Arms[TxWithRetryBeginCall, TxWithRetryBeginReturn]{
		Invoke:   s.OnBegin.invoke(ctx),
		Fallback: s.OnBegin.fallback,
		Fault:    func(err error) TxWithRetryBeginReturn { return TxWithRetryBeginReturn{Err: err} },
		Stamp: func(c *TxWithRetryBeginCall, r TxWithRetryBeginReturn) {
			c.Err = r.Err
		},
	})
	return r.Err
}

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *TxWithRetryCommitStub) invoke(ctx context.Context) func() TxWithRetryCommitReturn {
	if s.fn == nil {
		return nil
	}
	return func() TxWithRetryCommitReturn {
		r0 := s.fn(ctx)
		return TxWithRetryCommitReturn{Err: r0}
	}
}

// Commit records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *TxWithRetryStub) Commit(ctx context.Context) error {
	call := TxWithRetryCommitCall{Ctx: ctx}
	r := stub.Answer(s.OnCommit.MethodStub, &call, stub.Arms[TxWithRetryCommitCall, TxWithRetryCommitReturn]{
		Invoke:   s.OnCommit.invoke(ctx),
		Fallback: s.OnCommit.fallback,
		Fault:    func(err error) TxWithRetryCommitReturn { return TxWithRetryCommitReturn{Err: err} },
		Stamp: func(c *TxWithRetryCommitCall, r TxWithRetryCommitReturn) {
			c.Err = r.Err
		},
	})
	return r.Err
}

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *TxWithRetryRollbackStub) invoke(ctx context.Context) func() TxWithRetryRollbackReturn {
	if s.fn == nil {
		return nil
	}
	return func() TxWithRetryRollbackReturn {
		r0 := s.fn(ctx)
		return TxWithRetryRollbackReturn{Err: r0}
	}
}

// Rollback records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *TxWithRetryStub) Rollback(ctx context.Context) error {
	call := TxWithRetryRollbackCall{Ctx: ctx}
	r := stub.Answer(s.OnRollback.MethodStub, &call, stub.Arms[TxWithRetryRollbackCall, TxWithRetryRollbackReturn]{
		Invoke:   s.OnRollback.invoke(ctx),
		Fallback: s.OnRollback.fallback,
		Fault:    func(err error) TxWithRetryRollbackReturn { return TxWithRetryRollbackReturn{Err: err} },
		Stamp: func(c *TxWithRetryRollbackCall, r TxWithRetryRollbackReturn) {
			c.Err = r.Err
		},
	})
	return r.Err
}

// testkit: end of generated content.
// testkit:provenance 381e350d4125b08805bcc76411c6d1274866d55d449befb80cbf2f908a6a8a81
