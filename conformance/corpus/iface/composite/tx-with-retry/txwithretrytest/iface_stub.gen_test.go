// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/composite/tx-with-retry/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package txwithretrytest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/composite/tx-with-retry/txwithretrytest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// txWithRetryStubBeginSubject binds Begin into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func txWithRetryStubBeginSubject(tb testing.TB) stub.Subject[txwithretrytest.TxWithRetryBeginCall, txwithretrytest.TxWithRetryBeginReturn] {
	tb.Helper()
	s := txwithretrytest.NewTxWithRetryStub(tb)
	return stub.Subject[txwithretrytest.TxWithRetryBeginCall, txwithretrytest.TxWithRetryBeginReturn]{
		Stub: s.OnBegin.MethodStub,
		Call: func() {
			var a0 context.Context
			_ = s.Begin(a0)
		},
		Result: func() txwithretrytest.TxWithRetryBeginReturn {
			var a0 context.Context
			got0 := s.Begin(a0)
			return txwithretrytest.TxWithRetryBeginReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnBegin.Func(func(_ context.Context) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			r0 := s.Begin(a0)
			return r0
		},
	}
}

// TestTxWithRetryStubBegin pins how Begin answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestTxWithRetryStubBegin(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Begin", txWithRetryStubBeginSubject)

	// No check that Begin answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := txwithretrytest.NewTxWithRetryStub(t)
		var a0 context.Context
		_ = s.Begin(a0)
		got := s.OnBegin.AssertCalledOnce(t, "Begin must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := txwithretrytest.NewTxWithRetryStub(t)
		var seen []txwithretrytest.TxWithRetryBeginCall
		s.OnBegin.OnRecord(func(c txwithretrytest.TxWithRetryBeginCall) { seen = append(seen, c) })
		var a0 context.Context
		_ = s.Begin(a0)
		_ = s.Begin(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Begin call")
	})

	t.Run("wires WithTxWithRetryBegin at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := txwithretrytest.NewTxWithRetryStub(t, txwithretrytest.WithTxWithRetryBegin(func(_ context.Context) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		_ = s.Begin(a0)
		testkit.True(t, called, "WithTxWithRetryBegin must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Begin admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := txwithretrytest.NewTxWithRetryStub(t)
		var want0 error
		s.OnBegin.Returns(want0)
		var a0 context.Context
		_ = s.Begin(a0)
		s.ResetCalls()
		_ = s.Begin(a0)
		s.OnBegin.AssertCalledOnce(t, "Begin must record the call that followed the reset")
	})
}

// txWithRetryStubCommitSubject binds Commit into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func txWithRetryStubCommitSubject(tb testing.TB) stub.Subject[txwithretrytest.TxWithRetryCommitCall, txwithretrytest.TxWithRetryCommitReturn] {
	tb.Helper()
	s := txwithretrytest.NewTxWithRetryStub(tb)
	return stub.Subject[txwithretrytest.TxWithRetryCommitCall, txwithretrytest.TxWithRetryCommitReturn]{
		Stub: s.OnCommit.MethodStub,
		Call: func() {
			var a0 context.Context
			_ = s.Commit(a0)
		},
		Result: func() txwithretrytest.TxWithRetryCommitReturn {
			var a0 context.Context
			got0 := s.Commit(a0)
			return txwithretrytest.TxWithRetryCommitReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnCommit.Func(func(_ context.Context) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			r0 := s.Commit(a0)
			return r0
		},
	}
}

// TestTxWithRetryStubCommit pins how Commit answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestTxWithRetryStubCommit(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Commit", txWithRetryStubCommitSubject)

	// No check that Commit answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := txwithretrytest.NewTxWithRetryStub(t)
		var a0 context.Context
		_ = s.Commit(a0)
		got := s.OnCommit.AssertCalledOnce(t, "Commit must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := txwithretrytest.NewTxWithRetryStub(t)
		var seen []txwithretrytest.TxWithRetryCommitCall
		s.OnCommit.OnRecord(func(c txwithretrytest.TxWithRetryCommitCall) { seen = append(seen, c) })
		var a0 context.Context
		_ = s.Commit(a0)
		_ = s.Commit(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Commit call")
	})

	t.Run("wires WithTxWithRetryCommit at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := txwithretrytest.NewTxWithRetryStub(t, txwithretrytest.WithTxWithRetryCommit(func(_ context.Context) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		_ = s.Commit(a0)
		testkit.True(t, called, "WithTxWithRetryCommit must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Commit admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := txwithretrytest.NewTxWithRetryStub(t)
		var want0 error
		s.OnCommit.Returns(want0)
		var a0 context.Context
		_ = s.Commit(a0)
		s.ResetCalls()
		_ = s.Commit(a0)
		s.OnCommit.AssertCalledOnce(t, "Commit must record the call that followed the reset")
	})
}

// txWithRetryStubRollbackSubject binds Rollback into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func txWithRetryStubRollbackSubject(tb testing.TB) stub.Subject[txwithretrytest.TxWithRetryRollbackCall, txwithretrytest.TxWithRetryRollbackReturn] {
	tb.Helper()
	s := txwithretrytest.NewTxWithRetryStub(tb)
	return stub.Subject[txwithretrytest.TxWithRetryRollbackCall, txwithretrytest.TxWithRetryRollbackReturn]{
		Stub: s.OnRollback.MethodStub,
		Call: func() {
			var a0 context.Context
			_ = s.Rollback(a0)
		},
		Result: func() txwithretrytest.TxWithRetryRollbackReturn {
			var a0 context.Context
			got0 := s.Rollback(a0)
			return txwithretrytest.TxWithRetryRollbackReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnRollback.Func(func(_ context.Context) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			r0 := s.Rollback(a0)
			return r0
		},
	}
}

// TestTxWithRetryStubRollback pins how Rollback answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestTxWithRetryStubRollback(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Rollback", txWithRetryStubRollbackSubject)

	// No check that Rollback answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := txwithretrytest.NewTxWithRetryStub(t)
		var a0 context.Context
		_ = s.Rollback(a0)
		got := s.OnRollback.AssertCalledOnce(t, "Rollback must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := txwithretrytest.NewTxWithRetryStub(t)
		var seen []txwithretrytest.TxWithRetryRollbackCall
		s.OnRollback.OnRecord(func(c txwithretrytest.TxWithRetryRollbackCall) { seen = append(seen, c) })
		var a0 context.Context
		_ = s.Rollback(a0)
		_ = s.Rollback(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Rollback call")
	})

	t.Run("wires WithTxWithRetryRollback at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := txwithretrytest.NewTxWithRetryStub(t, txwithretrytest.WithTxWithRetryRollback(func(_ context.Context) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		_ = s.Rollback(a0)
		testkit.True(t, called, "WithTxWithRetryRollback must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Rollback admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := txwithretrytest.NewTxWithRetryStub(t)
		var want0 error
		s.OnRollback.Returns(want0)
		var a0 context.Context
		_ = s.Rollback(a0)
		s.ResetCalls()
		_ = s.Rollback(a0)
		s.OnRollback.AssertCalledOnce(t, "Rollback must record the call that followed the reset")
	})
}

// txWithRetryStubDouble describes how to build a TxWithRetryStub under each
// option whose effect is the same whatever a method's signature.
//
// Begin stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func txWithRetryStubDouble() stub.Double[txwithretrytest.TxWithRetryBeginCall] {
	instance := func(s *txwithretrytest.TxWithRetryStub) stub.Instance[txwithretrytest.TxWithRetryBeginCall] {
		return stub.Instance[txwithretrytest.TxWithRetryBeginCall]{
			Stub: s.OnBegin.MethodStub,
			Call: func() {
				var a0 context.Context
				_ = s.Begin(a0)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[txwithretrytest.TxWithRetryBeginCall]{
		New: func(tb testing.TB) stub.Instance[txwithretrytest.TxWithRetryBeginCall] {
			return instance(txwithretrytest.NewTxWithRetryStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[txwithretrytest.TxWithRetryBeginCall] {
			return instance(txwithretrytest.NewTxWithRetryStub(tb, txwithretrytest.TxWithRetryStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[txwithretrytest.TxWithRetryBeginCall] {
			return instance(txwithretrytest.NewTxWithRetryStub(tb, txwithretrytest.TxWithRetryStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[txwithretrytest.TxWithRetryBeginCall] {
			return instance(txwithretrytest.NewTxWithRetryStub(tb, txwithretrytest.TxWithRetryStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[txwithretrytest.TxWithRetryBeginCall] {
			return instance(txwithretrytest.NewTxWithRetryStub(tb, txwithretrytest.TxWithRetryStubStrict()))
		},
	}
}

// TestTxWithRetryStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestTxWithRetryStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, txWithRetryStubDouble())
}

// TestTxWithRetryStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestTxWithRetryStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := txwithretrytest.NewTxWithRetryStub(t)
	s := txwithretrytest.NewTxWithRetryStub(t, txwithretrytest.TxWithRetryStubDelegateTo(inner))

	t.Run("forwards Begin to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_ = s.Begin(a0)
		inner.OnBegin.AssertCalledOnce(t, "Begin must reach the wrapped implementation")
	})

	t.Run("surfaces what Begin answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Begin-delegate")
		inner.OnBegin.FaultsFor(time.Hour, want)
		var a0 context.Context
		r0 := s.Begin(a0)
		testkit.ErrorIs(t, r0, want, "Begin must surface the wrapped answer")
	})

	t.Run("forwards Commit to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_ = s.Commit(a0)
		inner.OnCommit.AssertCalledOnce(t, "Commit must reach the wrapped implementation")
	})

	t.Run("surfaces what Commit answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Commit-delegate")
		inner.OnCommit.FaultsFor(time.Hour, want)
		var a0 context.Context
		r0 := s.Commit(a0)
		testkit.ErrorIs(t, r0, want, "Commit must surface the wrapped answer")
	})

	t.Run("forwards Rollback to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_ = s.Rollback(a0)
		inner.OnRollback.AssertCalledOnce(t, "Rollback must reach the wrapped implementation")
	})

	t.Run("surfaces what Rollback answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Rollback-delegate")
		inner.OnRollback.FaultsFor(time.Hour, want)
		var a0 context.Context
		r0 := s.Rollback(a0)
		testkit.ErrorIs(t, r0, want, "Rollback must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 3dbd3dc5b264992f1c2761277da8eec217c7b123c633962044f5a25084a29333
