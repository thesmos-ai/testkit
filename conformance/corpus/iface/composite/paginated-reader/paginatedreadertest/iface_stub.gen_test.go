// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/composite/paginated-reader/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package paginatedreadertest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	paginatedreader "go.thesmos.sh/testkit/conformance/corpus/iface/composite/paginated-reader"
	"go.thesmos.sh/testkit/conformance/corpus/iface/composite/paginated-reader/paginatedreadertest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// paginatedReaderStubPageSubject binds Page into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func paginatedReaderStubPageSubject(tb testing.TB) stub.Subject[paginatedreadertest.PaginatedReaderPageCall, paginatedreadertest.PaginatedReaderPageReturn] {
	tb.Helper()
	s := paginatedreadertest.NewPaginatedReaderStub(tb)
	return stub.Subject[paginatedreadertest.PaginatedReaderPageCall, paginatedreadertest.PaginatedReaderPageReturn]{
		Stub: s.OnPage.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 int = 42
			_, _, _ = s.Page(a0, a1)
		},
		Result: func() paginatedreadertest.PaginatedReaderPageReturn {
			var a0 context.Context
			var a1 int = 42
			got0, got1, got2 := s.Page(a0, a1)
			return paginatedreadertest.PaginatedReaderPageReturn{Items: got0, Next: got1, Err: got2}
		},
		Override: func(mark func()) {
			s.OnPage.Func(func(_ context.Context, _ int) ([]paginatedreader.Value, int, error) {
				mark()
				var z0 []paginatedreader.Value
				var z1 int
				var z2 error
				return z0, z1, z2
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 int = 42
			_, _, err := s.Page(a0, a1)
			return err
		},
	}
}

// TestPaginatedReaderStubPage pins how Page answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestPaginatedReaderStubPage(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Page", paginatedReaderStubPageSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := paginatedreadertest.NewPaginatedReaderStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 []paginatedreader.Value
		var want1 int = 42
		var want2 error
		s.OnPage.Returns(want0, want1, want2)
		var a0 context.Context
		var a1 int = 42
		_, got1, _ := s.Page(a0, a1)
		testkit.Equal(t, got1, want1, "Page must answer with what Returns pinned")
		// Blanked above rather than dropped in silence. No literal exists
		// for result 0 (), result 2 (error), so the only value to pin there
		// is the zero — and a double that ignored Returns answers the zero
		// too, which makes the comparison pass either way.
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := paginatedreadertest.NewPaginatedReaderStub(t)
		var a0 context.Context
		var a1 int = 42
		_, _, _ = s.Page(a0, a1)
		got := s.OnPage.AssertCalledOnce(t, "Page must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Cursor, a1, "the recorded call carries Cursor")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := paginatedreadertest.NewPaginatedReaderStub(t)
		var seen []paginatedreadertest.PaginatedReaderPageCall
		s.OnPage.OnRecord(func(c paginatedreadertest.PaginatedReaderPageCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 int = 42
		_, _, _ = s.Page(a0, a1)
		_, _, _ = s.Page(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Page call")
	})

	t.Run("wires WithPaginatedReaderPage at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := paginatedreadertest.NewPaginatedReaderStub(t, paginatedreadertest.WithPaginatedReaderPage(func(_ context.Context, _ int) ([]paginatedreader.Value, int, error) {
			called = true
			var z0 []paginatedreader.Value
			var z1 int
			var z2 error
			return z0, z1, z2
		}))
		var a0 context.Context
		var a1 int = 42
		_, _, _ = s.Page(a0, a1)
		testkit.True(t, called, "WithPaginatedReaderPage must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		//
		// Pinned to a literal for the reason the Returns check is: a reset
		// that lost the configuration answers the zero, and so does one that
		// kept a configured zero.
		s := paginatedreadertest.NewPaginatedReaderStub(t)
		var want0 []paginatedreader.Value
		var want1 int = 42
		var want2 error
		s.OnPage.Returns(want0, want1, want2)
		var a0 context.Context
		var a1 int = 42
		_, _, _ = s.Page(a0, a1)
		s.ResetCalls()
		_, got1, _ := s.Page(a0, a1)
		testkit.Equal(t, got1, want1, "a reset must keep what Returns pinned")
	})
}

// paginatedReaderStubDouble describes how to build a PaginatedReaderStub under each
// option whose effect is the same whatever a method's signature.
//
// Page stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func paginatedReaderStubDouble() stub.Double[paginatedreadertest.PaginatedReaderPageCall] {
	instance := func(s *paginatedreadertest.PaginatedReaderStub) stub.Instance[paginatedreadertest.PaginatedReaderPageCall] {
		return stub.Instance[paginatedreadertest.PaginatedReaderPageCall]{
			Stub: s.OnPage.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 int = 42
				_, _, _ = s.Page(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[paginatedreadertest.PaginatedReaderPageCall]{
		New: func(tb testing.TB) stub.Instance[paginatedreadertest.PaginatedReaderPageCall] {
			return instance(paginatedreadertest.NewPaginatedReaderStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[paginatedreadertest.PaginatedReaderPageCall] {
			return instance(paginatedreadertest.NewPaginatedReaderStub(tb, paginatedreadertest.PaginatedReaderStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[paginatedreadertest.PaginatedReaderPageCall] {
			return instance(paginatedreadertest.NewPaginatedReaderStub(tb, paginatedreadertest.PaginatedReaderStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[paginatedreadertest.PaginatedReaderPageCall] {
			return instance(paginatedreadertest.NewPaginatedReaderStub(tb, paginatedreadertest.PaginatedReaderStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[paginatedreadertest.PaginatedReaderPageCall] {
			return instance(paginatedreadertest.NewPaginatedReaderStub(tb, paginatedreadertest.PaginatedReaderStubStrict()))
		},
	}
}

// TestPaginatedReaderStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestPaginatedReaderStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, paginatedReaderStubDouble())
}

// TestPaginatedReaderStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestPaginatedReaderStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := paginatedreadertest.NewPaginatedReaderStub(t)
	s := paginatedreadertest.NewPaginatedReaderStub(t, paginatedreadertest.PaginatedReaderStubDelegateTo(inner))

	t.Run("forwards Page to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 int = 42
		_, _, _ = s.Page(a0, a1)
		inner.OnPage.AssertCalledOnce(t, "Page must reach the wrapped implementation")
	})

	t.Run("surfaces what Page answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Page-delegate")
		inner.OnPage.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 int = 42
		_, _, err := s.Page(a0, a1)
		testkit.ErrorIs(t, err, want, "Page must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 572e2f59e153a379aae37e956f8ee60776499b0b6a66bf87a0247b851fffe450
