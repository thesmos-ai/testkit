// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/composite/paginated-reader/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package paginatedreadertest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	paginatedreader "go.thesmos.sh/testkit/conformance/corpus/iface/composite/paginated-reader"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// PaginatedReaderPageCall records one invocation of PaginatedReader.Page.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type PaginatedReaderPageCall struct {
	Ctx    context.Context
	Cursor int
	Items  []paginatedreader.Value
	Next   int
	Err    error
}

// --- Per-method configuration ---

// PaginatedReaderPageStub controls how the double answers Page and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type PaginatedReaderPageStub struct {
	*stub.MethodStub[PaginatedReaderPageCall]

	fn       func(context.Context, int) ([]paginatedreader.Value, int, error)
	fallback *PaginatedReaderPageReturn
}

// PaginatedReaderPageReturn holds the fixed answer configured through Returns.
type PaginatedReaderPageReturn struct {
	Items []paginatedreader.Value
	Next  int
	Err   error
}

// Returns pins a fixed result for every call to Page. A Func
// override and an injected fault both take precedence over it.
func (s *PaginatedReaderPageStub) Returns(items []paginatedreader.Value, next int, err error) *PaginatedReaderPageStub {
	s.fallback = &PaginatedReaderPageReturn{Items: items, Next: next, Err: err}
	return s
}

// Func supplies a body for Page, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *PaginatedReaderPageStub) Func(fn func(context.Context, int) ([]paginatedreader.Value, int, error)) *PaginatedReaderPageStub {
	s.fn = fn
	return s
}

// --- PaginatedReaderStub ---

// The double-runtime witness for the surface this double rides. A
// breaking change to the runtime renames it, and this file stops
// compiling with the skew named.
var _ = stub.CompatV1

// PaginatedReaderStubOption configures a [PaginatedReaderStub] at construction time.
type PaginatedReaderStubOption func(*PaginatedReaderStub)

// PaginatedReaderStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func PaginatedReaderStubStrict() PaginatedReaderStubOption {
	return func(s *PaginatedReaderStub) { s.strict = true }
}

// PaginatedReaderStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func PaginatedReaderStubDelegateTo(impl paginatedreader.PaginatedReader) PaginatedReaderStubOption {
	return func(s *PaginatedReaderStub) {
		s.OnPage.Func(impl.Page)
	}
}

// PaginatedReaderStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func PaginatedReaderStubWithClock(clk clock.Clock) PaginatedReaderStubOption {
	return func(s *PaginatedReaderStub) { s.group.SetClock(clk) }
}

// PaginatedReaderStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func PaginatedReaderStubWithRandSource(src rand.Source) PaginatedReaderStubOption {
	return func(s *PaginatedReaderStub) { s.group.SetRandSource(src) }
}

// PaginatedReaderStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func PaginatedReaderStubBenchMode() PaginatedReaderStubOption {
	return func(s *PaginatedReaderStub) { s.group.BenchMode() }
}

// WithPaginatedReaderPage sets Page's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithPaginatedReaderPage(fn func(context.Context, int) ([]paginatedreader.Value, int, error)) PaginatedReaderStubOption {
	return func(s *PaginatedReaderStub) { s.OnPage.Func(fn) }
}

// PaginatedReaderStub is a recording test double for PaginatedReader.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type PaginatedReaderStub struct {
	OnPage *PaginatedReaderPageStub

	// group is every method stub above, viewed through the surface that
	// does not depend on a signature — one home for the settings and the
	// lifecycle that apply to the whole double.
	group  *stub.Group
	strict bool
}

// Compile-time proof that PaginatedReaderStub satisfies PaginatedReader.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ paginatedreader.PaginatedReader = (*PaginatedReaderStub)(nil)

// NewPaginatedReaderStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewPaginatedReaderStub(tb testing.TB, opts ...PaginatedReaderStubOption) *PaginatedReaderStub {
	s := &PaginatedReaderStub{
		OnPage: &PaginatedReaderPageStub{MethodStub: stub.NewMethodStub[PaginatedReaderPageCall](tb, "PaginatedReader.Page")},
	}
	s.group = stub.NewGroup(
		s.OnPage.MethodStub,
	)
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.group.StrictAll()
	}
	s.group.Bind(tb)
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *PaginatedReaderStub) ResetCalls() {
	s.group.Reset()
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *PaginatedReaderPageStub) invoke(ctx context.Context, cursor int) func() PaginatedReaderPageReturn {
	if s.fn == nil {
		return nil
	}
	return func() PaginatedReaderPageReturn {
		items, next, err := s.fn(ctx, cursor)
		return PaginatedReaderPageReturn{Items: items, Next: next, Err: err}
	}
}

// Page records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *PaginatedReaderStub) Page(ctx context.Context, cursor int) (items []paginatedreader.Value, next int, err error) {
	call := PaginatedReaderPageCall{Ctx: ctx, Cursor: cursor}
	r := stub.Answer(s.OnPage.MethodStub, &call, stub.Arms[PaginatedReaderPageCall, PaginatedReaderPageReturn]{
		Invoke:   s.OnPage.invoke(ctx, cursor),
		Fallback: s.OnPage.fallback,
		Fault:    func(err error) PaginatedReaderPageReturn { return PaginatedReaderPageReturn{Err: err} },
		Stamp: func(c *PaginatedReaderPageCall, r PaginatedReaderPageReturn) {
			c.Items = r.Items
			c.Next = r.Next
			c.Err = r.Err
		},
	})
	return r.Items, r.Next, r.Err
}

// testkit: end of generated content.
// testkit:provenance 791f0d535c1d767218a71bdc06d91c96c6d39ce02354659e68747ef44e2dfca6
