// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/publisher-atmostonce/iface.go
// Plugins:   golang 1.0.0, suite 1.21.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package publisheratmostoncetest_test

import (
	"context"
	"errors"
	"testing"

	publisheratmostonce "go.thesmos.sh/testkit/conformance/corpus/iface/contract/publisher-atmostonce"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/publisher-atmostonce/publisheratmostoncetest"
	"go.thesmos.sh/testkit/engine/suite"
	"go.thesmos.sh/testkit/engine/suite/prove"
)

// TestContractProofs drives every planted defect through the check it is
// evidence for.
//
// A red here is the expected outcome for each one; a GREEN is the finding.
// It means a check tolerated the implementation built to break it, and a
// check that cannot fail is a line in a report rather than a claim about
// the subject.
func TestContractProofs(t *testing.T) {
	t.Parallel()
	prove.All(t,
		publisheratmostoncetest.ContractSuite.Suite(publisheratmostoncetest.DefaultContractFixture()).Checks,
		contractProofs())
}

// contractProofs is every defect this run derived and can spell.
//
// Each is the smallest implementation that breaks exactly one claim: the
// generated double with one method overridden, and nothing else changed.
// The reason beside it is the substring the red must contain, so a defect
// that died on an unrelated guard stops counting as evidence.
func contractProofs() prove.Defects[publisheratmostoncetest.Contract] {
	ix := publisheratmostoncetest.ContractSuite.Checks
	return prove.Defects[publisheratmostoncetest.Contract]{
		ix.Publish.Smoke(): prove.One("a Contract whose Publish panics",
			func(tb testing.TB) publisheratmostoncetest.Contract {
				return publisheratmostoncetest.NewContractStub(tb, publisheratmostoncetest.WithContractPublish(
					func(_ context.Context, _ publisheratmostonce.Value) error {
						panic("planted: Publish panics")
					}))
			}).Reasoned(suite.RedPanicked),
		ix.Publish.Cancels(): prove.One("a Contract whose Publish ignores the context it is handed",
			func(tb testing.TB) publisheratmostoncetest.Contract {
				return publisheratmostoncetest.NewContractStub(tb, publisheratmostoncetest.WithContractPublish(
					func(_ context.Context, _ publisheratmostonce.Value) (err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedCancelled),
		ix.Publish.NilContext(): prove.One("a Contract whose Publish forgives a nil context and answers",
			func(tb testing.TB) publisheratmostoncetest.Contract {
				return publisheratmostoncetest.NewContractStub(tb, publisheratmostoncetest.WithContractPublish(
					func(_ context.Context, _ publisheratmostonce.Value) (err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedNilContext),
		ix.Publish.Deadline(): prove.One("a Contract whose Publish ignores the context it is handed",
			func(tb testing.TB) publisheratmostoncetest.Contract {
				return publisheratmostoncetest.NewContractStub(tb, publisheratmostoncetest.WithContractPublish(
					func(_ context.Context, _ publisheratmostonce.Value) (err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedDeadline),
		ix.Subscribe.Smoke(): prove.One("a Contract whose Subscribe panics",
			func(tb testing.TB) publisheratmostoncetest.Contract {
				return publisheratmostoncetest.NewContractStub(tb, publisheratmostoncetest.WithContractSubscribe(
					func(_ context.Context) (<-chan publisheratmostonce.Value, error) {
						panic("planted: Subscribe panics")
					}))
			}).Reasoned(suite.RedPanicked),
		ix.Subscribe.Cancels(): prove.One("a Contract whose Subscribe ignores the context it is handed",
			func(tb testing.TB) publisheratmostoncetest.Contract {
				return publisheratmostoncetest.NewContractStub(tb, publisheratmostoncetest.WithContractSubscribe(
					func(_ context.Context) (r0 <-chan publisheratmostonce.Value, err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedCancelled),
		ix.Subscribe.NilContext(): prove.One("a Contract whose Subscribe forgives a nil context and answers",
			func(tb testing.TB) publisheratmostoncetest.Contract {
				return publisheratmostoncetest.NewContractStub(tb, publisheratmostoncetest.WithContractSubscribe(
					func(_ context.Context) (r0 <-chan publisheratmostonce.Value, err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedNilContext),
		ix.Subscribe.Deadline(): prove.One("a Contract whose Subscribe ignores the context it is handed",
			func(tb testing.TB) publisheratmostoncetest.Contract {
				return publisheratmostoncetest.NewContractStub(tb, publisheratmostoncetest.WithContractSubscribe(
					func(_ context.Context) (r0 <-chan publisheratmostonce.Value, err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedDeadline),
		ix.Subscribe.ZeroOnError(): prove.One("a Contract whose Subscribe answers a believable value beside its error",
			func(tb testing.TB) publisheratmostoncetest.Contract {
				return publisheratmostoncetest.NewContractStub(tb, publisheratmostoncetest.WithContractSubscribe(
					func(_ context.Context) (r0 <-chan publisheratmostonce.Value, err error) {
						// A believable answer beside the refusal. A caller
						// reading the error and one reading the value disagree
						// about what happened, which is the claim's own
						// violation rather than a subject that merely failed.
						r0 = make(chan publisheratmostonce.Value)
						err = errors.New("planted: Subscribe refused with a believable value")
						return
					}))
			}),
	}
}

// TestContractInvariants holds this package to what it says about itself.
//
// Four claims, none of which a consumer writes and none of which any
// other gate makes. The manifest pins the assertion SET, so a check that
// stops being emitted reads as a removed row in review rather than as a
// suite that quietly got smaller. The index and the drop hints pin the
// two ways a check is NAMED — an index entry pointing at nothing
// compiles, and a drop hint that falls back to the string form forfeits
// exactly the compile-break the index exists to give. The distinct-ID
// check catches two interfaces in one package claiming one identity,
// which method-name scopes make possible.
func TestContractInvariants(t *testing.T) {
	t.Parallel()

	s := publisheratmostoncetest.ContractSuite.Suite(publisheratmostoncetest.DefaultContractFixture())

	rows, err := suite.LockLines(s)
	if err != nil {
		t.Fatalf("render the manifest rows: %v", err)
	}
	suite.VerifyLock(t, "contract.checks.lock", rows)
	suite.VerifyIndex(t, "Contract",
		publisheratmostoncetest.ContractSuite.Checks.All(), s.IDs())
	suite.VerifyDropHints(t, "Contract", s.IDs(), s.DropHint)
	suite.VerifyDistinctIDs(t, s.IDs())
}

// testkit: end of generated content.
// testkit:provenance 3beca8d1bb13662b7a887d599b764a2667444c1919ac0509c2c91d49593ba39d
