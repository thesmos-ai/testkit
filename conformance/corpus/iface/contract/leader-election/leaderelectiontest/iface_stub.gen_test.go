// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/leader-election/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package leaderelectiontest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/leader-election/leaderelectiontest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// contractStubCampaignSubject binds Campaign into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func contractStubCampaignSubject(tb testing.TB) stub.Subject[leaderelectiontest.ContractCampaignCall, leaderelectiontest.ContractCampaignReturn] {
	tb.Helper()
	s := leaderelectiontest.NewContractStub(tb)
	return stub.Subject[leaderelectiontest.ContractCampaignCall, leaderelectiontest.ContractCampaignReturn]{
		Stub: s.OnCampaign.MethodStub,
		Call: func() {
			var a0 context.Context
			_ = s.Campaign(a0)
		},
		Result: func() leaderelectiontest.ContractCampaignReturn {
			var a0 context.Context
			got0 := s.Campaign(a0)
			return leaderelectiontest.ContractCampaignReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnCampaign.Func(func(_ context.Context) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			r0 := s.Campaign(a0)
			return r0
		},
	}
}

// TestContractStubCampaign pins how Campaign answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestContractStubCampaign(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Campaign", contractStubCampaignSubject)

	// No check that Campaign answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := leaderelectiontest.NewContractStub(t)
		var a0 context.Context
		_ = s.Campaign(a0)
		got := s.OnCampaign.AssertCalledOnce(t, "Campaign must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := leaderelectiontest.NewContractStub(t)
		var seen []leaderelectiontest.ContractCampaignCall
		s.OnCampaign.OnRecord(func(c leaderelectiontest.ContractCampaignCall) { seen = append(seen, c) })
		var a0 context.Context
		_ = s.Campaign(a0)
		_ = s.Campaign(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Campaign call")
	})

	t.Run("wires WithContractCampaign at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := leaderelectiontest.NewContractStub(t, leaderelectiontest.WithContractCampaign(func(_ context.Context) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		_ = s.Campaign(a0)
		testkit.True(t, called, "WithContractCampaign must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Campaign admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := leaderelectiontest.NewContractStub(t)
		var want0 error
		s.OnCampaign.Returns(want0)
		var a0 context.Context
		_ = s.Campaign(a0)
		s.ResetCalls()
		_ = s.Campaign(a0)
		s.OnCampaign.AssertCalledOnce(t, "Campaign must record the call that followed the reset")
	})
}

// contractStubResignSubject binds Resign into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func contractStubResignSubject(tb testing.TB) stub.Subject[leaderelectiontest.ContractResignCall, leaderelectiontest.ContractResignReturn] {
	tb.Helper()
	s := leaderelectiontest.NewContractStub(tb)
	return stub.Subject[leaderelectiontest.ContractResignCall, leaderelectiontest.ContractResignReturn]{
		Stub: s.OnResign.MethodStub,
		Call: func() {
			var a0 context.Context
			_ = s.Resign(a0)
		},
		Result: func() leaderelectiontest.ContractResignReturn {
			var a0 context.Context
			got0 := s.Resign(a0)
			return leaderelectiontest.ContractResignReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnResign.Func(func(_ context.Context) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			r0 := s.Resign(a0)
			return r0
		},
	}
}

// TestContractStubResign pins how Resign answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestContractStubResign(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Resign", contractStubResignSubject)

	// No check that Resign answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := leaderelectiontest.NewContractStub(t)
		var a0 context.Context
		_ = s.Resign(a0)
		got := s.OnResign.AssertCalledOnce(t, "Resign must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := leaderelectiontest.NewContractStub(t)
		var seen []leaderelectiontest.ContractResignCall
		s.OnResign.OnRecord(func(c leaderelectiontest.ContractResignCall) { seen = append(seen, c) })
		var a0 context.Context
		_ = s.Resign(a0)
		_ = s.Resign(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Resign call")
	})

	t.Run("wires WithContractResign at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := leaderelectiontest.NewContractStub(t, leaderelectiontest.WithContractResign(func(_ context.Context) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		_ = s.Resign(a0)
		testkit.True(t, called, "WithContractResign must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Resign admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := leaderelectiontest.NewContractStub(t)
		var want0 error
		s.OnResign.Returns(want0)
		var a0 context.Context
		_ = s.Resign(a0)
		s.ResetCalls()
		_ = s.Resign(a0)
		s.OnResign.AssertCalledOnce(t, "Resign must record the call that followed the reset")
	})
}

// contractStubIsLeaderSubject binds IsLeader into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func contractStubIsLeaderSubject(tb testing.TB) stub.Subject[leaderelectiontest.ContractIsLeaderCall, leaderelectiontest.ContractIsLeaderReturn] {
	tb.Helper()
	s := leaderelectiontest.NewContractStub(tb)
	return stub.Subject[leaderelectiontest.ContractIsLeaderCall, leaderelectiontest.ContractIsLeaderReturn]{
		Stub: s.OnIsLeader.MethodStub,
		Call: func() {
			var a0 context.Context
			_ = s.IsLeader(a0)
		},
		Result: func() leaderelectiontest.ContractIsLeaderReturn {
			var a0 context.Context
			got0 := s.IsLeader(a0)
			return leaderelectiontest.ContractIsLeaderReturn{Result: got0}
		},
		Override: func(mark func()) {
			s.OnIsLeader.Func(func(_ context.Context) bool {
				mark()
				var z0 bool
				return z0
			})
		},
	}
}

// TestContractStubIsLeader pins how IsLeader answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestContractStubIsLeader(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "IsLeader", contractStubIsLeaderSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := leaderelectiontest.NewContractStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 bool = true
		s.OnIsLeader.Returns(want0)
		var a0 context.Context
		got0 := s.IsLeader(a0)
		testkit.Equal(t, got0, want0, "IsLeader must answer with what Returns pinned")
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := leaderelectiontest.NewContractStub(t)
		var a0 context.Context
		_ = s.IsLeader(a0)
		got := s.OnIsLeader.AssertCalledOnce(t, "IsLeader must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := leaderelectiontest.NewContractStub(t)
		var seen []leaderelectiontest.ContractIsLeaderCall
		s.OnIsLeader.OnRecord(func(c leaderelectiontest.ContractIsLeaderCall) { seen = append(seen, c) })
		var a0 context.Context
		_ = s.IsLeader(a0)
		_ = s.IsLeader(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per IsLeader call")
	})

	t.Run("wires WithContractIsLeader at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := leaderelectiontest.NewContractStub(t, leaderelectiontest.WithContractIsLeader(func(_ context.Context) bool {
			called = true
			var z0 bool
			return z0
		}))
		var a0 context.Context
		_ = s.IsLeader(a0)
		testkit.True(t, called, "WithContractIsLeader must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		//
		// Pinned to a literal for the reason the Returns check is: a reset
		// that lost the configuration answers the zero, and so does one that
		// kept a configured zero.
		s := leaderelectiontest.NewContractStub(t)
		var want0 bool = true
		s.OnIsLeader.Returns(want0)
		var a0 context.Context
		_ = s.IsLeader(a0)
		s.ResetCalls()
		got0 := s.IsLeader(a0)
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
	})
}

// contractStubDouble describes how to build a ContractStub under each
// option whose effect is the same whatever a method's signature.
//
// Campaign stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func contractStubDouble() stub.Double[leaderelectiontest.ContractCampaignCall] {
	instance := func(s *leaderelectiontest.ContractStub) stub.Instance[leaderelectiontest.ContractCampaignCall] {
		return stub.Instance[leaderelectiontest.ContractCampaignCall]{
			Stub: s.OnCampaign.MethodStub,
			Call: func() {
				var a0 context.Context
				_ = s.Campaign(a0)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[leaderelectiontest.ContractCampaignCall]{
		New: func(tb testing.TB) stub.Instance[leaderelectiontest.ContractCampaignCall] {
			return instance(leaderelectiontest.NewContractStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[leaderelectiontest.ContractCampaignCall] {
			return instance(leaderelectiontest.NewContractStub(tb, leaderelectiontest.ContractStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[leaderelectiontest.ContractCampaignCall] {
			return instance(leaderelectiontest.NewContractStub(tb, leaderelectiontest.ContractStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[leaderelectiontest.ContractCampaignCall] {
			return instance(leaderelectiontest.NewContractStub(tb, leaderelectiontest.ContractStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[leaderelectiontest.ContractCampaignCall] {
			return instance(leaderelectiontest.NewContractStub(tb, leaderelectiontest.ContractStubStrict()))
		},
	}
}

// TestContractStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestContractStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, contractStubDouble())
}

// TestContractStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestContractStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := leaderelectiontest.NewContractStub(t)
	s := leaderelectiontest.NewContractStub(t, leaderelectiontest.ContractStubDelegateTo(inner))

	t.Run("forwards Campaign to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_ = s.Campaign(a0)
		inner.OnCampaign.AssertCalledOnce(t, "Campaign must reach the wrapped implementation")
	})

	t.Run("surfaces what Campaign answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Campaign-delegate")
		inner.OnCampaign.FaultsFor(time.Hour, want)
		var a0 context.Context
		r0 := s.Campaign(a0)
		testkit.ErrorIs(t, r0, want, "Campaign must surface the wrapped answer")
	})

	t.Run("forwards Resign to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_ = s.Resign(a0)
		inner.OnResign.AssertCalledOnce(t, "Resign must reach the wrapped implementation")
	})

	t.Run("surfaces what Resign answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Resign-delegate")
		inner.OnResign.FaultsFor(time.Hour, want)
		var a0 context.Context
		r0 := s.Resign(a0)
		testkit.ErrorIs(t, r0, want, "Resign must surface the wrapped answer")
	})

	t.Run("forwards IsLeader to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_ = s.IsLeader(a0)
		inner.OnIsLeader.AssertCalledOnce(t, "IsLeader must reach the wrapped implementation")
	})
}

// testkit: end of generated content.
// testkit:provenance 3a131731af0aa66c512749ee8bae7abcb63c4a26ad4a353252e3a0f9ceb6db75
