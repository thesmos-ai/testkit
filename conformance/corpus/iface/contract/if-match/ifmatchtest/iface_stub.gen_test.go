// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/if-match/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package ifmatchtest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	ifmatch "go.thesmos.sh/testkit/conformance/corpus/iface/contract/if-match"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/if-match/ifmatchtest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// contractStubPutSubject binds Put into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func contractStubPutSubject(tb testing.TB) stub.Subject[ifmatchtest.ContractPutCall, ifmatchtest.ContractPutReturn] {
	tb.Helper()
	s := ifmatchtest.NewContractStub(tb)
	return stub.Subject[ifmatchtest.ContractPutCall, ifmatchtest.ContractPutReturn]{
		Stub: s.OnPut.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 ifmatch.Value
			_ = s.Put(a0, a1)
		},
		Result: func() ifmatchtest.ContractPutReturn {
			var a0 context.Context
			var a1 ifmatch.Value
			got0 := s.Put(a0, a1)
			return ifmatchtest.ContractPutReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnPut.Func(func(_ context.Context, _ ifmatch.Value) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 ifmatch.Value
			r0 := s.Put(a0, a1)
			return r0
		},
	}
}

// TestContractStubPut pins how Put answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestContractStubPut(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Put", contractStubPutSubject)

	// No check that Put answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := ifmatchtest.NewContractStub(t)
		var a0 context.Context
		var a1 ifmatch.Value
		_ = s.Put(a0, a1)
		got := s.OnPut.AssertCalledOnce(t, "Put must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.V, a1, "the recorded call carries V")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := ifmatchtest.NewContractStub(t)
		var seen []ifmatchtest.ContractPutCall
		s.OnPut.OnRecord(func(c ifmatchtest.ContractPutCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 ifmatch.Value
		_ = s.Put(a0, a1)
		_ = s.Put(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Put call")
	})

	t.Run("wires WithContractPut at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := ifmatchtest.NewContractStub(t, ifmatchtest.WithContractPut(func(_ context.Context, _ ifmatch.Value) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		var a1 ifmatch.Value
		_ = s.Put(a0, a1)
		testkit.True(t, called, "WithContractPut must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Put admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := ifmatchtest.NewContractStub(t)
		var want0 error
		s.OnPut.Returns(want0)
		var a0 context.Context
		var a1 ifmatch.Value
		_ = s.Put(a0, a1)
		s.ResetCalls()
		_ = s.Put(a0, a1)
		s.OnPut.AssertCalledOnce(t, "Put must record the call that followed the reset")
	})
}

// contractStubMatchSubject binds Match into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func contractStubMatchSubject(tb testing.TB) stub.Subject[ifmatchtest.ContractMatchCall, ifmatchtest.ContractMatchReturn] {
	tb.Helper()
	s := ifmatchtest.NewContractStub(tb)
	return stub.Subject[ifmatchtest.ContractMatchCall, ifmatchtest.ContractMatchReturn]{
		Stub: s.OnMatch.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 ifmatch.Value
			_, _ = s.Match(a0, a1)
		},
		Result: func() ifmatchtest.ContractMatchReturn {
			var a0 context.Context
			var a1 ifmatch.Value
			got0, got1 := s.Match(a0, a1)
			return ifmatchtest.ContractMatchReturn{Result: got0, Err: got1}
		},
		Override: func(mark func()) {
			s.OnMatch.Func(func(_ context.Context, _ ifmatch.Value) (bool, error) {
				mark()
				var z0 bool
				var z1 error
				return z0, z1
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 ifmatch.Value
			_, r1 := s.Match(a0, a1)
			return r1
		},
	}
}

// TestContractStubMatch pins how Match answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestContractStubMatch(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Match", contractStubMatchSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := ifmatchtest.NewContractStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 bool = true
		var want1 error
		s.OnMatch.Returns(want0, want1)
		var a0 context.Context
		var a1 ifmatch.Value
		got0, _ := s.Match(a0, a1)
		testkit.Equal(t, got0, want0, "Match must answer with what Returns pinned")
		// Blanked above rather than dropped in silence. No literal exists
		// for result 1 (error), so the only value to pin there
		// is the zero — and a double that ignored Returns answers the zero
		// too, which makes the comparison pass either way.
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := ifmatchtest.NewContractStub(t)
		var a0 context.Context
		var a1 ifmatch.Value
		_, _ = s.Match(a0, a1)
		got := s.OnMatch.AssertCalledOnce(t, "Match must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.V, a1, "the recorded call carries V")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := ifmatchtest.NewContractStub(t)
		var seen []ifmatchtest.ContractMatchCall
		s.OnMatch.OnRecord(func(c ifmatchtest.ContractMatchCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 ifmatch.Value
		_, _ = s.Match(a0, a1)
		_, _ = s.Match(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Match call")
	})

	t.Run("wires WithContractMatch at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := ifmatchtest.NewContractStub(t, ifmatchtest.WithContractMatch(func(_ context.Context, _ ifmatch.Value) (bool, error) {
			called = true
			var z0 bool
			var z1 error
			return z0, z1
		}))
		var a0 context.Context
		var a1 ifmatch.Value
		_, _ = s.Match(a0, a1)
		testkit.True(t, called, "WithContractMatch must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		//
		// Pinned to a literal for the reason the Returns check is: a reset
		// that lost the configuration answers the zero, and so does one that
		// kept a configured zero.
		s := ifmatchtest.NewContractStub(t)
		var want0 bool = true
		var want1 error
		s.OnMatch.Returns(want0, want1)
		var a0 context.Context
		var a1 ifmatch.Value
		_, _ = s.Match(a0, a1)
		s.ResetCalls()
		got0, _ := s.Match(a0, a1)
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
	})
}

// contractStubDouble describes how to build a ContractStub under each
// option whose effect is the same whatever a method's signature.
//
// Put stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func contractStubDouble() stub.Double[ifmatchtest.ContractPutCall] {
	instance := func(s *ifmatchtest.ContractStub) stub.Instance[ifmatchtest.ContractPutCall] {
		return stub.Instance[ifmatchtest.ContractPutCall]{
			Stub: s.OnPut.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 ifmatch.Value
				_ = s.Put(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[ifmatchtest.ContractPutCall]{
		New: func(tb testing.TB) stub.Instance[ifmatchtest.ContractPutCall] {
			return instance(ifmatchtest.NewContractStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[ifmatchtest.ContractPutCall] {
			return instance(ifmatchtest.NewContractStub(tb, ifmatchtest.ContractStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[ifmatchtest.ContractPutCall] {
			return instance(ifmatchtest.NewContractStub(tb, ifmatchtest.ContractStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[ifmatchtest.ContractPutCall] {
			return instance(ifmatchtest.NewContractStub(tb, ifmatchtest.ContractStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[ifmatchtest.ContractPutCall] {
			return instance(ifmatchtest.NewContractStub(tb, ifmatchtest.ContractStubStrict()))
		},
	}
}

// TestContractStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestContractStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, contractStubDouble())
}

// TestContractStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestContractStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := ifmatchtest.NewContractStub(t)
	s := ifmatchtest.NewContractStub(t, ifmatchtest.ContractStubDelegateTo(inner))

	t.Run("forwards Put to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 ifmatch.Value
		_ = s.Put(a0, a1)
		inner.OnPut.AssertCalledOnce(t, "Put must reach the wrapped implementation")
	})

	t.Run("surfaces what Put answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Put-delegate")
		inner.OnPut.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 ifmatch.Value
		r0 := s.Put(a0, a1)
		testkit.ErrorIs(t, r0, want, "Put must surface the wrapped answer")
	})

	t.Run("forwards Match to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 ifmatch.Value
		_, _ = s.Match(a0, a1)
		inner.OnMatch.AssertCalledOnce(t, "Match must reach the wrapped implementation")
	})

	t.Run("surfaces what Match answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Match-delegate")
		inner.OnMatch.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 ifmatch.Value
		_, r1 := s.Match(a0, a1)
		testkit.ErrorIs(t, r1, want, "Match must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 57e904086689ab9a6f70e5d8a8c1eafdc66225e805b14fe661b47e67c46f3749
