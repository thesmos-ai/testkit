// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/workflow/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package workflowtest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/workflow/workflowtest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// contractStubRunSubject binds Run into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func contractStubRunSubject(tb testing.TB) stub.Subject[workflowtest.ContractRunCall, workflowtest.ContractRunReturn] {
	tb.Helper()
	s := workflowtest.NewContractStub(tb)
	return stub.Subject[workflowtest.ContractRunCall, workflowtest.ContractRunReturn]{
		Stub: s.OnRun.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string = "test-key"
			_ = s.Run(a0, a1)
		},
		Result: func() workflowtest.ContractRunReturn {
			var a0 context.Context
			var a1 string = "test-key"
			got0 := s.Run(a0, a1)
			return workflowtest.ContractRunReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnRun.Func(func(_ context.Context, _ string) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 string = "test-key"
			r0 := s.Run(a0, a1)
			return r0
		},
	}
}

// TestContractStubRun pins how Run answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestContractStubRun(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Run", contractStubRunSubject)

	// No check that Run answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := workflowtest.NewContractStub(t)
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Run(a0, a1)
		got := s.OnRun.AssertCalledOnce(t, "Run must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Key, a1, "the recorded call carries Key")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := workflowtest.NewContractStub(t)
		var seen []workflowtest.ContractRunCall
		s.OnRun.OnRecord(func(c workflowtest.ContractRunCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Run(a0, a1)
		_ = s.Run(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Run call")
	})

	t.Run("wires WithContractRun at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := workflowtest.NewContractStub(t, workflowtest.WithContractRun(func(_ context.Context, _ string) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Run(a0, a1)
		testkit.True(t, called, "WithContractRun must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Run admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := workflowtest.NewContractStub(t)
		var want0 error
		s.OnRun.Returns(want0)
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Run(a0, a1)
		s.ResetCalls()
		_ = s.Run(a0, a1)
		s.OnRun.AssertCalledOnce(t, "Run must record the call that followed the reset")
	})
}

// contractStubStateSubject binds State into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func contractStubStateSubject(tb testing.TB) stub.Subject[workflowtest.ContractStateCall, workflowtest.ContractStateReturn] {
	tb.Helper()
	s := workflowtest.NewContractStub(tb)
	return stub.Subject[workflowtest.ContractStateCall, workflowtest.ContractStateReturn]{
		Stub: s.OnState.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string = "test-key"
			_, _ = s.State(a0, a1)
		},
		Result: func() workflowtest.ContractStateReturn {
			var a0 context.Context
			var a1 string = "test-key"
			got0, got1 := s.State(a0, a1)
			return workflowtest.ContractStateReturn{Result: got0, Err: got1}
		},
		Override: func(mark func()) {
			s.OnState.Func(func(_ context.Context, _ string) (string, error) {
				mark()
				var z0 string
				var z1 error
				return z0, z1
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 string = "test-key"
			_, r1 := s.State(a0, a1)
			return r1
		},
	}
}

// TestContractStubState pins how State answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestContractStubState(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "State", contractStubStateSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := workflowtest.NewContractStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 string = "test-state"
		var want1 error
		s.OnState.Returns(want0, want1)
		var a0 context.Context
		var a1 string = "test-key"
		got0, _ := s.State(a0, a1)
		testkit.Equal(t, got0, want0, "State must answer with what Returns pinned")
		// Blanked above rather than dropped in silence. No literal exists
		// for result 1 (error), so the only value to pin there
		// is the zero — and a double that ignored Returns answers the zero
		// too, which makes the comparison pass either way.
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := workflowtest.NewContractStub(t)
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.State(a0, a1)
		got := s.OnState.AssertCalledOnce(t, "State must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Key, a1, "the recorded call carries Key")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := workflowtest.NewContractStub(t)
		var seen []workflowtest.ContractStateCall
		s.OnState.OnRecord(func(c workflowtest.ContractStateCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.State(a0, a1)
		_, _ = s.State(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per State call")
	})

	t.Run("wires WithContractState at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := workflowtest.NewContractStub(t, workflowtest.WithContractState(func(_ context.Context, _ string) (string, error) {
			called = true
			var z0 string
			var z1 error
			return z0, z1
		}))
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.State(a0, a1)
		testkit.True(t, called, "WithContractState must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		//
		// Pinned to a literal for the reason the Returns check is: a reset
		// that lost the configuration answers the zero, and so does one that
		// kept a configured zero.
		s := workflowtest.NewContractStub(t)
		var want0 string = "test-state"
		var want1 error
		s.OnState.Returns(want0, want1)
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.State(a0, a1)
		s.ResetCalls()
		got0, _ := s.State(a0, a1)
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
	})
}

// contractStubDouble describes how to build a ContractStub under each
// option whose effect is the same whatever a method's signature.
//
// Run stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func contractStubDouble() stub.Double[workflowtest.ContractRunCall] {
	instance := func(s *workflowtest.ContractStub) stub.Instance[workflowtest.ContractRunCall] {
		return stub.Instance[workflowtest.ContractRunCall]{
			Stub: s.OnRun.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 string = "test-key"
				_ = s.Run(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[workflowtest.ContractRunCall]{
		New: func(tb testing.TB) stub.Instance[workflowtest.ContractRunCall] {
			return instance(workflowtest.NewContractStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[workflowtest.ContractRunCall] {
			return instance(workflowtest.NewContractStub(tb, workflowtest.ContractStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[workflowtest.ContractRunCall] {
			return instance(workflowtest.NewContractStub(tb, workflowtest.ContractStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[workflowtest.ContractRunCall] {
			return instance(workflowtest.NewContractStub(tb, workflowtest.ContractStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[workflowtest.ContractRunCall] {
			return instance(workflowtest.NewContractStub(tb, workflowtest.ContractStubStrict()))
		},
	}
}

// TestContractStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestContractStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, contractStubDouble())
}

// TestContractStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestContractStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := workflowtest.NewContractStub(t)
	s := workflowtest.NewContractStub(t, workflowtest.ContractStubDelegateTo(inner))

	t.Run("forwards Run to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Run(a0, a1)
		inner.OnRun.AssertCalledOnce(t, "Run must reach the wrapped implementation")
	})

	t.Run("surfaces what Run answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Run-delegate")
		inner.OnRun.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 string = "test-key"
		r0 := s.Run(a0, a1)
		testkit.ErrorIs(t, r0, want, "Run must surface the wrapped answer")
	})

	t.Run("forwards State to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.State(a0, a1)
		inner.OnState.AssertCalledOnce(t, "State must reach the wrapped implementation")
	})

	t.Run("surfaces what State answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("State-delegate")
		inner.OnState.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 string = "test-key"
		_, r1 := s.State(a0, a1)
		testkit.ErrorIs(t, r1, want, "State must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 9cf0d689ac4d994993007ceb45eca1494650cd4eabd51cb1269085b2919b5635
