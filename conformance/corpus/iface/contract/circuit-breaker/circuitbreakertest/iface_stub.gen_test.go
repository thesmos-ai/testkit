// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/circuit-breaker/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package circuitbreakertest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/circuit-breaker/circuitbreakertest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// contractStubRunSubject binds Run into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func contractStubRunSubject(tb testing.TB) stub.Subject[circuitbreakertest.ContractRunCall, circuitbreakertest.ContractRunReturn] {
	tb.Helper()
	s := circuitbreakertest.NewContractStub(tb)
	return stub.Subject[circuitbreakertest.ContractRunCall, circuitbreakertest.ContractRunReturn]{
		Stub: s.OnRun.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string = "test-key"
			_ = s.Run(a0, a1)
		},
		Result: func() circuitbreakertest.ContractRunReturn {
			var a0 context.Context
			var a1 string = "test-key"
			got0 := s.Run(a0, a1)
			return circuitbreakertest.ContractRunReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnRun.Func(func(_ context.Context, _ string) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 string = "test-key"
			r0 := s.Run(a0, a1)
			return r0
		},
	}
}

// TestContractStubRun pins how Run answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestContractStubRun(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Run", contractStubRunSubject)

	// No check that Run answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := circuitbreakertest.NewContractStub(t)
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Run(a0, a1)
		got := s.OnRun.AssertCalledOnce(t, "Run must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Key, a1, "the recorded call carries Key")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := circuitbreakertest.NewContractStub(t)
		var seen []circuitbreakertest.ContractRunCall
		s.OnRun.OnRecord(func(c circuitbreakertest.ContractRunCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Run(a0, a1)
		_ = s.Run(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Run call")
	})

	t.Run("wires WithContractRun at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := circuitbreakertest.NewContractStub(t, circuitbreakertest.WithContractRun(func(_ context.Context, _ string) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Run(a0, a1)
		testkit.True(t, called, "WithContractRun must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Run admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := circuitbreakertest.NewContractStub(t)
		var want0 error
		s.OnRun.Returns(want0)
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Run(a0, a1)
		s.ResetCalls()
		_ = s.Run(a0, a1)
		s.OnRun.AssertCalledOnce(t, "Run must record the call that followed the reset")
	})
}

// contractStubDouble describes how to build a ContractStub under each
// option whose effect is the same whatever a method's signature.
//
// Run stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func contractStubDouble() stub.Double[circuitbreakertest.ContractRunCall] {
	instance := func(s *circuitbreakertest.ContractStub) stub.Instance[circuitbreakertest.ContractRunCall] {
		return stub.Instance[circuitbreakertest.ContractRunCall]{
			Stub: s.OnRun.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 string = "test-key"
				_ = s.Run(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[circuitbreakertest.ContractRunCall]{
		New: func(tb testing.TB) stub.Instance[circuitbreakertest.ContractRunCall] {
			return instance(circuitbreakertest.NewContractStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[circuitbreakertest.ContractRunCall] {
			return instance(circuitbreakertest.NewContractStub(tb, circuitbreakertest.ContractStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[circuitbreakertest.ContractRunCall] {
			return instance(circuitbreakertest.NewContractStub(tb, circuitbreakertest.ContractStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[circuitbreakertest.ContractRunCall] {
			return instance(circuitbreakertest.NewContractStub(tb, circuitbreakertest.ContractStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[circuitbreakertest.ContractRunCall] {
			return instance(circuitbreakertest.NewContractStub(tb, circuitbreakertest.ContractStubStrict()))
		},
	}
}

// TestContractStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestContractStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, contractStubDouble())
}

// TestContractStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestContractStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := circuitbreakertest.NewContractStub(t)
	s := circuitbreakertest.NewContractStub(t, circuitbreakertest.ContractStubDelegateTo(inner))

	t.Run("forwards Run to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Run(a0, a1)
		inner.OnRun.AssertCalledOnce(t, "Run must reach the wrapped implementation")
	})

	t.Run("surfaces what Run answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Run-delegate")
		inner.OnRun.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 string = "test-key"
		r0 := s.Run(a0, a1)
		testkit.ErrorIs(t, r0, want, "Run must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 8d0c6e8c9878c41ead5645cd8dbf893e71fe5d6269775a7c38dea94c8b9f0e14
