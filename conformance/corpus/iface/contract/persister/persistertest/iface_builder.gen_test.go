// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/persister/iface.go
// Plugins:   golang 1.0.0, builder 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package persistertest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/persister"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/persister/persistertest"
)

// TestNewValue pins what the constructor seeds.
func TestNewValue(t *testing.T) {
	t.Parallel()

	t.Run("seeds Key with its declared default", func(t *testing.T) {
		t.Parallel()
		// A default the constructor drops reads as a field the author never
		// declared, and every test built on it starts from the wrong value.
		testkit.Equal(t, persistertest.NewValue().Build().Key, "test-key",
			"NewValue must seed Key")
	})

	t.Run("seeds Body with its declared default", func(t *testing.T) {
		t.Parallel()
		// A default the constructor drops reads as a field the author never
		// declared, and every test built on it starts from the wrong value.
		testkit.Equal(t, persistertest.NewValue().Build().Body, "test-body",
			"NewValue must seed Body")
	})
}

// TestNewValueFrom pins the seeding constructor, which is what a test
// uses to vary one field of a value it already holds.
func TestNewValueFrom(t *testing.T) {
	t.Parallel()
	t.Run("carries the value through untouched", func(t *testing.T) {
		t.Parallel()
		// Anything it altered would be a field the caller did not ask to
		// change, which defeats the point of seeding from a value at all.
		//
		// Populated, not the zero value. `From(zero).Build() == zero` holds
		// for a constructor that drops every field it is handed, which is the
		// one defect this check exists to catch — so the seed is built
		// through the setters, each already proven above to reach its field.
		seed := persistertest.NewValue().WithKey("test-key").WithBody("test-body").Build()
		testkit.Equal(t, persistertest.NewValueFrom(seed).Build(), seed,
			"NewValueFrom must not alter what it was given")
	})
}

// TestValueBuilderWithKey pins the setter for Key.
func TestValueBuilderWithKey(t *testing.T) {
	t.Parallel()

	t.Run("reaches Key", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, persistertest.NewValue().WithKey("test-key").Build().Key, "test-key",
			"WithKey must reach Key")
		testkit.Equal(t, persistertest.NewValue().WithKey("other-key").Build().Key, "other-key",
			"WithKey must reach Key")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v string
		b := persistertest.NewValue()
		testkit.True(t, b.WithKey(v) == b, "the setter returns its receiver")
	})
}

// TestValueBuilderWithBody pins the setter for Body.
func TestValueBuilderWithBody(t *testing.T) {
	t.Parallel()

	t.Run("reaches Body", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, persistertest.NewValue().WithBody("test-body").Build().Body, "test-body",
			"WithBody must reach Body")
		testkit.Equal(t, persistertest.NewValue().WithBody("other-body").Build().Body, "other-body",
			"WithBody must reach Body")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v string
		b := persistertest.NewValue()
		testkit.True(t, b.WithBody(v) == b, "the setter returns its receiver")
	})
}

// TestValueBuilderMutate pins the escape hatch for a field no setter reaches.
func TestValueBuilderMutate(t *testing.T) {
	t.Parallel()

	t.Run("reaches the value under construction", func(t *testing.T) {
		t.Parallel()
		// Handed a copy, every mutation would be discarded and the caller
		// would see a builder that silently ignored them.
		reached := false
		persistertest.NewValue().Mutate(func(*persister.Value) { reached = true })
		testkit.True(t, reached, "Mutate must be applied")
	})
}

// TestValueBuilderClone pins that a clone can be configured on its own.
func TestValueBuilderClone(t *testing.T) {
	t.Parallel()

	t.Run("carries the configuration across", func(t *testing.T) {
		t.Parallel()
		// A clone starting from nothing would be a second constructor under
		// another name, and a test cloning a configured builder would silently
		// lose everything it had set.
		b := persistertest.NewValue()
		testkit.Equal(t, b.Clone().Build(), b.Build(), "a clone starts where its original stood")
	})
}

// TestValueBuilderBuild pins what the builder hands back.
func TestValueBuilderBuild(t *testing.T) {
	t.Parallel()
	t.Run("returns every field that was configured", func(t *testing.T) {
		t.Parallel()
		// Every field set on the way, not a zero compared against itself:
		// a Build that answered the zero would satisfy the second form
		// however much had been configured.
		seed := persistertest.NewValue().WithKey("test-key").WithBody("test-body").Build()
		testkit.Equal(t, persistertest.NewValueFrom(seed).Build(), seed,
			"Build returns the value under construction")
	})
}

// testkit: end of generated content.
// testkit:provenance 5e160691af5f9af31596bd0c236a88b84fc114b849f741d24fcc9d437ee19ea3
