// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/persister/iface.go
// Plugins:   golang 1.0.0, builder 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package persistertest

import (
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/persister"
)

// ValueBuilder constructs [Value] values for tests.
//
// Start with [NewValue] or [NewValueFrom], set fields with the With
// methods, and call [ValueBuilder.Build]. Every setter returns the builder, so a
// test states only the fields it cares about and the rest keep whatever the
// constructor seeded.
type ValueBuilder struct {
	v persister.Value
}

// NewValue returns a builder seeded with the defaults the source declares.
func NewValue() *ValueBuilder {
	return &ValueBuilder{v: persister.Value{
		Key:  "test-key",
		Body: "test-body",
	}}
}

// NewValueFrom returns a builder seeded with v, for the case where a test
// varies one field of a value it already has.
func NewValueFrom(v persister.Value) *ValueBuilder {
	return &ValueBuilder{v: v}
}

// WithKey replaces Key.
func (b *ValueBuilder) WithKey(v string) *ValueBuilder {
	b.v.Key = v
	return b
}

// WithBody replaces Body.
func (b *ValueBuilder) WithBody(v string) *ValueBuilder {
	b.v.Body = v
	return b
}

// Mutate applies fn to the value under construction, for the field a setter
// does not reach — an unexported one, or a shape this builder does not model.
func (b *ValueBuilder) Mutate(fn func(*persister.Value)) *ValueBuilder {
	fn(&b.v)
	return b
}

// Clone returns a builder that can be configured without affecting this one.
//
// The slice, byte-slice and map fields are copied, so appending through one
// clone is not visible through another. Values held *inside* those — a struct
// in a slice that owns a slice of its own — are shared, as are pointer fields,
// which is what a pointer means.
func (b *ValueBuilder) Clone() *ValueBuilder {
	out := &ValueBuilder{v: b.v}
	return out
}

// Build returns the constructed [Value].
func (b *ValueBuilder) Build() persister.Value {
	return b.v
}

// testkit: end of generated content.
// testkit:provenance b686948c87b5d4cad50e682e24afee08b6d88b01860bec8855568d074175add3
