// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/publisher-redeliver/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package publisherredelivertest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	publisherredeliver "go.thesmos.sh/testkit/conformance/corpus/iface/contract/publisher-redeliver"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/publisher-redeliver/publisherredelivertest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// contractStubPublishSubject binds Publish into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func contractStubPublishSubject(tb testing.TB) stub.Subject[publisherredelivertest.ContractPublishCall, publisherredelivertest.ContractPublishReturn] {
	tb.Helper()
	s := publisherredelivertest.NewContractStub(tb)
	return stub.Subject[publisherredelivertest.ContractPublishCall, publisherredelivertest.ContractPublishReturn]{
		Stub: s.OnPublish.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 publisherredeliver.Value
			_ = s.Publish(a0, a1)
		},
		Result: func() publisherredelivertest.ContractPublishReturn {
			var a0 context.Context
			var a1 publisherredeliver.Value
			got0 := s.Publish(a0, a1)
			return publisherredelivertest.ContractPublishReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnPublish.Func(func(_ context.Context, _ publisherredeliver.Value) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 publisherredeliver.Value
			r0 := s.Publish(a0, a1)
			return r0
		},
	}
}

// TestContractStubPublish pins how Publish answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestContractStubPublish(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Publish", contractStubPublishSubject)

	// No check that Publish answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := publisherredelivertest.NewContractStub(t)
		var a0 context.Context
		var a1 publisherredeliver.Value
		_ = s.Publish(a0, a1)
		got := s.OnPublish.AssertCalledOnce(t, "Publish must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.V, a1, "the recorded call carries V")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := publisherredelivertest.NewContractStub(t)
		var seen []publisherredelivertest.ContractPublishCall
		s.OnPublish.OnRecord(func(c publisherredelivertest.ContractPublishCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 publisherredeliver.Value
		_ = s.Publish(a0, a1)
		_ = s.Publish(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Publish call")
	})

	t.Run("wires WithContractPublish at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := publisherredelivertest.NewContractStub(t, publisherredelivertest.WithContractPublish(func(_ context.Context, _ publisherredeliver.Value) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		var a1 publisherredeliver.Value
		_ = s.Publish(a0, a1)
		testkit.True(t, called, "WithContractPublish must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Publish admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := publisherredelivertest.NewContractStub(t)
		var want0 error
		s.OnPublish.Returns(want0)
		var a0 context.Context
		var a1 publisherredeliver.Value
		_ = s.Publish(a0, a1)
		s.ResetCalls()
		_ = s.Publish(a0, a1)
		s.OnPublish.AssertCalledOnce(t, "Publish must record the call that followed the reset")
	})
}

// contractStubRepublishSubject binds Republish into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func contractStubRepublishSubject(tb testing.TB) stub.Subject[publisherredelivertest.ContractRepublishCall, publisherredelivertest.ContractRepublishReturn] {
	tb.Helper()
	s := publisherredelivertest.NewContractStub(tb)
	return stub.Subject[publisherredelivertest.ContractRepublishCall, publisherredelivertest.ContractRepublishReturn]{
		Stub: s.OnRepublish.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 publisherredeliver.Value
			_ = s.Republish(a0, a1)
		},
		Result: func() publisherredelivertest.ContractRepublishReturn {
			var a0 context.Context
			var a1 publisherredeliver.Value
			got0 := s.Republish(a0, a1)
			return publisherredelivertest.ContractRepublishReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnRepublish.Func(func(_ context.Context, _ publisherredeliver.Value) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 publisherredeliver.Value
			r0 := s.Republish(a0, a1)
			return r0
		},
	}
}

// TestContractStubRepublish pins how Republish answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestContractStubRepublish(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Republish", contractStubRepublishSubject)

	// No check that Republish answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := publisherredelivertest.NewContractStub(t)
		var a0 context.Context
		var a1 publisherredeliver.Value
		_ = s.Republish(a0, a1)
		got := s.OnRepublish.AssertCalledOnce(t, "Republish must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.V, a1, "the recorded call carries V")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := publisherredelivertest.NewContractStub(t)
		var seen []publisherredelivertest.ContractRepublishCall
		s.OnRepublish.OnRecord(func(c publisherredelivertest.ContractRepublishCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 publisherredeliver.Value
		_ = s.Republish(a0, a1)
		_ = s.Republish(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Republish call")
	})

	t.Run("wires WithContractRepublish at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := publisherredelivertest.NewContractStub(t, publisherredelivertest.WithContractRepublish(func(_ context.Context, _ publisherredeliver.Value) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		var a1 publisherredeliver.Value
		_ = s.Republish(a0, a1)
		testkit.True(t, called, "WithContractRepublish must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Republish admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := publisherredelivertest.NewContractStub(t)
		var want0 error
		s.OnRepublish.Returns(want0)
		var a0 context.Context
		var a1 publisherredeliver.Value
		_ = s.Republish(a0, a1)
		s.ResetCalls()
		_ = s.Republish(a0, a1)
		s.OnRepublish.AssertCalledOnce(t, "Republish must record the call that followed the reset")
	})
}

// contractStubSubscribeSubject binds Subscribe into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func contractStubSubscribeSubject(tb testing.TB) stub.Subject[publisherredelivertest.ContractSubscribeCall, publisherredelivertest.ContractSubscribeReturn] {
	tb.Helper()
	s := publisherredelivertest.NewContractStub(tb)
	return stub.Subject[publisherredelivertest.ContractSubscribeCall, publisherredelivertest.ContractSubscribeReturn]{
		Stub: s.OnSubscribe.MethodStub,
		Call: func() {
			var a0 context.Context
			_, _ = s.Subscribe(a0)
		},
		Result: func() publisherredelivertest.ContractSubscribeReturn {
			var a0 context.Context
			got0, got1 := s.Subscribe(a0)
			return publisherredelivertest.ContractSubscribeReturn{Result: got0, Err: got1}
		},
		Override: func(mark func()) {
			s.OnSubscribe.Func(func(_ context.Context) (<-chan publisherredeliver.Value, error) {
				mark()
				var z0 <-chan publisherredeliver.Value
				var z1 error
				return z0, z1
			})
		},
		Fails: func() error {
			var a0 context.Context
			_, r1 := s.Subscribe(a0)
			return r1
		},
	}
}

// TestContractStubSubscribe pins how Subscribe answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestContractStubSubscribe(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Subscribe", contractStubSubscribeSubject)

	// No check that Subscribe answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := publisherredelivertest.NewContractStub(t)
		var a0 context.Context
		_, _ = s.Subscribe(a0)
		got := s.OnSubscribe.AssertCalledOnce(t, "Subscribe must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := publisherredelivertest.NewContractStub(t)
		var seen []publisherredelivertest.ContractSubscribeCall
		s.OnSubscribe.OnRecord(func(c publisherredelivertest.ContractSubscribeCall) { seen = append(seen, c) })
		var a0 context.Context
		_, _ = s.Subscribe(a0)
		_, _ = s.Subscribe(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Subscribe call")
	})

	t.Run("wires WithContractSubscribe at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := publisherredelivertest.NewContractStub(t, publisherredelivertest.WithContractSubscribe(func(_ context.Context) (<-chan publisherredeliver.Value, error) {
			called = true
			var z0 <-chan publisherredeliver.Value
			var z1 error
			return z0, z1
		}))
		var a0 context.Context
		_, _ = s.Subscribe(a0)
		testkit.True(t, called, "WithContractSubscribe must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Subscribe admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := publisherredelivertest.NewContractStub(t)
		var want0 <-chan publisherredeliver.Value
		var want1 error
		s.OnSubscribe.Returns(want0, want1)
		var a0 context.Context
		_, _ = s.Subscribe(a0)
		s.ResetCalls()
		_, _ = s.Subscribe(a0)
		s.OnSubscribe.AssertCalledOnce(t, "Subscribe must record the call that followed the reset")
	})
}

// contractStubDouble describes how to build a ContractStub under each
// option whose effect is the same whatever a method's signature.
//
// Publish stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func contractStubDouble() stub.Double[publisherredelivertest.ContractPublishCall] {
	instance := func(s *publisherredelivertest.ContractStub) stub.Instance[publisherredelivertest.ContractPublishCall] {
		return stub.Instance[publisherredelivertest.ContractPublishCall]{
			Stub: s.OnPublish.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 publisherredeliver.Value
				_ = s.Publish(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[publisherredelivertest.ContractPublishCall]{
		New: func(tb testing.TB) stub.Instance[publisherredelivertest.ContractPublishCall] {
			return instance(publisherredelivertest.NewContractStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[publisherredelivertest.ContractPublishCall] {
			return instance(publisherredelivertest.NewContractStub(tb, publisherredelivertest.ContractStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[publisherredelivertest.ContractPublishCall] {
			return instance(publisherredelivertest.NewContractStub(tb, publisherredelivertest.ContractStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[publisherredelivertest.ContractPublishCall] {
			return instance(publisherredelivertest.NewContractStub(tb, publisherredelivertest.ContractStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[publisherredelivertest.ContractPublishCall] {
			return instance(publisherredelivertest.NewContractStub(tb, publisherredelivertest.ContractStubStrict()))
		},
	}
}

// TestContractStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestContractStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, contractStubDouble())
}

// TestContractStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestContractStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := publisherredelivertest.NewContractStub(t)
	s := publisherredelivertest.NewContractStub(t, publisherredelivertest.ContractStubDelegateTo(inner))

	t.Run("forwards Publish to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 publisherredeliver.Value
		_ = s.Publish(a0, a1)
		inner.OnPublish.AssertCalledOnce(t, "Publish must reach the wrapped implementation")
	})

	t.Run("surfaces what Publish answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Publish-delegate")
		inner.OnPublish.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 publisherredeliver.Value
		r0 := s.Publish(a0, a1)
		testkit.ErrorIs(t, r0, want, "Publish must surface the wrapped answer")
	})

	t.Run("forwards Republish to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 publisherredeliver.Value
		_ = s.Republish(a0, a1)
		inner.OnRepublish.AssertCalledOnce(t, "Republish must reach the wrapped implementation")
	})

	t.Run("surfaces what Republish answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Republish-delegate")
		inner.OnRepublish.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 publisherredeliver.Value
		r0 := s.Republish(a0, a1)
		testkit.ErrorIs(t, r0, want, "Republish must surface the wrapped answer")
	})

	t.Run("forwards Subscribe to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_, _ = s.Subscribe(a0)
		inner.OnSubscribe.AssertCalledOnce(t, "Subscribe must reach the wrapped implementation")
	})

	t.Run("surfaces what Subscribe answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Subscribe-delegate")
		inner.OnSubscribe.FaultsFor(time.Hour, want)
		var a0 context.Context
		_, r1 := s.Subscribe(a0)
		testkit.ErrorIs(t, r1, want, "Subscribe must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance f190ce14bedc9f19825d7e2ddf1c6282f3304ee0a8af4ca94d47e213d41149ed
