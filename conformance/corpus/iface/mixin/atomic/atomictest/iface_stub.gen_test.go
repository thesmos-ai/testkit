// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/atomic/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package atomictest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/atomic"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/atomic/atomictest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// mixedStubWriteSubject binds Write into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubWriteSubject(tb testing.TB) stub.Subject[atomictest.MixedWriteCall, atomictest.MixedWriteReturn] {
	tb.Helper()
	s := atomictest.NewMixedStub(tb)
	return stub.Subject[atomictest.MixedWriteCall, atomictest.MixedWriteReturn]{
		Stub: s.OnWrite.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 atomic.Entry
			_ = s.Write(a0, a1)
		},
		Result: func() atomictest.MixedWriteReturn {
			var a0 context.Context
			var a1 atomic.Entry
			got0 := s.Write(a0, a1)
			return atomictest.MixedWriteReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnWrite.Func(func(_ context.Context, _ atomic.Entry) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 atomic.Entry
			r0 := s.Write(a0, a1)
			return r0
		},
	}
}

// TestMixedStubWrite pins how Write answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubWrite(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Write", mixedStubWriteSubject)

	// No check that Write answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := atomictest.NewMixedStub(t)
		var a0 context.Context
		var a1 atomic.Entry
		_ = s.Write(a0, a1)
		got := s.OnWrite.AssertCalledOnce(t, "Write must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.E, a1, "the recorded call carries E")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := atomictest.NewMixedStub(t)
		var seen []atomictest.MixedWriteCall
		s.OnWrite.OnRecord(func(c atomictest.MixedWriteCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 atomic.Entry
		_ = s.Write(a0, a1)
		_ = s.Write(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Write call")
	})

	t.Run("wires WithMixedWrite at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := atomictest.NewMixedStub(t, atomictest.WithMixedWrite(func(_ context.Context, _ atomic.Entry) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		var a1 atomic.Entry
		_ = s.Write(a0, a1)
		testkit.True(t, called, "WithMixedWrite must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Write admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := atomictest.NewMixedStub(t)
		var want0 error
		s.OnWrite.Returns(want0)
		var a0 context.Context
		var a1 atomic.Entry
		_ = s.Write(a0, a1)
		s.ResetCalls()
		_ = s.Write(a0, a1)
		s.OnWrite.AssertCalledOnce(t, "Write must record the call that followed the reset")
	})
}

// mixedStubReadSubject binds Read into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubReadSubject(tb testing.TB) stub.Subject[atomictest.MixedReadCall, atomictest.MixedReadReturn] {
	tb.Helper()
	s := atomictest.NewMixedStub(tb)
	return stub.Subject[atomictest.MixedReadCall, atomictest.MixedReadReturn]{
		Stub: s.OnRead.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string = "test-key"
			_, _ = s.Read(a0, a1)
		},
		Result: func() atomictest.MixedReadReturn {
			var a0 context.Context
			var a1 string = "test-key"
			got0, got1 := s.Read(a0, a1)
			return atomictest.MixedReadReturn{Result: got0, Err: got1}
		},
		Override: func(mark func()) {
			s.OnRead.Func(func(_ context.Context, _ string) (atomic.Entry, error) {
				mark()
				var z0 atomic.Entry
				var z1 error
				return z0, z1
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 string = "test-key"
			_, r1 := s.Read(a0, a1)
			return r1
		},
	}
}

// TestMixedStubRead pins how Read answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubRead(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Read", mixedStubReadSubject)

	// No check that Read answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := atomictest.NewMixedStub(t)
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.Read(a0, a1)
		got := s.OnRead.AssertCalledOnce(t, "Read must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Key, a1, "the recorded call carries Key")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := atomictest.NewMixedStub(t)
		var seen []atomictest.MixedReadCall
		s.OnRead.OnRecord(func(c atomictest.MixedReadCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.Read(a0, a1)
		_, _ = s.Read(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Read call")
	})

	t.Run("wires WithMixedRead at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := atomictest.NewMixedStub(t, atomictest.WithMixedRead(func(_ context.Context, _ string) (atomic.Entry, error) {
			called = true
			var z0 atomic.Entry
			var z1 error
			return z0, z1
		}))
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.Read(a0, a1)
		testkit.True(t, called, "WithMixedRead must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Read admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := atomictest.NewMixedStub(t)
		var want0 atomic.Entry
		var want1 error
		s.OnRead.Returns(want0, want1)
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.Read(a0, a1)
		s.ResetCalls()
		_, _ = s.Read(a0, a1)
		s.OnRead.AssertCalledOnce(t, "Read must record the call that followed the reset")
	})
}

// mixedStubDouble describes how to build a MixedStub under each
// option whose effect is the same whatever a method's signature.
//
// Write stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func mixedStubDouble() stub.Double[atomictest.MixedWriteCall] {
	instance := func(s *atomictest.MixedStub) stub.Instance[atomictest.MixedWriteCall] {
		return stub.Instance[atomictest.MixedWriteCall]{
			Stub: s.OnWrite.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 atomic.Entry
				_ = s.Write(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[atomictest.MixedWriteCall]{
		New: func(tb testing.TB) stub.Instance[atomictest.MixedWriteCall] {
			return instance(atomictest.NewMixedStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[atomictest.MixedWriteCall] {
			return instance(atomictest.NewMixedStub(tb, atomictest.MixedStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[atomictest.MixedWriteCall] {
			return instance(atomictest.NewMixedStub(tb, atomictest.MixedStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[atomictest.MixedWriteCall] {
			return instance(atomictest.NewMixedStub(tb, atomictest.MixedStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[atomictest.MixedWriteCall] {
			return instance(atomictest.NewMixedStub(tb, atomictest.MixedStubStrict()))
		},
	}
}

// TestMixedStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestMixedStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, mixedStubDouble())
}

// TestMixedStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestMixedStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := atomictest.NewMixedStub(t)
	s := atomictest.NewMixedStub(t, atomictest.MixedStubDelegateTo(inner))

	t.Run("forwards Write to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 atomic.Entry
		_ = s.Write(a0, a1)
		inner.OnWrite.AssertCalledOnce(t, "Write must reach the wrapped implementation")
	})

	t.Run("surfaces what Write answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Write-delegate")
		inner.OnWrite.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 atomic.Entry
		r0 := s.Write(a0, a1)
		testkit.ErrorIs(t, r0, want, "Write must surface the wrapped answer")
	})

	t.Run("forwards Read to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.Read(a0, a1)
		inner.OnRead.AssertCalledOnce(t, "Read must reach the wrapped implementation")
	})

	t.Run("surfaces what Read answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Read-delegate")
		inner.OnRead.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 string = "test-key"
		_, r1 := s.Read(a0, a1)
		testkit.ErrorIs(t, r1, want, "Read must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 080deb926b37635711e0bcf5087635ef382c6f111f0fd67ca25e43a5489702a2
