// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/bounded/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package boundedtest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/bounded/boundedtest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// mixedStubAddSubject binds Add into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubAddSubject(tb testing.TB) stub.Subject[boundedtest.MixedAddCall, boundedtest.MixedAddReturn] {
	tb.Helper()
	s := boundedtest.NewMixedStub(tb)
	return stub.Subject[boundedtest.MixedAddCall, boundedtest.MixedAddReturn]{
		Stub: s.OnAdd.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string = "test-item"
			_ = s.Add(a0, a1)
		},
		Result: func() boundedtest.MixedAddReturn {
			var a0 context.Context
			var a1 string = "test-item"
			got0 := s.Add(a0, a1)
			return boundedtest.MixedAddReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnAdd.Func(func(_ context.Context, _ string) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 string = "test-item"
			r0 := s.Add(a0, a1)
			return r0
		},
	}
}

// TestMixedStubAdd pins how Add answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubAdd(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Add", mixedStubAddSubject)

	// No check that Add answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := boundedtest.NewMixedStub(t)
		var a0 context.Context
		var a1 string = "test-item"
		_ = s.Add(a0, a1)
		got := s.OnAdd.AssertCalledOnce(t, "Add must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Item, a1, "the recorded call carries Item")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := boundedtest.NewMixedStub(t)
		var seen []boundedtest.MixedAddCall
		s.OnAdd.OnRecord(func(c boundedtest.MixedAddCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string = "test-item"
		_ = s.Add(a0, a1)
		_ = s.Add(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Add call")
	})

	t.Run("wires WithMixedAdd at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := boundedtest.NewMixedStub(t, boundedtest.WithMixedAdd(func(_ context.Context, _ string) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		var a1 string = "test-item"
		_ = s.Add(a0, a1)
		testkit.True(t, called, "WithMixedAdd must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Add admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := boundedtest.NewMixedStub(t)
		var want0 error
		s.OnAdd.Returns(want0)
		var a0 context.Context
		var a1 string = "test-item"
		_ = s.Add(a0, a1)
		s.ResetCalls()
		_ = s.Add(a0, a1)
		s.OnAdd.AssertCalledOnce(t, "Add must record the call that followed the reset")
	})
}

// mixedStubListSubject binds List into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubListSubject(tb testing.TB) stub.Subject[boundedtest.MixedListCall, boundedtest.MixedListReturn] {
	tb.Helper()
	s := boundedtest.NewMixedStub(tb)
	return stub.Subject[boundedtest.MixedListCall, boundedtest.MixedListReturn]{
		Stub: s.OnList.MethodStub,
		Call: func() {
			var a0 context.Context
			_, _ = s.List(a0)
		},
		Result: func() boundedtest.MixedListReturn {
			var a0 context.Context
			got0, got1 := s.List(a0)
			return boundedtest.MixedListReturn{Result: got0, Err: got1}
		},
		Override: func(mark func()) {
			s.OnList.Func(func(_ context.Context) ([]string, error) {
				mark()
				var z0 []string
				var z1 error
				return z0, z1
			})
		},
		Fails: func() error {
			var a0 context.Context
			_, r1 := s.List(a0)
			return r1
		},
	}
}

// TestMixedStubList pins how List answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubList(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "List", mixedStubListSubject)

	// No check that List answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := boundedtest.NewMixedStub(t)
		var a0 context.Context
		_, _ = s.List(a0)
		got := s.OnList.AssertCalledOnce(t, "List must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := boundedtest.NewMixedStub(t)
		var seen []boundedtest.MixedListCall
		s.OnList.OnRecord(func(c boundedtest.MixedListCall) { seen = append(seen, c) })
		var a0 context.Context
		_, _ = s.List(a0)
		_, _ = s.List(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per List call")
	})

	t.Run("wires WithMixedList at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := boundedtest.NewMixedStub(t, boundedtest.WithMixedList(func(_ context.Context) ([]string, error) {
			called = true
			var z0 []string
			var z1 error
			return z0, z1
		}))
		var a0 context.Context
		_, _ = s.List(a0)
		testkit.True(t, called, "WithMixedList must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of List admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := boundedtest.NewMixedStub(t)
		var want0 []string
		var want1 error
		s.OnList.Returns(want0, want1)
		var a0 context.Context
		_, _ = s.List(a0)
		s.ResetCalls()
		_, _ = s.List(a0)
		s.OnList.AssertCalledOnce(t, "List must record the call that followed the reset")
	})
}

// mixedStubDouble describes how to build a MixedStub under each
// option whose effect is the same whatever a method's signature.
//
// Add stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func mixedStubDouble() stub.Double[boundedtest.MixedAddCall] {
	instance := func(s *boundedtest.MixedStub) stub.Instance[boundedtest.MixedAddCall] {
		return stub.Instance[boundedtest.MixedAddCall]{
			Stub: s.OnAdd.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 string = "test-item"
				_ = s.Add(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[boundedtest.MixedAddCall]{
		New: func(tb testing.TB) stub.Instance[boundedtest.MixedAddCall] {
			return instance(boundedtest.NewMixedStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[boundedtest.MixedAddCall] {
			return instance(boundedtest.NewMixedStub(tb, boundedtest.MixedStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[boundedtest.MixedAddCall] {
			return instance(boundedtest.NewMixedStub(tb, boundedtest.MixedStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[boundedtest.MixedAddCall] {
			return instance(boundedtest.NewMixedStub(tb, boundedtest.MixedStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[boundedtest.MixedAddCall] {
			return instance(boundedtest.NewMixedStub(tb, boundedtest.MixedStubStrict()))
		},
	}
}

// TestMixedStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestMixedStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, mixedStubDouble())
}

// TestMixedStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestMixedStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := boundedtest.NewMixedStub(t)
	s := boundedtest.NewMixedStub(t, boundedtest.MixedStubDelegateTo(inner))

	t.Run("forwards Add to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string = "test-item"
		_ = s.Add(a0, a1)
		inner.OnAdd.AssertCalledOnce(t, "Add must reach the wrapped implementation")
	})

	t.Run("surfaces what Add answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Add-delegate")
		inner.OnAdd.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 string = "test-item"
		r0 := s.Add(a0, a1)
		testkit.ErrorIs(t, r0, want, "Add must surface the wrapped answer")
	})

	t.Run("forwards List to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_, _ = s.List(a0)
		inner.OnList.AssertCalledOnce(t, "List must reach the wrapped implementation")
	})

	t.Run("surfaces what List answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("List-delegate")
		inner.OnList.FaultsFor(time.Hour, want)
		var a0 context.Context
		_, r1 := s.List(a0)
		testkit.ErrorIs(t, r1, want, "List must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 0a5323e7f065f614cfebfa3c87c1fa4f3ccf04ee746694c696729cc98fc48f88
