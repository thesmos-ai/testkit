// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/lifecycleafterclose/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package lifecycleafterclosetest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/lifecycleafterclose/lifecycleafterclosetest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// mixedStubCloseSubject binds Close into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubCloseSubject(tb testing.TB) stub.Subject[lifecycleafterclosetest.MixedCloseCall, lifecycleafterclosetest.MixedCloseReturn] {
	tb.Helper()
	s := lifecycleafterclosetest.NewMixedStub(tb)
	return stub.Subject[lifecycleafterclosetest.MixedCloseCall, lifecycleafterclosetest.MixedCloseReturn]{
		Stub: s.OnClose.MethodStub,
		Call: func() {
			var a0 context.Context
			_ = s.Close(a0)
		},
		Result: func() lifecycleafterclosetest.MixedCloseReturn {
			var a0 context.Context
			got0 := s.Close(a0)
			return lifecycleafterclosetest.MixedCloseReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnClose.Func(func(_ context.Context) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			r0 := s.Close(a0)
			return r0
		},
	}
}

// TestMixedStubClose pins how Close answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubClose(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Close", mixedStubCloseSubject)

	// No check that Close answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := lifecycleafterclosetest.NewMixedStub(t)
		var a0 context.Context
		_ = s.Close(a0)
		got := s.OnClose.AssertCalledOnce(t, "Close must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := lifecycleafterclosetest.NewMixedStub(t)
		var seen []lifecycleafterclosetest.MixedCloseCall
		s.OnClose.OnRecord(func(c lifecycleafterclosetest.MixedCloseCall) { seen = append(seen, c) })
		var a0 context.Context
		_ = s.Close(a0)
		_ = s.Close(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Close call")
	})

	t.Run("wires WithMixedClose at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := lifecycleafterclosetest.NewMixedStub(t, lifecycleafterclosetest.WithMixedClose(func(_ context.Context) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		_ = s.Close(a0)
		testkit.True(t, called, "WithMixedClose must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Close admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := lifecycleafterclosetest.NewMixedStub(t)
		var want0 error
		s.OnClose.Returns(want0)
		var a0 context.Context
		_ = s.Close(a0)
		s.ResetCalls()
		_ = s.Close(a0)
		s.OnClose.AssertCalledOnce(t, "Close must record the call that followed the reset")
	})
}

// mixedStubWorkSubject binds Work into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubWorkSubject(tb testing.TB) stub.Subject[lifecycleafterclosetest.MixedWorkCall, lifecycleafterclosetest.MixedWorkReturn] {
	tb.Helper()
	s := lifecycleafterclosetest.NewMixedStub(tb)
	return stub.Subject[lifecycleafterclosetest.MixedWorkCall, lifecycleafterclosetest.MixedWorkReturn]{
		Stub: s.OnWork.MethodStub,
		Call: func() {
			var a0 context.Context
			_ = s.Work(a0)
		},
		Result: func() lifecycleafterclosetest.MixedWorkReturn {
			var a0 context.Context
			got0 := s.Work(a0)
			return lifecycleafterclosetest.MixedWorkReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnWork.Func(func(_ context.Context) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			r0 := s.Work(a0)
			return r0
		},
	}
}

// TestMixedStubWork pins how Work answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubWork(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Work", mixedStubWorkSubject)

	// No check that Work answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := lifecycleafterclosetest.NewMixedStub(t)
		var a0 context.Context
		_ = s.Work(a0)
		got := s.OnWork.AssertCalledOnce(t, "Work must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := lifecycleafterclosetest.NewMixedStub(t)
		var seen []lifecycleafterclosetest.MixedWorkCall
		s.OnWork.OnRecord(func(c lifecycleafterclosetest.MixedWorkCall) { seen = append(seen, c) })
		var a0 context.Context
		_ = s.Work(a0)
		_ = s.Work(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Work call")
	})

	t.Run("wires WithMixedWork at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := lifecycleafterclosetest.NewMixedStub(t, lifecycleafterclosetest.WithMixedWork(func(_ context.Context) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		_ = s.Work(a0)
		testkit.True(t, called, "WithMixedWork must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Work admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := lifecycleafterclosetest.NewMixedStub(t)
		var want0 error
		s.OnWork.Returns(want0)
		var a0 context.Context
		_ = s.Work(a0)
		s.ResetCalls()
		_ = s.Work(a0)
		s.OnWork.AssertCalledOnce(t, "Work must record the call that followed the reset")
	})
}

// mixedStubDouble describes how to build a MixedStub under each
// option whose effect is the same whatever a method's signature.
//
// Close stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func mixedStubDouble() stub.Double[lifecycleafterclosetest.MixedCloseCall] {
	instance := func(s *lifecycleafterclosetest.MixedStub) stub.Instance[lifecycleafterclosetest.MixedCloseCall] {
		return stub.Instance[lifecycleafterclosetest.MixedCloseCall]{
			Stub: s.OnClose.MethodStub,
			Call: func() {
				var a0 context.Context
				_ = s.Close(a0)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[lifecycleafterclosetest.MixedCloseCall]{
		New: func(tb testing.TB) stub.Instance[lifecycleafterclosetest.MixedCloseCall] {
			return instance(lifecycleafterclosetest.NewMixedStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[lifecycleafterclosetest.MixedCloseCall] {
			return instance(lifecycleafterclosetest.NewMixedStub(tb, lifecycleafterclosetest.MixedStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[lifecycleafterclosetest.MixedCloseCall] {
			return instance(lifecycleafterclosetest.NewMixedStub(tb, lifecycleafterclosetest.MixedStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[lifecycleafterclosetest.MixedCloseCall] {
			return instance(lifecycleafterclosetest.NewMixedStub(tb, lifecycleafterclosetest.MixedStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[lifecycleafterclosetest.MixedCloseCall] {
			return instance(lifecycleafterclosetest.NewMixedStub(tb, lifecycleafterclosetest.MixedStubStrict()))
		},
	}
}

// TestMixedStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestMixedStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, mixedStubDouble())
}

// TestMixedStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestMixedStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := lifecycleafterclosetest.NewMixedStub(t)
	s := lifecycleafterclosetest.NewMixedStub(t, lifecycleafterclosetest.MixedStubDelegateTo(inner))

	t.Run("forwards Close to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_ = s.Close(a0)
		inner.OnClose.AssertCalledOnce(t, "Close must reach the wrapped implementation")
	})

	t.Run("surfaces what Close answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Close-delegate")
		inner.OnClose.FaultsFor(time.Hour, want)
		var a0 context.Context
		r0 := s.Close(a0)
		testkit.ErrorIs(t, r0, want, "Close must surface the wrapped answer")
	})

	t.Run("forwards Work to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_ = s.Work(a0)
		inner.OnWork.AssertCalledOnce(t, "Work must reach the wrapped implementation")
	})

	t.Run("surfaces what Work answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Work-delegate")
		inner.OnWork.FaultsFor(time.Hour, want)
		var a0 context.Context
		r0 := s.Work(a0)
		testkit.ErrorIs(t, r0, want, "Work must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 46136891ae1fb51a31a8c82f94b71d0ddd857215bb960c801b9ab29bb31c914a
