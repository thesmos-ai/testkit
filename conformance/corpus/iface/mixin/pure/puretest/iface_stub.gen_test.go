// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/pure/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package puretest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/pure/puretest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// mixedStubDeriveSubject binds Derive into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubDeriveSubject(tb testing.TB) stub.Subject[puretest.MixedDeriveCall, puretest.MixedDeriveReturn] {
	tb.Helper()
	s := puretest.NewMixedStub(tb)
	return stub.Subject[puretest.MixedDeriveCall, puretest.MixedDeriveReturn]{
		Stub: s.OnDerive.MethodStub,
		Call: func() {
			var a0 string = "test-input"
			_ = s.Derive(a0)
		},
		Result: func() puretest.MixedDeriveReturn {
			var a0 string = "test-input"
			got0 := s.Derive(a0)
			return puretest.MixedDeriveReturn{Result: got0}
		},
		Override: func(mark func()) {
			s.OnDerive.Func(func(_ string) string {
				mark()
				var z0 string
				return z0
			})
		},
	}
}

// TestMixedStubDerive pins how Derive answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubDerive(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Derive", mixedStubDeriveSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := puretest.NewMixedStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 string = "test-derive"
		s.OnDerive.Returns(want0)
		var a0 string = "test-input"
		got0 := s.Derive(a0)
		testkit.Equal(t, got0, want0, "Derive must answer with what Returns pinned")
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := puretest.NewMixedStub(t)
		var a0 string = "test-input"
		_ = s.Derive(a0)
		got := s.OnDerive.AssertCalledOnce(t, "Derive must record the call")
		testkit.Equal(t, got.Input, a0, "the recorded call carries Input")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := puretest.NewMixedStub(t)
		var seen []puretest.MixedDeriveCall
		s.OnDerive.OnRecord(func(c puretest.MixedDeriveCall) { seen = append(seen, c) })
		var a0 string = "test-input"
		_ = s.Derive(a0)
		_ = s.Derive(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Derive call")
	})

	t.Run("wires WithMixedDerive at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := puretest.NewMixedStub(t, puretest.WithMixedDerive(func(_ string) string {
			called = true
			var z0 string
			return z0
		}))
		var a0 string = "test-input"
		_ = s.Derive(a0)
		testkit.True(t, called, "WithMixedDerive must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		//
		// Pinned to a literal for the reason the Returns check is: a reset
		// that lost the configuration answers the zero, and so does one that
		// kept a configured zero.
		s := puretest.NewMixedStub(t)
		var want0 string = "test-derive"
		s.OnDerive.Returns(want0)
		var a0 string = "test-input"
		_ = s.Derive(a0)
		s.ResetCalls()
		got0 := s.Derive(a0)
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
	})
}

// mixedStubDouble describes how to build a MixedStub under each
// option whose effect is the same whatever a method's signature.
//
// Derive stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func mixedStubDouble() stub.Double[puretest.MixedDeriveCall] {
	instance := func(s *puretest.MixedStub) stub.Instance[puretest.MixedDeriveCall] {
		return stub.Instance[puretest.MixedDeriveCall]{
			Stub: s.OnDerive.MethodStub,
			Call: func() {
				var a0 string = "test-input"
				_ = s.Derive(a0)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[puretest.MixedDeriveCall]{
		New: func(tb testing.TB) stub.Instance[puretest.MixedDeriveCall] {
			return instance(puretest.NewMixedStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[puretest.MixedDeriveCall] {
			return instance(puretest.NewMixedStub(tb, puretest.MixedStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[puretest.MixedDeriveCall] {
			return instance(puretest.NewMixedStub(tb, puretest.MixedStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[puretest.MixedDeriveCall] {
			return instance(puretest.NewMixedStub(tb, puretest.MixedStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[puretest.MixedDeriveCall] {
			return instance(puretest.NewMixedStub(tb, puretest.MixedStubStrict()))
		},
	}
}

// TestMixedStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestMixedStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, mixedStubDouble())
}

// TestMixedStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestMixedStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := puretest.NewMixedStub(t)
	s := puretest.NewMixedStub(t, puretest.MixedStubDelegateTo(inner))

	t.Run("forwards Derive to the wrapped implementation", func(t *testing.T) {
		var a0 string = "test-input"
		_ = s.Derive(a0)
		inner.OnDerive.AssertCalledOnce(t, "Derive must reach the wrapped implementation")
	})
}

// testkit: end of generated content.
// testkit:provenance ca77d9b5ed1bd083693c5e82b331d097da180d57dfdb4a3812838a36a51c105c
