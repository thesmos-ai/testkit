// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/tamperevident/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package tamperevidenttest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/tamperevident/tamperevidenttest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// mixedStubStoreSubject binds Store into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubStoreSubject(tb testing.TB) stub.Subject[tamperevidenttest.MixedStoreCall, tamperevidenttest.MixedStoreReturn] {
	tb.Helper()
	s := tamperevidenttest.NewMixedStub(tb)
	return stub.Subject[tamperevidenttest.MixedStoreCall, tamperevidenttest.MixedStoreReturn]{
		Stub: s.OnStore.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string = "test-body"
			_ = s.Store(a0, a1)
		},
		Result: func() tamperevidenttest.MixedStoreReturn {
			var a0 context.Context
			var a1 string = "test-body"
			got0 := s.Store(a0, a1)
			return tamperevidenttest.MixedStoreReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnStore.Func(func(_ context.Context, _ string) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 string = "test-body"
			r0 := s.Store(a0, a1)
			return r0
		},
	}
}

// TestMixedStubStore pins how Store answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubStore(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Store", mixedStubStoreSubject)

	// No check that Store answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := tamperevidenttest.NewMixedStub(t)
		var a0 context.Context
		var a1 string = "test-body"
		_ = s.Store(a0, a1)
		got := s.OnStore.AssertCalledOnce(t, "Store must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Body, a1, "the recorded call carries Body")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := tamperevidenttest.NewMixedStub(t)
		var seen []tamperevidenttest.MixedStoreCall
		s.OnStore.OnRecord(func(c tamperevidenttest.MixedStoreCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string = "test-body"
		_ = s.Store(a0, a1)
		_ = s.Store(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Store call")
	})

	t.Run("wires WithMixedStore at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := tamperevidenttest.NewMixedStub(t, tamperevidenttest.WithMixedStore(func(_ context.Context, _ string) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		var a1 string = "test-body"
		_ = s.Store(a0, a1)
		testkit.True(t, called, "WithMixedStore must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Store admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := tamperevidenttest.NewMixedStub(t)
		var want0 error
		s.OnStore.Returns(want0)
		var a0 context.Context
		var a1 string = "test-body"
		_ = s.Store(a0, a1)
		s.ResetCalls()
		_ = s.Store(a0, a1)
		s.OnStore.AssertCalledOnce(t, "Store must record the call that followed the reset")
	})
}

// mixedStubCorruptSubject binds Corrupt into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubCorruptSubject(tb testing.TB) stub.Subject[tamperevidenttest.MixedCorruptCall, tamperevidenttest.MixedCorruptReturn] {
	tb.Helper()
	s := tamperevidenttest.NewMixedStub(tb)
	return stub.Subject[tamperevidenttest.MixedCorruptCall, tamperevidenttest.MixedCorruptReturn]{
		Stub: s.OnCorrupt.MethodStub,
		Call: func() {
			var a0 context.Context
			_ = s.Corrupt(a0)
		},
		Result: func() tamperevidenttest.MixedCorruptReturn {
			var a0 context.Context
			got0 := s.Corrupt(a0)
			return tamperevidenttest.MixedCorruptReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnCorrupt.Func(func(_ context.Context) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			r0 := s.Corrupt(a0)
			return r0
		},
	}
}

// TestMixedStubCorrupt pins how Corrupt answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubCorrupt(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Corrupt", mixedStubCorruptSubject)

	// No check that Corrupt answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := tamperevidenttest.NewMixedStub(t)
		var a0 context.Context
		_ = s.Corrupt(a0)
		got := s.OnCorrupt.AssertCalledOnce(t, "Corrupt must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := tamperevidenttest.NewMixedStub(t)
		var seen []tamperevidenttest.MixedCorruptCall
		s.OnCorrupt.OnRecord(func(c tamperevidenttest.MixedCorruptCall) { seen = append(seen, c) })
		var a0 context.Context
		_ = s.Corrupt(a0)
		_ = s.Corrupt(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Corrupt call")
	})

	t.Run("wires WithMixedCorrupt at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := tamperevidenttest.NewMixedStub(t, tamperevidenttest.WithMixedCorrupt(func(_ context.Context) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		_ = s.Corrupt(a0)
		testkit.True(t, called, "WithMixedCorrupt must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Corrupt admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := tamperevidenttest.NewMixedStub(t)
		var want0 error
		s.OnCorrupt.Returns(want0)
		var a0 context.Context
		_ = s.Corrupt(a0)
		s.ResetCalls()
		_ = s.Corrupt(a0)
		s.OnCorrupt.AssertCalledOnce(t, "Corrupt must record the call that followed the reset")
	})
}

// mixedStubVerifySubject binds Verify into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubVerifySubject(tb testing.TB) stub.Subject[tamperevidenttest.MixedVerifyCall, tamperevidenttest.MixedVerifyReturn] {
	tb.Helper()
	s := tamperevidenttest.NewMixedStub(tb)
	return stub.Subject[tamperevidenttest.MixedVerifyCall, tamperevidenttest.MixedVerifyReturn]{
		Stub: s.OnVerify.MethodStub,
		Call: func() {
			var a0 context.Context
			_ = s.Verify(a0)
		},
		Result: func() tamperevidenttest.MixedVerifyReturn {
			var a0 context.Context
			got0 := s.Verify(a0)
			return tamperevidenttest.MixedVerifyReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnVerify.Func(func(_ context.Context) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			r0 := s.Verify(a0)
			return r0
		},
	}
}

// TestMixedStubVerify pins how Verify answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubVerify(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Verify", mixedStubVerifySubject)

	// No check that Verify answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := tamperevidenttest.NewMixedStub(t)
		var a0 context.Context
		_ = s.Verify(a0)
		got := s.OnVerify.AssertCalledOnce(t, "Verify must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := tamperevidenttest.NewMixedStub(t)
		var seen []tamperevidenttest.MixedVerifyCall
		s.OnVerify.OnRecord(func(c tamperevidenttest.MixedVerifyCall) { seen = append(seen, c) })
		var a0 context.Context
		_ = s.Verify(a0)
		_ = s.Verify(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Verify call")
	})

	t.Run("wires WithMixedVerify at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := tamperevidenttest.NewMixedStub(t, tamperevidenttest.WithMixedVerify(func(_ context.Context) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		_ = s.Verify(a0)
		testkit.True(t, called, "WithMixedVerify must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Verify admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := tamperevidenttest.NewMixedStub(t)
		var want0 error
		s.OnVerify.Returns(want0)
		var a0 context.Context
		_ = s.Verify(a0)
		s.ResetCalls()
		_ = s.Verify(a0)
		s.OnVerify.AssertCalledOnce(t, "Verify must record the call that followed the reset")
	})
}

// mixedStubDouble describes how to build a MixedStub under each
// option whose effect is the same whatever a method's signature.
//
// Store stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func mixedStubDouble() stub.Double[tamperevidenttest.MixedStoreCall] {
	instance := func(s *tamperevidenttest.MixedStub) stub.Instance[tamperevidenttest.MixedStoreCall] {
		return stub.Instance[tamperevidenttest.MixedStoreCall]{
			Stub: s.OnStore.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 string = "test-body"
				_ = s.Store(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[tamperevidenttest.MixedStoreCall]{
		New: func(tb testing.TB) stub.Instance[tamperevidenttest.MixedStoreCall] {
			return instance(tamperevidenttest.NewMixedStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[tamperevidenttest.MixedStoreCall] {
			return instance(tamperevidenttest.NewMixedStub(tb, tamperevidenttest.MixedStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[tamperevidenttest.MixedStoreCall] {
			return instance(tamperevidenttest.NewMixedStub(tb, tamperevidenttest.MixedStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[tamperevidenttest.MixedStoreCall] {
			return instance(tamperevidenttest.NewMixedStub(tb, tamperevidenttest.MixedStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[tamperevidenttest.MixedStoreCall] {
			return instance(tamperevidenttest.NewMixedStub(tb, tamperevidenttest.MixedStubStrict()))
		},
	}
}

// TestMixedStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestMixedStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, mixedStubDouble())
}

// TestMixedStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestMixedStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := tamperevidenttest.NewMixedStub(t)
	s := tamperevidenttest.NewMixedStub(t, tamperevidenttest.MixedStubDelegateTo(inner))

	t.Run("forwards Store to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string = "test-body"
		_ = s.Store(a0, a1)
		inner.OnStore.AssertCalledOnce(t, "Store must reach the wrapped implementation")
	})

	t.Run("surfaces what Store answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Store-delegate")
		inner.OnStore.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 string = "test-body"
		r0 := s.Store(a0, a1)
		testkit.ErrorIs(t, r0, want, "Store must surface the wrapped answer")
	})

	t.Run("forwards Corrupt to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_ = s.Corrupt(a0)
		inner.OnCorrupt.AssertCalledOnce(t, "Corrupt must reach the wrapped implementation")
	})

	t.Run("surfaces what Corrupt answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Corrupt-delegate")
		inner.OnCorrupt.FaultsFor(time.Hour, want)
		var a0 context.Context
		r0 := s.Corrupt(a0)
		testkit.ErrorIs(t, r0, want, "Corrupt must surface the wrapped answer")
	})

	t.Run("forwards Verify to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_ = s.Verify(a0)
		inner.OnVerify.AssertCalledOnce(t, "Verify must reach the wrapped implementation")
	})

	t.Run("surfaces what Verify answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Verify-delegate")
		inner.OnVerify.FaultsFor(time.Hour, want)
		var a0 context.Context
		r0 := s.Verify(a0)
		testkit.ErrorIs(t, r0, want, "Verify must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 31c32b1b1e579ee9e930c2d9fb43fabc0d26a632665f0d2c6d201bf99857f15d
