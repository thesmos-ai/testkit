// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/timeaware/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package timeawaretest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/timeaware/timeawaretest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// mixedStubTouchSubject binds Touch into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubTouchSubject(tb testing.TB) stub.Subject[timeawaretest.MixedTouchCall, timeawaretest.MixedTouchReturn] {
	tb.Helper()
	s := timeawaretest.NewMixedStub(tb)
	return stub.Subject[timeawaretest.MixedTouchCall, timeawaretest.MixedTouchReturn]{
		Stub: s.OnTouch.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string = "test-key"
			_ = s.Touch(a0, a1)
		},
		Result: func() timeawaretest.MixedTouchReturn {
			var a0 context.Context
			var a1 string = "test-key"
			got0 := s.Touch(a0, a1)
			return timeawaretest.MixedTouchReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnTouch.Func(func(_ context.Context, _ string) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 string = "test-key"
			r0 := s.Touch(a0, a1)
			return r0
		},
	}
}

// TestMixedStubTouch pins how Touch answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubTouch(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Touch", mixedStubTouchSubject)

	// No check that Touch answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := timeawaretest.NewMixedStub(t)
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Touch(a0, a1)
		got := s.OnTouch.AssertCalledOnce(t, "Touch must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Key, a1, "the recorded call carries Key")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := timeawaretest.NewMixedStub(t)
		var seen []timeawaretest.MixedTouchCall
		s.OnTouch.OnRecord(func(c timeawaretest.MixedTouchCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Touch(a0, a1)
		_ = s.Touch(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Touch call")
	})

	t.Run("wires WithMixedTouch at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := timeawaretest.NewMixedStub(t, timeawaretest.WithMixedTouch(func(_ context.Context, _ string) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Touch(a0, a1)
		testkit.True(t, called, "WithMixedTouch must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Touch admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := timeawaretest.NewMixedStub(t)
		var want0 error
		s.OnTouch.Returns(want0)
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Touch(a0, a1)
		s.ResetCalls()
		_ = s.Touch(a0, a1)
		s.OnTouch.AssertCalledOnce(t, "Touch must record the call that followed the reset")
	})
}

// mixedStubAgeOfSubject binds AgeOf into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubAgeOfSubject(tb testing.TB) stub.Subject[timeawaretest.MixedAgeOfCall, timeawaretest.MixedAgeOfReturn] {
	tb.Helper()
	s := timeawaretest.NewMixedStub(tb)
	return stub.Subject[timeawaretest.MixedAgeOfCall, timeawaretest.MixedAgeOfReturn]{
		Stub: s.OnAgeOf.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string = "test-key"
			_, _ = s.AgeOf(a0, a1)
		},
		Result: func() timeawaretest.MixedAgeOfReturn {
			var a0 context.Context
			var a1 string = "test-key"
			got0, got1 := s.AgeOf(a0, a1)
			return timeawaretest.MixedAgeOfReturn{Result: got0, Err: got1}
		},
		Override: func(mark func()) {
			s.OnAgeOf.Func(func(_ context.Context, _ string) (int64, error) {
				mark()
				var z0 int64
				var z1 error
				return z0, z1
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 string = "test-key"
			_, r1 := s.AgeOf(a0, a1)
			return r1
		},
	}
}

// TestMixedStubAgeOf pins how AgeOf answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubAgeOf(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "AgeOf", mixedStubAgeOfSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := timeawaretest.NewMixedStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 int64 = 42
		var want1 error
		s.OnAgeOf.Returns(want0, want1)
		var a0 context.Context
		var a1 string = "test-key"
		got0, _ := s.AgeOf(a0, a1)
		testkit.Equal(t, got0, want0, "AgeOf must answer with what Returns pinned")
		// Blanked above rather than dropped in silence. No literal exists
		// for result 1 (error), so the only value to pin there
		// is the zero — and a double that ignored Returns answers the zero
		// too, which makes the comparison pass either way.
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := timeawaretest.NewMixedStub(t)
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.AgeOf(a0, a1)
		got := s.OnAgeOf.AssertCalledOnce(t, "AgeOf must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Key, a1, "the recorded call carries Key")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := timeawaretest.NewMixedStub(t)
		var seen []timeawaretest.MixedAgeOfCall
		s.OnAgeOf.OnRecord(func(c timeawaretest.MixedAgeOfCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.AgeOf(a0, a1)
		_, _ = s.AgeOf(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per AgeOf call")
	})

	t.Run("wires WithMixedAgeOf at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := timeawaretest.NewMixedStub(t, timeawaretest.WithMixedAgeOf(func(_ context.Context, _ string) (int64, error) {
			called = true
			var z0 int64
			var z1 error
			return z0, z1
		}))
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.AgeOf(a0, a1)
		testkit.True(t, called, "WithMixedAgeOf must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		//
		// Pinned to a literal for the reason the Returns check is: a reset
		// that lost the configuration answers the zero, and so does one that
		// kept a configured zero.
		s := timeawaretest.NewMixedStub(t)
		var want0 int64 = 42
		var want1 error
		s.OnAgeOf.Returns(want0, want1)
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.AgeOf(a0, a1)
		s.ResetCalls()
		got0, _ := s.AgeOf(a0, a1)
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
	})
}

// mixedStubDouble describes how to build a MixedStub under each
// option whose effect is the same whatever a method's signature.
//
// Touch stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func mixedStubDouble() stub.Double[timeawaretest.MixedTouchCall] {
	instance := func(s *timeawaretest.MixedStub) stub.Instance[timeawaretest.MixedTouchCall] {
		return stub.Instance[timeawaretest.MixedTouchCall]{
			Stub: s.OnTouch.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 string = "test-key"
				_ = s.Touch(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[timeawaretest.MixedTouchCall]{
		New: func(tb testing.TB) stub.Instance[timeawaretest.MixedTouchCall] {
			return instance(timeawaretest.NewMixedStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[timeawaretest.MixedTouchCall] {
			return instance(timeawaretest.NewMixedStub(tb, timeawaretest.MixedStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[timeawaretest.MixedTouchCall] {
			return instance(timeawaretest.NewMixedStub(tb, timeawaretest.MixedStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[timeawaretest.MixedTouchCall] {
			return instance(timeawaretest.NewMixedStub(tb, timeawaretest.MixedStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[timeawaretest.MixedTouchCall] {
			return instance(timeawaretest.NewMixedStub(tb, timeawaretest.MixedStubStrict()))
		},
	}
}

// TestMixedStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestMixedStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, mixedStubDouble())
}

// TestMixedStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestMixedStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := timeawaretest.NewMixedStub(t)
	s := timeawaretest.NewMixedStub(t, timeawaretest.MixedStubDelegateTo(inner))

	t.Run("forwards Touch to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Touch(a0, a1)
		inner.OnTouch.AssertCalledOnce(t, "Touch must reach the wrapped implementation")
	})

	t.Run("surfaces what Touch answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Touch-delegate")
		inner.OnTouch.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 string = "test-key"
		r0 := s.Touch(a0, a1)
		testkit.ErrorIs(t, r0, want, "Touch must surface the wrapped answer")
	})

	t.Run("forwards AgeOf to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.AgeOf(a0, a1)
		inner.OnAgeOf.AssertCalledOnce(t, "AgeOf must reach the wrapped implementation")
	})

	t.Run("surfaces what AgeOf answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("AgeOf-delegate")
		inner.OnAgeOf.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 string = "test-key"
		_, r1 := s.AgeOf(a0, a1)
		testkit.ErrorIs(t, r1, want, "AgeOf must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 30e808949575461b55a6df121ef6312781e3e08a8c04d423a12ec23652cf03fa
