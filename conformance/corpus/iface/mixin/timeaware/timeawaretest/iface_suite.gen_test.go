// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/timeaware/iface.go
// Plugins:   golang 1.0.0, suite 1.21.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package timeawaretest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/timeaware/timeawaretest"
	"go.thesmos.sh/testkit/engine/suite"
	"go.thesmos.sh/testkit/engine/suite/prove"
)

// TestMixedProofs drives every planted defect through the check it is
// evidence for.
//
// A red here is the expected outcome for each one; a GREEN is the finding.
// It means a check tolerated the implementation built to break it, and a
// check that cannot fail is a line in a report rather than a claim about
// the subject.
func TestMixedProofs(t *testing.T) {
	t.Parallel()
	prove.All(t,
		timeawaretest.MixedSuite.Suite(timeawaretest.DefaultMixedFixture()).Checks,
		mixedProofs())
}

// mixedProofs is every defect this run derived and can spell.
//
// Each is the smallest implementation that breaks exactly one claim: the
// generated double with one method overridden, and nothing else changed.
// The reason beside it is the substring the red must contain, so a defect
// that died on an unrelated guard stops counting as evidence.
func mixedProofs() prove.Defects[timeawaretest.Mixed] {
	ix := timeawaretest.MixedSuite.Checks
	return prove.Defects[timeawaretest.Mixed]{
		ix.Touch.Smoke(): prove.One("a Mixed whose Touch panics",
			func(tb testing.TB) timeawaretest.Mixed {
				return timeawaretest.NewMixedStub(tb, timeawaretest.WithMixedTouch(
					func(_ context.Context, _ string) error {
						panic("planted: Touch panics")
					}))
			}).Reasoned(suite.RedPanicked),
		ix.Touch.Cancels(): prove.One("a Mixed whose Touch ignores the context it is handed",
			func(tb testing.TB) timeawaretest.Mixed {
				return timeawaretest.NewMixedStub(tb, timeawaretest.WithMixedTouch(
					func(_ context.Context, _ string) (err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedCancelled),
		ix.Touch.NilContext(): prove.One("a Mixed whose Touch forgives a nil context and answers",
			func(tb testing.TB) timeawaretest.Mixed {
				return timeawaretest.NewMixedStub(tb, timeawaretest.WithMixedTouch(
					func(_ context.Context, _ string) (err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedNilContext),
		ix.Touch.Deadline(): prove.One("a Mixed whose Touch ignores the context it is handed",
			func(tb testing.TB) timeawaretest.Mixed {
				return timeawaretest.NewMixedStub(tb, timeawaretest.WithMixedTouch(
					func(_ context.Context, _ string) (err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedDeadline),
		ix.AgeOf.Smoke(): prove.One("a Mixed whose AgeOf panics",
			func(tb testing.TB) timeawaretest.Mixed {
				return timeawaretest.NewMixedStub(tb, timeawaretest.WithMixedAgeOf(
					func(_ context.Context, _ string) (int64, error) {
						panic("planted: AgeOf panics")
					}))
			}).Reasoned(suite.RedPanicked),
		ix.AgeOf.Cancels(): prove.One("a Mixed whose AgeOf ignores the context it is handed",
			func(tb testing.TB) timeawaretest.Mixed {
				return timeawaretest.NewMixedStub(tb, timeawaretest.WithMixedAgeOf(
					func(_ context.Context, _ string) (r0 int64, err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedCancelled),
		ix.AgeOf.NilContext(): prove.One("a Mixed whose AgeOf forgives a nil context and answers",
			func(tb testing.TB) timeawaretest.Mixed {
				return timeawaretest.NewMixedStub(tb, timeawaretest.WithMixedAgeOf(
					func(_ context.Context, _ string) (r0 int64, err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedNilContext),
		ix.AgeOf.Deadline(): prove.One("a Mixed whose AgeOf ignores the context it is handed",
			func(tb testing.TB) timeawaretest.Mixed {
				return timeawaretest.NewMixedStub(tb, timeawaretest.WithMixedAgeOf(
					func(_ context.Context, _ string) (r0 int64, err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedDeadline),
		ix.AgeOf.ZeroOnError(): prove.One("a Mixed whose AgeOf answers a believable value beside its error",
			func(tb testing.TB) timeawaretest.Mixed {
				return timeawaretest.NewMixedStub(tb, timeawaretest.WithMixedAgeOf(
					func(_ context.Context, _ string) (r0 int64, err error) {
						// A believable answer beside the refusal. A caller
						// reading the error and one reading the value disagree
						// about what happened, which is the claim's own
						// violation rather than a subject that merely failed.
						r0 = 2
						err = errors.New("planted: AgeOf refused with a believable value")
						return
					}))
			}),
		ix.AgeOf.Miss(): prove.One("a Mixed whose AgeOf answers for an input nothing wrote",
			func(tb testing.TB) timeawaretest.Mixed {
				return timeawaretest.NewMixedStub(tb, timeawaretest.WithMixedAgeOf(
					func(_ context.Context, _ string) (r0 int64, err error) {
						// A value for a call a correct subject answers nothing for.
						r0 = 2
						return
					}))
			}),
	}
}

// TestMixedInvariants holds this package to what it says about itself.
//
// Four claims, none of which a consumer writes and none of which any
// other gate makes. The manifest pins the assertion SET, so a check that
// stops being emitted reads as a removed row in review rather than as a
// suite that quietly got smaller. The index and the drop hints pin the
// two ways a check is NAMED — an index entry pointing at nothing
// compiles, and a drop hint that falls back to the string form forfeits
// exactly the compile-break the index exists to give. The distinct-ID
// check catches two interfaces in one package claiming one identity,
// which method-name scopes make possible.
func TestMixedInvariants(t *testing.T) {
	t.Parallel()

	s := timeawaretest.MixedSuite.Suite(timeawaretest.DefaultMixedFixture())

	rows, err := suite.LockLines(s)
	if err != nil {
		t.Fatalf("render the manifest rows: %v", err)
	}
	suite.VerifyLock(t, "mixed.checks.lock", rows)
	suite.VerifyIndex(t, "Mixed",
		timeawaretest.MixedSuite.Checks.All(), s.IDs())
	suite.VerifyDropHints(t, "Mixed", s.IDs(), s.DropHint)
	suite.VerifyDistinctIDs(t, s.IDs())
}

// testkit: end of generated content.
// testkit:provenance 70167619122e62d8bf28d4c8645c4494e379c54f5d07ee4e0ee8b2f02a881e97
