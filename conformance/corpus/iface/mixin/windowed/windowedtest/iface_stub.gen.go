// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/windowed/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package windowedtest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/windowed"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// MixedRecordCall records one invocation of Mixed.Record.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type MixedRecordCall struct {
	Ctx context.Context
	Key string
	Err error
}

// MixedCountInCall records one invocation of Mixed.CountIn.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type MixedCountInCall struct {
	Ctx    context.Context
	Key    string
	Result int
	Err    error
}

// --- Per-method configuration ---

// MixedRecordStub controls how the double answers Record and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type MixedRecordStub struct {
	*stub.MethodStub[MixedRecordCall]

	fn       func(context.Context, string) error
	fallback *MixedRecordReturn
}

// MixedRecordReturn holds the fixed answer configured through Returns.
type MixedRecordReturn struct {
	Err error
}

// Returns pins a fixed result for every call to Record. A Func
// override and an injected fault both take precedence over it.
func (s *MixedRecordStub) Returns(err error) *MixedRecordStub {
	s.fallback = &MixedRecordReturn{Err: err}
	return s
}

// Func supplies a body for Record, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *MixedRecordStub) Func(fn func(context.Context, string) error) *MixedRecordStub {
	s.fn = fn
	return s
}

// MixedCountInStub controls how the double answers CountIn and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type MixedCountInStub struct {
	*stub.MethodStub[MixedCountInCall]

	fn       func(context.Context, string) (int, error)
	fallback *MixedCountInReturn
}

// MixedCountInReturn holds the fixed answer configured through Returns.
type MixedCountInReturn struct {
	Result int
	Err    error
}

// Returns pins a fixed result for every call to CountIn. A Func
// override and an injected fault both take precedence over it.
func (s *MixedCountInStub) Returns(result int, err error) *MixedCountInStub {
	s.fallback = &MixedCountInReturn{Result: result, Err: err}
	return s
}

// Func supplies a body for CountIn, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *MixedCountInStub) Func(fn func(context.Context, string) (int, error)) *MixedCountInStub {
	s.fn = fn
	return s
}

// --- MixedStub ---

// The double-runtime witness for the surface this double rides. A
// breaking change to the runtime renames it, and this file stops
// compiling with the skew named.
var _ = stub.CompatV1

// MixedStubOption configures a [MixedStub] at construction time.
type MixedStubOption func(*MixedStub)

// MixedStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func MixedStubStrict() MixedStubOption {
	return func(s *MixedStub) { s.strict = true }
}

// MixedStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func MixedStubDelegateTo(impl windowed.Mixed) MixedStubOption {
	return func(s *MixedStub) {
		s.OnRecord.Func(impl.Record)
		s.OnCountIn.Func(impl.CountIn)
	}
}

// MixedStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func MixedStubWithClock(clk clock.Clock) MixedStubOption {
	return func(s *MixedStub) { s.group.SetClock(clk) }
}

// MixedStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func MixedStubWithRandSource(src rand.Source) MixedStubOption {
	return func(s *MixedStub) { s.group.SetRandSource(src) }
}

// MixedStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func MixedStubBenchMode() MixedStubOption {
	return func(s *MixedStub) { s.group.BenchMode() }
}

// WithMixedRecord sets Record's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithMixedRecord(fn func(context.Context, string) error) MixedStubOption {
	return func(s *MixedStub) { s.OnRecord.Func(fn) }
}

// WithMixedCountIn sets CountIn's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithMixedCountIn(fn func(context.Context, string) (int, error)) MixedStubOption {
	return func(s *MixedStub) { s.OnCountIn.Func(fn) }
}

// MixedStub is a recording test double for Mixed.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type MixedStub struct {
	OnRecord  *MixedRecordStub
	OnCountIn *MixedCountInStub

	// group is every method stub above, viewed through the surface that
	// does not depend on a signature — one home for the settings and the
	// lifecycle that apply to the whole double.
	group  *stub.Group
	strict bool
}

// Compile-time proof that MixedStub satisfies Mixed.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ windowed.Mixed = (*MixedStub)(nil)

// NewMixedStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewMixedStub(tb testing.TB, opts ...MixedStubOption) *MixedStub {
	s := &MixedStub{
		OnRecord:  &MixedRecordStub{MethodStub: stub.NewMethodStub[MixedRecordCall](tb, "Mixed.Record")},
		OnCountIn: &MixedCountInStub{MethodStub: stub.NewMethodStub[MixedCountInCall](tb, "Mixed.CountIn")},
	}
	s.group = stub.NewGroup(
		s.OnRecord.MethodStub,
		s.OnCountIn.MethodStub,
	)
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.group.StrictAll()
	}
	s.group.Bind(tb)
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *MixedStub) ResetCalls() {
	s.group.Reset()
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *MixedRecordStub) invoke(ctx context.Context, key string) func() MixedRecordReturn {
	if s.fn == nil {
		return nil
	}
	return func() MixedRecordReturn {
		r0 := s.fn(ctx, key)
		return MixedRecordReturn{Err: r0}
	}
}

// Record records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *MixedStub) Record(ctx context.Context, key string) error {
	call := MixedRecordCall{Ctx: ctx, Key: key}
	r := stub.Answer(s.OnRecord.MethodStub, &call, stub.Arms[MixedRecordCall, MixedRecordReturn]{
		Invoke:   s.OnRecord.invoke(ctx, key),
		Fallback: s.OnRecord.fallback,
		Fault:    func(err error) MixedRecordReturn { return MixedRecordReturn{Err: err} },
		Stamp: func(c *MixedRecordCall, r MixedRecordReturn) {
			c.Err = r.Err
		},
	})
	return r.Err
}

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *MixedCountInStub) invoke(ctx context.Context, key string) func() MixedCountInReturn {
	if s.fn == nil {
		return nil
	}
	return func() MixedCountInReturn {
		r0, r1 := s.fn(ctx, key)
		return MixedCountInReturn{Result: r0, Err: r1}
	}
}

// CountIn records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *MixedStub) CountIn(ctx context.Context, key string) (int, error) {
	call := MixedCountInCall{Ctx: ctx, Key: key}
	r := stub.Answer(s.OnCountIn.MethodStub, &call, stub.Arms[MixedCountInCall, MixedCountInReturn]{
		Invoke:   s.OnCountIn.invoke(ctx, key),
		Fallback: s.OnCountIn.fallback,
		Fault:    func(err error) MixedCountInReturn { return MixedCountInReturn{Err: err} },
		Stamp: func(c *MixedCountInCall, r MixedCountInReturn) {
			c.Result = r.Result
			c.Err = r.Err
		},
	})
	return r.Result, r.Err
}

// testkit: end of generated content.
// testkit:provenance 41938bcc66b726a5c59dd4922a000f13555f4a149ffce6a99f958f6fefee3abd
