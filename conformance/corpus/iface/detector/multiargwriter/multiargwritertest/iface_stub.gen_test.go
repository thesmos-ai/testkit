// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/multiargwriter/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package multiargwritertest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/multiargwriter/multiargwritertest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// multiArgWriterStubSetSubject binds Set into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func multiArgWriterStubSetSubject(tb testing.TB) stub.Subject[multiargwritertest.MultiArgWriterSetCall, multiargwritertest.MultiArgWriterSetReturn] {
	tb.Helper()
	s := multiargwritertest.NewMultiArgWriterStub(tb)
	return stub.Subject[multiargwritertest.MultiArgWriterSetCall, multiargwritertest.MultiArgWriterSetReturn]{
		Stub: s.OnSet.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string = "test-key"
			var a2 string = "test-body"
			var a3 string = "test-mime"
			_ = s.Set(a0, a1, a2, a3)
		},
		Result: func() multiargwritertest.MultiArgWriterSetReturn {
			var a0 context.Context
			var a1 string = "test-key"
			var a2 string = "test-body"
			var a3 string = "test-mime"
			got0 := s.Set(a0, a1, a2, a3)
			return multiargwritertest.MultiArgWriterSetReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnSet.Func(func(_ context.Context, _ string, _ string, _ string) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 string = "test-key"
			var a2 string = "test-body"
			var a3 string = "test-mime"
			r0 := s.Set(a0, a1, a2, a3)
			return r0
		},
	}
}

// TestMultiArgWriterStubSet pins how Set answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMultiArgWriterStubSet(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Set", multiArgWriterStubSetSubject)

	// No check that Set answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := multiargwritertest.NewMultiArgWriterStub(t)
		var a0 context.Context
		var a1 string = "test-key"
		var a2 string = "test-body"
		var a3 string = "test-mime"
		_ = s.Set(a0, a1, a2, a3)
		got := s.OnSet.AssertCalledOnce(t, "Set must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Key, a1, "the recorded call carries Key")
		testkit.Equal(t, got.Body, a2, "the recorded call carries Body")
		testkit.Equal(t, got.Mime, a3, "the recorded call carries Mime")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := multiargwritertest.NewMultiArgWriterStub(t)
		var seen []multiargwritertest.MultiArgWriterSetCall
		s.OnSet.OnRecord(func(c multiargwritertest.MultiArgWriterSetCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string = "test-key"
		var a2 string = "test-body"
		var a3 string = "test-mime"
		_ = s.Set(a0, a1, a2, a3)
		_ = s.Set(a0, a1, a2, a3)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Set call")
	})

	t.Run("wires WithMultiArgWriterSet at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := multiargwritertest.NewMultiArgWriterStub(t, multiargwritertest.WithMultiArgWriterSet(func(_ context.Context, _ string, _ string, _ string) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		var a1 string = "test-key"
		var a2 string = "test-body"
		var a3 string = "test-mime"
		_ = s.Set(a0, a1, a2, a3)
		testkit.True(t, called, "WithMultiArgWriterSet must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Set admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := multiargwritertest.NewMultiArgWriterStub(t)
		var want0 error
		s.OnSet.Returns(want0)
		var a0 context.Context
		var a1 string = "test-key"
		var a2 string = "test-body"
		var a3 string = "test-mime"
		_ = s.Set(a0, a1, a2, a3)
		s.ResetCalls()
		_ = s.Set(a0, a1, a2, a3)
		s.OnSet.AssertCalledOnce(t, "Set must record the call that followed the reset")
	})
}

// multiArgWriterStubDouble describes how to build a MultiArgWriterStub under each
// option whose effect is the same whatever a method's signature.
//
// Set stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func multiArgWriterStubDouble() stub.Double[multiargwritertest.MultiArgWriterSetCall] {
	instance := func(s *multiargwritertest.MultiArgWriterStub) stub.Instance[multiargwritertest.MultiArgWriterSetCall] {
		return stub.Instance[multiargwritertest.MultiArgWriterSetCall]{
			Stub: s.OnSet.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 string = "test-key"
				var a2 string = "test-body"
				var a3 string = "test-mime"
				_ = s.Set(a0, a1, a2, a3)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[multiargwritertest.MultiArgWriterSetCall]{
		New: func(tb testing.TB) stub.Instance[multiargwritertest.MultiArgWriterSetCall] {
			return instance(multiargwritertest.NewMultiArgWriterStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[multiargwritertest.MultiArgWriterSetCall] {
			return instance(multiargwritertest.NewMultiArgWriterStub(tb, multiargwritertest.MultiArgWriterStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[multiargwritertest.MultiArgWriterSetCall] {
			return instance(multiargwritertest.NewMultiArgWriterStub(tb, multiargwritertest.MultiArgWriterStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[multiargwritertest.MultiArgWriterSetCall] {
			return instance(multiargwritertest.NewMultiArgWriterStub(tb, multiargwritertest.MultiArgWriterStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[multiargwritertest.MultiArgWriterSetCall] {
			return instance(multiargwritertest.NewMultiArgWriterStub(tb, multiargwritertest.MultiArgWriterStubStrict()))
		},
	}
}

// TestMultiArgWriterStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestMultiArgWriterStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, multiArgWriterStubDouble())
}

// TestMultiArgWriterStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestMultiArgWriterStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := multiargwritertest.NewMultiArgWriterStub(t)
	s := multiargwritertest.NewMultiArgWriterStub(t, multiargwritertest.MultiArgWriterStubDelegateTo(inner))

	t.Run("forwards Set to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string = "test-key"
		var a2 string = "test-body"
		var a3 string = "test-mime"
		_ = s.Set(a0, a1, a2, a3)
		inner.OnSet.AssertCalledOnce(t, "Set must reach the wrapped implementation")
	})

	t.Run("surfaces what Set answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Set-delegate")
		inner.OnSet.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 string = "test-key"
		var a2 string = "test-body"
		var a3 string = "test-mime"
		r0 := s.Set(a0, a1, a2, a3)
		testkit.ErrorIs(t, r0, want, "Set must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 6c5490307e4a2bf229cf26e4dd5dd0f3b3444f47f9e7dd74619a67409bd4065f
