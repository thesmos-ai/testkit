// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/multiargwriter/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package multiargwritertest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/multiargwriter"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// MultiArgWriterSetCall records one invocation of MultiArgWriter.Set.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type MultiArgWriterSetCall struct {
	Ctx  context.Context
	Key  string
	Body string
	Mime string
	Err  error
}

// --- Per-method configuration ---

// MultiArgWriterSetStub controls how the double answers Set and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type MultiArgWriterSetStub struct {
	*stub.MethodStub[MultiArgWriterSetCall]

	fn       func(context.Context, string, string, string) error
	fallback *MultiArgWriterSetReturn
}

// MultiArgWriterSetReturn holds the fixed answer configured through Returns.
type MultiArgWriterSetReturn struct {
	Err error
}

// Returns pins a fixed result for every call to Set. A Func
// override and an injected fault both take precedence over it.
func (s *MultiArgWriterSetStub) Returns(err error) *MultiArgWriterSetStub {
	s.fallback = &MultiArgWriterSetReturn{Err: err}
	return s
}

// Func supplies a body for Set, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *MultiArgWriterSetStub) Func(fn func(context.Context, string, string, string) error) *MultiArgWriterSetStub {
	s.fn = fn
	return s
}

// --- MultiArgWriterStub ---

// The double-runtime witness for the surface this double rides. A
// breaking change to the runtime renames it, and this file stops
// compiling with the skew named.
var _ = stub.CompatV1

// MultiArgWriterStubOption configures a [MultiArgWriterStub] at construction time.
type MultiArgWriterStubOption func(*MultiArgWriterStub)

// MultiArgWriterStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func MultiArgWriterStubStrict() MultiArgWriterStubOption {
	return func(s *MultiArgWriterStub) { s.strict = true }
}

// MultiArgWriterStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func MultiArgWriterStubDelegateTo(impl multiargwriter.MultiArgWriter) MultiArgWriterStubOption {
	return func(s *MultiArgWriterStub) {
		s.OnSet.Func(impl.Set)
	}
}

// MultiArgWriterStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func MultiArgWriterStubWithClock(clk clock.Clock) MultiArgWriterStubOption {
	return func(s *MultiArgWriterStub) { s.group.SetClock(clk) }
}

// MultiArgWriterStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func MultiArgWriterStubWithRandSource(src rand.Source) MultiArgWriterStubOption {
	return func(s *MultiArgWriterStub) { s.group.SetRandSource(src) }
}

// MultiArgWriterStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func MultiArgWriterStubBenchMode() MultiArgWriterStubOption {
	return func(s *MultiArgWriterStub) { s.group.BenchMode() }
}

// WithMultiArgWriterSet sets Set's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithMultiArgWriterSet(fn func(context.Context, string, string, string) error) MultiArgWriterStubOption {
	return func(s *MultiArgWriterStub) { s.OnSet.Func(fn) }
}

// MultiArgWriterStub is a recording test double for MultiArgWriter.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type MultiArgWriterStub struct {
	OnSet *MultiArgWriterSetStub

	// group is every method stub above, viewed through the surface that
	// does not depend on a signature — one home for the settings and the
	// lifecycle that apply to the whole double.
	group  *stub.Group
	strict bool
}

// Compile-time proof that MultiArgWriterStub satisfies MultiArgWriter.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ multiargwriter.MultiArgWriter = (*MultiArgWriterStub)(nil)

// NewMultiArgWriterStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewMultiArgWriterStub(tb testing.TB, opts ...MultiArgWriterStubOption) *MultiArgWriterStub {
	s := &MultiArgWriterStub{
		OnSet: &MultiArgWriterSetStub{MethodStub: stub.NewMethodStub[MultiArgWriterSetCall](tb, "MultiArgWriter.Set")},
	}
	s.group = stub.NewGroup(
		s.OnSet.MethodStub,
	)
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.group.StrictAll()
	}
	s.group.Bind(tb)
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *MultiArgWriterStub) ResetCalls() {
	s.group.Reset()
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *MultiArgWriterSetStub) invoke(ctx context.Context, key string, body string, mime string) func() MultiArgWriterSetReturn {
	if s.fn == nil {
		return nil
	}
	return func() MultiArgWriterSetReturn {
		r0 := s.fn(ctx, key, body, mime)
		return MultiArgWriterSetReturn{Err: r0}
	}
}

// Set records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *MultiArgWriterStub) Set(ctx context.Context, key string, body string, mime string) error {
	call := MultiArgWriterSetCall{Ctx: ctx, Key: key, Body: body, Mime: mime}
	r := stub.Answer(s.OnSet.MethodStub, &call, stub.Arms[MultiArgWriterSetCall, MultiArgWriterSetReturn]{
		Invoke:   s.OnSet.invoke(ctx, key, body, mime),
		Fallback: s.OnSet.fallback,
		Fault:    func(err error) MultiArgWriterSetReturn { return MultiArgWriterSetReturn{Err: err} },
		Stamp: func(c *MultiArgWriterSetCall, r MultiArgWriterSetReturn) {
			c.Err = r.Err
		},
	})
	return r.Err
}

// testkit: end of generated content.
// testkit:provenance 232ccc0e90d58839daf5a9b8f4209510515a9b670d09f56d9c3c6447c9d329a2
