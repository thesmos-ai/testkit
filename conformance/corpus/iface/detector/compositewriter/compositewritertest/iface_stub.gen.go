// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/compositewriter/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package compositewritertest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/compositewriter"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// CompositeWriterSetCall records one invocation of CompositeWriter.Set.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type CompositeWriterSetCall struct {
	Ctx context.Context
	Key string
	V   compositewriter.Value
	Err error
}

// --- Per-method configuration ---

// CompositeWriterSetStub controls how the double answers Set and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type CompositeWriterSetStub struct {
	*stub.MethodStub[CompositeWriterSetCall]

	fn       func(context.Context, string, compositewriter.Value) error
	fallback *CompositeWriterSetReturn
}

// CompositeWriterSetReturn holds the fixed answer configured through Returns.
type CompositeWriterSetReturn struct {
	Err error
}

// Returns pins a fixed result for every call to Set. A Func
// override and an injected fault both take precedence over it.
func (s *CompositeWriterSetStub) Returns(err error) *CompositeWriterSetStub {
	s.fallback = &CompositeWriterSetReturn{Err: err}
	return s
}

// Func supplies a body for Set, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *CompositeWriterSetStub) Func(fn func(context.Context, string, compositewriter.Value) error) *CompositeWriterSetStub {
	s.fn = fn
	return s
}

// --- CompositeWriterStub ---

// The double-runtime witness for the surface this double rides. A
// breaking change to the runtime renames it, and this file stops
// compiling with the skew named.
var _ = stub.CompatV1

// CompositeWriterStubOption configures a [CompositeWriterStub] at construction time.
type CompositeWriterStubOption func(*CompositeWriterStub)

// CompositeWriterStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func CompositeWriterStubStrict() CompositeWriterStubOption {
	return func(s *CompositeWriterStub) { s.strict = true }
}

// CompositeWriterStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func CompositeWriterStubDelegateTo(impl compositewriter.CompositeWriter) CompositeWriterStubOption {
	return func(s *CompositeWriterStub) {
		s.OnSet.Func(impl.Set)
	}
}

// CompositeWriterStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func CompositeWriterStubWithClock(clk clock.Clock) CompositeWriterStubOption {
	return func(s *CompositeWriterStub) { s.group.SetClock(clk) }
}

// CompositeWriterStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func CompositeWriterStubWithRandSource(src rand.Source) CompositeWriterStubOption {
	return func(s *CompositeWriterStub) { s.group.SetRandSource(src) }
}

// CompositeWriterStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func CompositeWriterStubBenchMode() CompositeWriterStubOption {
	return func(s *CompositeWriterStub) { s.group.BenchMode() }
}

// WithCompositeWriterSet sets Set's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithCompositeWriterSet(fn func(context.Context, string, compositewriter.Value) error) CompositeWriterStubOption {
	return func(s *CompositeWriterStub) { s.OnSet.Func(fn) }
}

// CompositeWriterStub is a recording test double for CompositeWriter.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type CompositeWriterStub struct {
	OnSet *CompositeWriterSetStub

	// group is every method stub above, viewed through the surface that
	// does not depend on a signature — one home for the settings and the
	// lifecycle that apply to the whole double.
	group  *stub.Group
	strict bool
}

// Compile-time proof that CompositeWriterStub satisfies CompositeWriter.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ compositewriter.CompositeWriter = (*CompositeWriterStub)(nil)

// NewCompositeWriterStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewCompositeWriterStub(tb testing.TB, opts ...CompositeWriterStubOption) *CompositeWriterStub {
	s := &CompositeWriterStub{
		OnSet: &CompositeWriterSetStub{MethodStub: stub.NewMethodStub[CompositeWriterSetCall](tb, "CompositeWriter.Set")},
	}
	s.group = stub.NewGroup(
		s.OnSet.MethodStub,
	)
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.group.StrictAll()
	}
	s.group.Bind(tb)
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *CompositeWriterStub) ResetCalls() {
	s.group.Reset()
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *CompositeWriterSetStub) invoke(ctx context.Context, key string, v compositewriter.Value) func() CompositeWriterSetReturn {
	if s.fn == nil {
		return nil
	}
	return func() CompositeWriterSetReturn {
		r0 := s.fn(ctx, key, v)
		return CompositeWriterSetReturn{Err: r0}
	}
}

// Set records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *CompositeWriterStub) Set(ctx context.Context, key string, v compositewriter.Value) error {
	call := CompositeWriterSetCall{Ctx: ctx, Key: key, V: v}
	r := stub.Answer(s.OnSet.MethodStub, &call, stub.Arms[CompositeWriterSetCall, CompositeWriterSetReturn]{
		Invoke:   s.OnSet.invoke(ctx, key, v),
		Fallback: s.OnSet.fallback,
		Fault:    func(err error) CompositeWriterSetReturn { return CompositeWriterSetReturn{Err: err} },
		Stamp: func(c *CompositeWriterSetCall, r CompositeWriterSetReturn) {
			c.Err = r.Err
		},
	})
	return r.Err
}

// testkit: end of generated content.
// testkit:provenance 306a13b73dde89018af13dac7cba97c7f41483a558188a06cf3df9f757dfedb5
