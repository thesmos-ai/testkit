// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/batchreader/iface.go
// Plugins:   golang 1.0.0, suite 1.21.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package batchreadertest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/batchreader"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/batchreader/batchreadertest"
	"go.thesmos.sh/testkit/engine/suite"
	"go.thesmos.sh/testkit/engine/suite/prove"
)

// TestBatchReaderProofs drives every planted defect through the check it is
// evidence for.
//
// A red here is the expected outcome for each one; a GREEN is the finding.
// It means a check tolerated the implementation built to break it, and a
// check that cannot fail is a line in a report rather than a claim about
// the subject.
func TestBatchReaderProofs(t *testing.T) {
	t.Parallel()
	prove.All(t,
		batchreadertest.BatchReaderSuite.Suite(batchreadertest.DefaultBatchReaderFixture()).Checks,
		batchReaderProofs())
}

// batchReaderProofs is every defect this run derived and can spell.
//
// Each is the smallest implementation that breaks exactly one claim: the
// generated double with one method overridden, and nothing else changed.
// The reason beside it is the substring the red must contain, so a defect
// that died on an unrelated guard stops counting as evidence.
func batchReaderProofs() prove.Defects[batchreadertest.BatchReader] {
	ix := batchreadertest.BatchReaderSuite.Checks
	return prove.Defects[batchreadertest.BatchReader]{
		ix.GetAll.Smoke(): prove.One("a BatchReader whose GetAll panics",
			func(tb testing.TB) batchreadertest.BatchReader {
				return batchreadertest.NewBatchReaderStub(tb, batchreadertest.WithBatchReaderGetAll(
					func(_ context.Context, _ ...string) ([]batchreader.Value, error) {
						panic("planted: GetAll panics")
					}))
			}).Reasoned(suite.RedPanicked),
		ix.GetAll.Cancels(): prove.One("a BatchReader whose GetAll ignores the context it is handed",
			func(tb testing.TB) batchreadertest.BatchReader {
				return batchreadertest.NewBatchReaderStub(tb, batchreadertest.WithBatchReaderGetAll(
					func(_ context.Context, _ ...string) (r0 []batchreader.Value, err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedCancelled),
		ix.GetAll.NilContext(): prove.One("a BatchReader whose GetAll forgives a nil context and answers",
			func(tb testing.TB) batchreadertest.BatchReader {
				return batchreadertest.NewBatchReaderStub(tb, batchreadertest.WithBatchReaderGetAll(
					func(_ context.Context, _ ...string) (r0 []batchreader.Value, err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedNilContext),
		ix.GetAll.Deadline(): prove.One("a BatchReader whose GetAll ignores the context it is handed",
			func(tb testing.TB) batchreadertest.BatchReader {
				return batchreadertest.NewBatchReaderStub(tb, batchreadertest.WithBatchReaderGetAll(
					func(_ context.Context, _ ...string) (r0 []batchreader.Value, err error) {
						// The call arrives and nothing is done with it; the bare
						// return answers every slot's zero, which for the error
						// slot is the nil this claim forbids.
						return
					}))
			}).Reasoned(suite.RedDeadline),
		ix.GetAll.ZeroOnError(): prove.One("a BatchReader whose GetAll answers a believable value beside its error",
			func(tb testing.TB) batchreadertest.BatchReader {
				return batchreadertest.NewBatchReaderStub(tb, batchreadertest.WithBatchReaderGetAll(
					func(_ context.Context, _ ...string) (r0 []batchreader.Value, err error) {
						// A believable answer beside the refusal. A caller
						// reading the error and one reading the value disagree
						// about what happened, which is the claim's own
						// violation rather than a subject that merely failed.
						r0 = []batchreader.Value{{Key: "other-"}}
						err = errors.New("planted: GetAll refused with a believable value")
						return
					}))
			}),
	}
}

// TestBatchReaderInvariants holds this package to what it says about itself.
//
// Four claims, none of which a consumer writes and none of which any
// other gate makes. The manifest pins the assertion SET, so a check that
// stops being emitted reads as a removed row in review rather than as a
// suite that quietly got smaller. The index and the drop hints pin the
// two ways a check is NAMED — an index entry pointing at nothing
// compiles, and a drop hint that falls back to the string form forfeits
// exactly the compile-break the index exists to give. The distinct-ID
// check catches two interfaces in one package claiming one identity,
// which method-name scopes make possible.
func TestBatchReaderInvariants(t *testing.T) {
	t.Parallel()

	s := batchreadertest.BatchReaderSuite.Suite(batchreadertest.DefaultBatchReaderFixture())

	rows, err := suite.LockLines(s)
	if err != nil {
		t.Fatalf("render the manifest rows: %v", err)
	}
	suite.VerifyLock(t, "batchReader.checks.lock", rows)
	suite.VerifyIndex(t, "BatchReader",
		batchreadertest.BatchReaderSuite.Checks.All(), s.IDs())
	suite.VerifyDropHints(t, "BatchReader", s.IDs(), s.DropHint)
	suite.VerifyDistinctIDs(t, s.IDs())
}

// testkit: end of generated content.
// testkit:provenance 17e60ab02f99a3ddbc4d1ebc2f0ac271a8a809634105a92840353ae69716a735
