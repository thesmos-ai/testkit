// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/batchreader/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package batchreadertest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/batchreader"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// BatchReaderGetAllCall records one invocation of BatchReader.GetAll.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type BatchReaderGetAllCall struct {
	Ctx    context.Context
	Keys   []string
	Result []batchreader.Value
	Err    error
}

// --- Per-method configuration ---

// BatchReaderGetAllStub controls how the double answers GetAll and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type BatchReaderGetAllStub struct {
	*stub.MethodStub[BatchReaderGetAllCall]

	fn       func(context.Context, ...string) ([]batchreader.Value, error)
	fallback *BatchReaderGetAllReturn
}

// BatchReaderGetAllReturn holds the fixed answer configured through Returns.
type BatchReaderGetAllReturn struct {
	Result []batchreader.Value
	Err    error
}

// Returns pins a fixed result for every call to GetAll. A Func
// override and an injected fault both take precedence over it.
func (s *BatchReaderGetAllStub) Returns(result []batchreader.Value, err error) *BatchReaderGetAllStub {
	s.fallback = &BatchReaderGetAllReturn{Result: result, Err: err}
	return s
}

// Func supplies a body for GetAll, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *BatchReaderGetAllStub) Func(fn func(context.Context, ...string) ([]batchreader.Value, error)) *BatchReaderGetAllStub {
	s.fn = fn
	return s
}

// --- BatchReaderStub ---

// The double-runtime witness for the surface this double rides. A
// breaking change to the runtime renames it, and this file stops
// compiling with the skew named.
var _ = stub.CompatV1

// BatchReaderStubOption configures a [BatchReaderStub] at construction time.
type BatchReaderStubOption func(*BatchReaderStub)

// BatchReaderStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func BatchReaderStubStrict() BatchReaderStubOption {
	return func(s *BatchReaderStub) { s.strict = true }
}

// BatchReaderStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func BatchReaderStubDelegateTo(impl batchreader.BatchReader) BatchReaderStubOption {
	return func(s *BatchReaderStub) {
		s.OnGetAll.Func(impl.GetAll)
	}
}

// BatchReaderStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func BatchReaderStubWithClock(clk clock.Clock) BatchReaderStubOption {
	return func(s *BatchReaderStub) { s.group.SetClock(clk) }
}

// BatchReaderStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func BatchReaderStubWithRandSource(src rand.Source) BatchReaderStubOption {
	return func(s *BatchReaderStub) { s.group.SetRandSource(src) }
}

// BatchReaderStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func BatchReaderStubBenchMode() BatchReaderStubOption {
	return func(s *BatchReaderStub) { s.group.BenchMode() }
}

// WithBatchReaderGetAll sets GetAll's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithBatchReaderGetAll(fn func(context.Context, ...string) ([]batchreader.Value, error)) BatchReaderStubOption {
	return func(s *BatchReaderStub) { s.OnGetAll.Func(fn) }
}

// BatchReaderStub is a recording test double for BatchReader.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type BatchReaderStub struct {
	OnGetAll *BatchReaderGetAllStub

	// group is every method stub above, viewed through the surface that
	// does not depend on a signature — one home for the settings and the
	// lifecycle that apply to the whole double.
	group  *stub.Group
	strict bool
}

// Compile-time proof that BatchReaderStub satisfies BatchReader.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ batchreader.BatchReader = (*BatchReaderStub)(nil)

// NewBatchReaderStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewBatchReaderStub(tb testing.TB, opts ...BatchReaderStubOption) *BatchReaderStub {
	s := &BatchReaderStub{
		OnGetAll: &BatchReaderGetAllStub{MethodStub: stub.NewMethodStub[BatchReaderGetAllCall](tb, "BatchReader.GetAll")},
	}
	s.group = stub.NewGroup(
		s.OnGetAll.MethodStub,
	)
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.group.StrictAll()
	}
	s.group.Bind(tb)
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *BatchReaderStub) ResetCalls() {
	s.group.Reset()
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *BatchReaderGetAllStub) invoke(ctx context.Context, keys ...string) func() BatchReaderGetAllReturn {
	if s.fn == nil {
		return nil
	}
	return func() BatchReaderGetAllReturn {
		r0, r1 := s.fn(ctx, keys...)
		return BatchReaderGetAllReturn{Result: r0, Err: r1}
	}
}

// GetAll records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *BatchReaderStub) GetAll(ctx context.Context, keys ...string) ([]batchreader.Value, error) {
	call := BatchReaderGetAllCall{Ctx: ctx, Keys: keys}
	r := stub.Answer(s.OnGetAll.MethodStub, &call, stub.Arms[BatchReaderGetAllCall, BatchReaderGetAllReturn]{
		Invoke:   s.OnGetAll.invoke(ctx, keys...),
		Fallback: s.OnGetAll.fallback,
		Fault:    func(err error) BatchReaderGetAllReturn { return BatchReaderGetAllReturn{Err: err} },
		Stamp: func(c *BatchReaderGetAllCall, r BatchReaderGetAllReturn) {
			c.Result = r.Result
			c.Err = r.Err
		},
	})
	return r.Result, r.Err
}

// testkit: end of generated content.
// testkit:provenance 3624148d09f8f4fce99d6a945615046833c343c72fb7f45198a0ade624610c54
