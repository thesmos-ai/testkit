// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/readerwithbool/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package readerwithbooltest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/readerwithbool"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/readerwithbool/readerwithbooltest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// readerWithBoolStubLoadSubject binds Load into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func readerWithBoolStubLoadSubject(tb testing.TB) stub.Subject[readerwithbooltest.ReaderWithBoolLoadCall, readerwithbooltest.ReaderWithBoolLoadReturn] {
	tb.Helper()
	s := readerwithbooltest.NewReaderWithBoolStub(tb)
	return stub.Subject[readerwithbooltest.ReaderWithBoolLoadCall, readerwithbooltest.ReaderWithBoolLoadReturn]{
		Stub: s.OnLoad.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string = "test-key"
			_, _ = s.Load(a0, a1)
		},
		Result: func() readerwithbooltest.ReaderWithBoolLoadReturn {
			var a0 context.Context
			var a1 string = "test-key"
			got0, got1 := s.Load(a0, a1)
			return readerwithbooltest.ReaderWithBoolLoadReturn{Result0: got0, Result1: got1}
		},
		Override: func(mark func()) {
			s.OnLoad.Func(func(_ context.Context, _ string) (readerwithbool.Value, bool) {
				mark()
				var z0 readerwithbool.Value
				var z1 bool
				return z0, z1
			})
		},
	}
}

// TestReaderWithBoolStubLoad pins how Load answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestReaderWithBoolStubLoad(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Load", readerWithBoolStubLoadSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := readerwithbooltest.NewReaderWithBoolStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 readerwithbool.Value
		var want1 bool = true
		s.OnLoad.Returns(want0, want1)
		var a0 context.Context
		var a1 string = "test-key"
		_, got1 := s.Load(a0, a1)
		testkit.Equal(t, got1, want1, "Load must answer with what Returns pinned")
		// Blanked above rather than dropped in silence. No literal exists
		// for result 0 (go.thesmos.sh/testkit/conformance/corpus/iface/detector/readerwithbool.Value), so the only value to pin there
		// is the zero — and a double that ignored Returns answers the zero
		// too, which makes the comparison pass either way.
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := readerwithbooltest.NewReaderWithBoolStub(t)
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.Load(a0, a1)
		got := s.OnLoad.AssertCalledOnce(t, "Load must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Key, a1, "the recorded call carries Key")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := readerwithbooltest.NewReaderWithBoolStub(t)
		var seen []readerwithbooltest.ReaderWithBoolLoadCall
		s.OnLoad.OnRecord(func(c readerwithbooltest.ReaderWithBoolLoadCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.Load(a0, a1)
		_, _ = s.Load(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Load call")
	})

	t.Run("wires WithReaderWithBoolLoad at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := readerwithbooltest.NewReaderWithBoolStub(t, readerwithbooltest.WithReaderWithBoolLoad(func(_ context.Context, _ string) (readerwithbool.Value, bool) {
			called = true
			var z0 readerwithbool.Value
			var z1 bool
			return z0, z1
		}))
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.Load(a0, a1)
		testkit.True(t, called, "WithReaderWithBoolLoad must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		//
		// Pinned to a literal for the reason the Returns check is: a reset
		// that lost the configuration answers the zero, and so does one that
		// kept a configured zero.
		s := readerwithbooltest.NewReaderWithBoolStub(t)
		var want0 readerwithbool.Value
		var want1 bool = true
		s.OnLoad.Returns(want0, want1)
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.Load(a0, a1)
		s.ResetCalls()
		_, got1 := s.Load(a0, a1)
		testkit.Equal(t, got1, want1, "a reset must keep what Returns pinned")
	})
}

// readerWithBoolStubDouble describes how to build a ReaderWithBoolStub under each
// option whose effect is the same whatever a method's signature.
//
// Load stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func readerWithBoolStubDouble() stub.Double[readerwithbooltest.ReaderWithBoolLoadCall] {
	instance := func(s *readerwithbooltest.ReaderWithBoolStub) stub.Instance[readerwithbooltest.ReaderWithBoolLoadCall] {
		return stub.Instance[readerwithbooltest.ReaderWithBoolLoadCall]{
			Stub: s.OnLoad.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 string = "test-key"
				_, _ = s.Load(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[readerwithbooltest.ReaderWithBoolLoadCall]{
		New: func(tb testing.TB) stub.Instance[readerwithbooltest.ReaderWithBoolLoadCall] {
			return instance(readerwithbooltest.NewReaderWithBoolStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[readerwithbooltest.ReaderWithBoolLoadCall] {
			return instance(readerwithbooltest.NewReaderWithBoolStub(tb, readerwithbooltest.ReaderWithBoolStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[readerwithbooltest.ReaderWithBoolLoadCall] {
			return instance(readerwithbooltest.NewReaderWithBoolStub(tb, readerwithbooltest.ReaderWithBoolStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[readerwithbooltest.ReaderWithBoolLoadCall] {
			return instance(readerwithbooltest.NewReaderWithBoolStub(tb, readerwithbooltest.ReaderWithBoolStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[readerwithbooltest.ReaderWithBoolLoadCall] {
			return instance(readerwithbooltest.NewReaderWithBoolStub(tb, readerwithbooltest.ReaderWithBoolStubStrict()))
		},
	}
}

// TestReaderWithBoolStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestReaderWithBoolStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, readerWithBoolStubDouble())
}

// TestReaderWithBoolStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestReaderWithBoolStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := readerwithbooltest.NewReaderWithBoolStub(t)
	s := readerwithbooltest.NewReaderWithBoolStub(t, readerwithbooltest.ReaderWithBoolStubDelegateTo(inner))

	t.Run("forwards Load to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string = "test-key"
		_, _ = s.Load(a0, a1)
		inner.OnLoad.AssertCalledOnce(t, "Load must reach the wrapped implementation")
	})
}

// testkit: end of generated content.
// testkit:provenance 20c7db79a8f27836507c3b49037517dd3e9fca89613c44c4c073f28058efc10b
