// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/readerwithbool/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package readerwithbooltest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/readerwithbool"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// ReaderWithBoolLoadCall records one invocation of ReaderWithBool.Load.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type ReaderWithBoolLoadCall struct {
	Ctx     context.Context
	Key     string
	Result0 readerwithbool.Value
	Result1 bool
}

// --- Per-method configuration ---

// ReaderWithBoolLoadStub controls how the double answers Load and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type ReaderWithBoolLoadStub struct {
	*stub.MethodStub[ReaderWithBoolLoadCall]

	fn       func(context.Context, string) (readerwithbool.Value, bool)
	fallback *ReaderWithBoolLoadReturn
}

// ReaderWithBoolLoadReturn holds the fixed answer configured through Returns.
type ReaderWithBoolLoadReturn struct {
	Result0 readerwithbool.Value
	Result1 bool
}

// Returns pins a fixed result for every call to Load. A Func
// override and an injected fault both take precedence over it.
func (s *ReaderWithBoolLoadStub) Returns(result0 readerwithbool.Value, result1 bool) *ReaderWithBoolLoadStub {
	s.fallback = &ReaderWithBoolLoadReturn{Result0: result0, Result1: result1}
	return s
}

// Func supplies a body for Load, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *ReaderWithBoolLoadStub) Func(fn func(context.Context, string) (readerwithbool.Value, bool)) *ReaderWithBoolLoadStub {
	s.fn = fn
	return s
}

// --- ReaderWithBoolStub ---

// The double-runtime witness for the surface this double rides. A
// breaking change to the runtime renames it, and this file stops
// compiling with the skew named.
var _ = stub.CompatV1

// ReaderWithBoolStubOption configures a [ReaderWithBoolStub] at construction time.
type ReaderWithBoolStubOption func(*ReaderWithBoolStub)

// ReaderWithBoolStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func ReaderWithBoolStubStrict() ReaderWithBoolStubOption {
	return func(s *ReaderWithBoolStub) { s.strict = true }
}

// ReaderWithBoolStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func ReaderWithBoolStubDelegateTo(impl readerwithbool.ReaderWithBool) ReaderWithBoolStubOption {
	return func(s *ReaderWithBoolStub) {
		s.OnLoad.Func(impl.Load)
	}
}

// ReaderWithBoolStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func ReaderWithBoolStubWithClock(clk clock.Clock) ReaderWithBoolStubOption {
	return func(s *ReaderWithBoolStub) { s.group.SetClock(clk) }
}

// ReaderWithBoolStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func ReaderWithBoolStubWithRandSource(src rand.Source) ReaderWithBoolStubOption {
	return func(s *ReaderWithBoolStub) { s.group.SetRandSource(src) }
}

// ReaderWithBoolStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func ReaderWithBoolStubBenchMode() ReaderWithBoolStubOption {
	return func(s *ReaderWithBoolStub) { s.group.BenchMode() }
}

// WithReaderWithBoolLoad sets Load's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithReaderWithBoolLoad(fn func(context.Context, string) (readerwithbool.Value, bool)) ReaderWithBoolStubOption {
	return func(s *ReaderWithBoolStub) { s.OnLoad.Func(fn) }
}

// ReaderWithBoolStub is a recording test double for ReaderWithBool.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type ReaderWithBoolStub struct {
	OnLoad *ReaderWithBoolLoadStub

	// group is every method stub above, viewed through the surface that
	// does not depend on a signature — one home for the settings and the
	// lifecycle that apply to the whole double.
	group  *stub.Group
	strict bool
}

// Compile-time proof that ReaderWithBoolStub satisfies ReaderWithBool.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ readerwithbool.ReaderWithBool = (*ReaderWithBoolStub)(nil)

// NewReaderWithBoolStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewReaderWithBoolStub(tb testing.TB, opts ...ReaderWithBoolStubOption) *ReaderWithBoolStub {
	s := &ReaderWithBoolStub{
		OnLoad: &ReaderWithBoolLoadStub{MethodStub: stub.NewMethodStub[ReaderWithBoolLoadCall](tb, "ReaderWithBool.Load")},
	}
	s.group = stub.NewGroup(
		s.OnLoad.MethodStub,
	)
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.group.StrictAll()
	}
	s.group.Bind(tb)
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *ReaderWithBoolStub) ResetCalls() {
	s.group.Reset()
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *ReaderWithBoolLoadStub) invoke(ctx context.Context, key string) func() ReaderWithBoolLoadReturn {
	if s.fn == nil {
		return nil
	}
	return func() ReaderWithBoolLoadReturn {
		r0, r1 := s.fn(ctx, key)
		return ReaderWithBoolLoadReturn{Result0: r0, Result1: r1}
	}
}

// Load records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *ReaderWithBoolStub) Load(ctx context.Context, key string) (readerwithbool.Value, bool) {
	call := ReaderWithBoolLoadCall{Ctx: ctx, Key: key}
	r := stub.Answer(s.OnLoad.MethodStub, &call, stub.Arms[ReaderWithBoolLoadCall, ReaderWithBoolLoadReturn]{
		Invoke:   s.OnLoad.invoke(ctx, key),
		Fallback: s.OnLoad.fallback,
		Stamp: func(c *ReaderWithBoolLoadCall, r ReaderWithBoolLoadReturn) {
			c.Result0 = r.Result0
			c.Result1 = r.Result1
		},
	})
	return r.Result0, r.Result1
}

// testkit: end of generated content.
// testkit:provenance 7f9a6be22cc3b88f5d18e60f2b27e7b7196f0c0613ea0d86ef0ad7063c74bec4
