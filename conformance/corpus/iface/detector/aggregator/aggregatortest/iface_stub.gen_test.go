// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/aggregator/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package aggregatortest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/aggregator/aggregatortest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// aggregatorStubCountSubject binds Count into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func aggregatorStubCountSubject(tb testing.TB) stub.Subject[aggregatortest.AggregatorCountCall, aggregatortest.AggregatorCountReturn] {
	tb.Helper()
	s := aggregatortest.NewAggregatorStub(tb)
	return stub.Subject[aggregatortest.AggregatorCountCall, aggregatortest.AggregatorCountReturn]{
		Stub: s.OnCount.MethodStub,
		Call: func() {
			var a0 context.Context
			_, _ = s.Count(a0)
		},
		Result: func() aggregatortest.AggregatorCountReturn {
			var a0 context.Context
			got0, got1 := s.Count(a0)
			return aggregatortest.AggregatorCountReturn{Result: got0, Err: got1}
		},
		Override: func(mark func()) {
			s.OnCount.Func(func(_ context.Context) (int, error) {
				mark()
				var z0 int
				var z1 error
				return z0, z1
			})
		},
		Fails: func() error {
			var a0 context.Context
			_, r1 := s.Count(a0)
			return r1
		},
	}
}

// TestAggregatorStubCount pins how Count answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestAggregatorStubCount(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Count", aggregatorStubCountSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := aggregatortest.NewAggregatorStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 int = 42
		var want1 error
		s.OnCount.Returns(want0, want1)
		var a0 context.Context
		got0, _ := s.Count(a0)
		testkit.Equal(t, got0, want0, "Count must answer with what Returns pinned")
		// Blanked above rather than dropped in silence. No literal exists
		// for result 1 (error), so the only value to pin there
		// is the zero — and a double that ignored Returns answers the zero
		// too, which makes the comparison pass either way.
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := aggregatortest.NewAggregatorStub(t)
		var a0 context.Context
		_, _ = s.Count(a0)
		got := s.OnCount.AssertCalledOnce(t, "Count must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := aggregatortest.NewAggregatorStub(t)
		var seen []aggregatortest.AggregatorCountCall
		s.OnCount.OnRecord(func(c aggregatortest.AggregatorCountCall) { seen = append(seen, c) })
		var a0 context.Context
		_, _ = s.Count(a0)
		_, _ = s.Count(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Count call")
	})

	t.Run("wires WithAggregatorCount at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := aggregatortest.NewAggregatorStub(t, aggregatortest.WithAggregatorCount(func(_ context.Context) (int, error) {
			called = true
			var z0 int
			var z1 error
			return z0, z1
		}))
		var a0 context.Context
		_, _ = s.Count(a0)
		testkit.True(t, called, "WithAggregatorCount must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		//
		// Pinned to a literal for the reason the Returns check is: a reset
		// that lost the configuration answers the zero, and so does one that
		// kept a configured zero.
		s := aggregatortest.NewAggregatorStub(t)
		var want0 int = 42
		var want1 error
		s.OnCount.Returns(want0, want1)
		var a0 context.Context
		_, _ = s.Count(a0)
		s.ResetCalls()
		got0, _ := s.Count(a0)
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
	})
}

// aggregatorStubDouble describes how to build a AggregatorStub under each
// option whose effect is the same whatever a method's signature.
//
// Count stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func aggregatorStubDouble() stub.Double[aggregatortest.AggregatorCountCall] {
	instance := func(s *aggregatortest.AggregatorStub) stub.Instance[aggregatortest.AggregatorCountCall] {
		return stub.Instance[aggregatortest.AggregatorCountCall]{
			Stub: s.OnCount.MethodStub,
			Call: func() {
				var a0 context.Context
				_, _ = s.Count(a0)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[aggregatortest.AggregatorCountCall]{
		New: func(tb testing.TB) stub.Instance[aggregatortest.AggregatorCountCall] {
			return instance(aggregatortest.NewAggregatorStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[aggregatortest.AggregatorCountCall] {
			return instance(aggregatortest.NewAggregatorStub(tb, aggregatortest.AggregatorStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[aggregatortest.AggregatorCountCall] {
			return instance(aggregatortest.NewAggregatorStub(tb, aggregatortest.AggregatorStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[aggregatortest.AggregatorCountCall] {
			return instance(aggregatortest.NewAggregatorStub(tb, aggregatortest.AggregatorStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[aggregatortest.AggregatorCountCall] {
			return instance(aggregatortest.NewAggregatorStub(tb, aggregatortest.AggregatorStubStrict()))
		},
	}
}

// TestAggregatorStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestAggregatorStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, aggregatorStubDouble())
}

// TestAggregatorStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestAggregatorStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := aggregatortest.NewAggregatorStub(t)
	s := aggregatortest.NewAggregatorStub(t, aggregatortest.AggregatorStubDelegateTo(inner))

	t.Run("forwards Count to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_, _ = s.Count(a0)
		inner.OnCount.AssertCalledOnce(t, "Count must reach the wrapped implementation")
	})

	t.Run("surfaces what Count answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Count-delegate")
		inner.OnCount.FaultsFor(time.Hour, want)
		var a0 context.Context
		_, r1 := s.Count(a0)
		testkit.ErrorIs(t, r1, want, "Count must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance ced903dc72a7cbf6eeb16213322723069ad55b3569ddfe358197fb91685102b6
