// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/aggregator/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package aggregatortest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/aggregator"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// AggregatorCountCall records one invocation of Aggregator.Count.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type AggregatorCountCall struct {
	Ctx    context.Context
	Result int
	Err    error
}

// --- Per-method configuration ---

// AggregatorCountStub controls how the double answers Count and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type AggregatorCountStub struct {
	*stub.MethodStub[AggregatorCountCall]

	fn       func(context.Context) (int, error)
	fallback *AggregatorCountReturn
}

// AggregatorCountReturn holds the fixed answer configured through Returns.
type AggregatorCountReturn struct {
	Result int
	Err    error
}

// Returns pins a fixed result for every call to Count. A Func
// override and an injected fault both take precedence over it.
func (s *AggregatorCountStub) Returns(result int, err error) *AggregatorCountStub {
	s.fallback = &AggregatorCountReturn{Result: result, Err: err}
	return s
}

// Func supplies a body for Count, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *AggregatorCountStub) Func(fn func(context.Context) (int, error)) *AggregatorCountStub {
	s.fn = fn
	return s
}

// --- AggregatorStub ---

// The double-runtime witness for the surface this double rides. A
// breaking change to the runtime renames it, and this file stops
// compiling with the skew named.
var _ = stub.CompatV1

// AggregatorStubOption configures a [AggregatorStub] at construction time.
type AggregatorStubOption func(*AggregatorStub)

// AggregatorStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func AggregatorStubStrict() AggregatorStubOption {
	return func(s *AggregatorStub) { s.strict = true }
}

// AggregatorStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func AggregatorStubDelegateTo(impl aggregator.Aggregator) AggregatorStubOption {
	return func(s *AggregatorStub) {
		s.OnCount.Func(impl.Count)
	}
}

// AggregatorStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func AggregatorStubWithClock(clk clock.Clock) AggregatorStubOption {
	return func(s *AggregatorStub) { s.group.SetClock(clk) }
}

// AggregatorStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func AggregatorStubWithRandSource(src rand.Source) AggregatorStubOption {
	return func(s *AggregatorStub) { s.group.SetRandSource(src) }
}

// AggregatorStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func AggregatorStubBenchMode() AggregatorStubOption {
	return func(s *AggregatorStub) { s.group.BenchMode() }
}

// WithAggregatorCount sets Count's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithAggregatorCount(fn func(context.Context) (int, error)) AggregatorStubOption {
	return func(s *AggregatorStub) { s.OnCount.Func(fn) }
}

// AggregatorStub is a recording test double for Aggregator.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type AggregatorStub struct {
	OnCount *AggregatorCountStub

	// group is every method stub above, viewed through the surface that
	// does not depend on a signature — one home for the settings and the
	// lifecycle that apply to the whole double.
	group  *stub.Group
	strict bool
}

// Compile-time proof that AggregatorStub satisfies Aggregator.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ aggregator.Aggregator = (*AggregatorStub)(nil)

// NewAggregatorStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewAggregatorStub(tb testing.TB, opts ...AggregatorStubOption) *AggregatorStub {
	s := &AggregatorStub{
		OnCount: &AggregatorCountStub{MethodStub: stub.NewMethodStub[AggregatorCountCall](tb, "Aggregator.Count")},
	}
	s.group = stub.NewGroup(
		s.OnCount.MethodStub,
	)
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.group.StrictAll()
	}
	s.group.Bind(tb)
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *AggregatorStub) ResetCalls() {
	s.group.Reset()
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *AggregatorCountStub) invoke(ctx context.Context) func() AggregatorCountReturn {
	if s.fn == nil {
		return nil
	}
	return func() AggregatorCountReturn {
		r0, r1 := s.fn(ctx)
		return AggregatorCountReturn{Result: r0, Err: r1}
	}
}

// Count records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *AggregatorStub) Count(ctx context.Context) (int, error) {
	call := AggregatorCountCall{Ctx: ctx}
	r := stub.Answer(s.OnCount.MethodStub, &call, stub.Arms[AggregatorCountCall, AggregatorCountReturn]{
		Invoke:   s.OnCount.invoke(ctx),
		Fallback: s.OnCount.fallback,
		Fault:    func(err error) AggregatorCountReturn { return AggregatorCountReturn{Err: err} },
		Stamp: func(c *AggregatorCountCall, r AggregatorCountReturn) {
			c.Result = r.Result
			c.Err = r.Err
		},
	})
	return r.Result, r.Err
}

// testkit: end of generated content.
// testkit:provenance bf75304288c0f0ce6a2022a7dc24f9f05870a2cf9bd07c35885f1deb53d8131f
