// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/streamreader/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package streamreadertest

import (
	"context"
	"iter"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/streamreader"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// StreamReaderAddCall records one invocation of StreamReader.Add.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type StreamReaderAddCall struct {
	Ctx context.Context
	V   streamreader.Value
	Err error
}

// StreamReaderListCall records one invocation of StreamReader.List.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type StreamReaderListCall struct {
	Ctx    context.Context
	Result iter.Seq2[streamreader.Value, error]
}

// --- Per-method configuration ---

// StreamReaderAddStub controls how the double answers Add and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type StreamReaderAddStub struct {
	*stub.MethodStub[StreamReaderAddCall]

	fn       func(context.Context, streamreader.Value) error
	fallback *StreamReaderAddReturn
}

// StreamReaderAddReturn holds the fixed answer configured through Returns.
type StreamReaderAddReturn struct {
	Err error
}

// Returns pins a fixed result for every call to Add. A Func
// override and an injected fault both take precedence over it.
func (s *StreamReaderAddStub) Returns(err error) *StreamReaderAddStub {
	s.fallback = &StreamReaderAddReturn{Err: err}
	return s
}

// Func supplies a body for Add, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *StreamReaderAddStub) Func(fn func(context.Context, streamreader.Value) error) *StreamReaderAddStub {
	s.fn = fn
	return s
}

// StreamReaderListStub controls how the double answers List and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type StreamReaderListStub struct {
	*stub.MethodStub[StreamReaderListCall]

	fn       func(context.Context) iter.Seq2[streamreader.Value, error]
	fallback *StreamReaderListReturn
}

// StreamReaderListReturn holds the fixed answer configured through Returns.
type StreamReaderListReturn struct {
	Result iter.Seq2[streamreader.Value, error]
}

// Returns pins a fixed result for every call to List. A Func
// override and an injected fault both take precedence over it.
func (s *StreamReaderListStub) Returns(result iter.Seq2[streamreader.Value, error]) *StreamReaderListStub {
	s.fallback = &StreamReaderListReturn{Result: result}
	return s
}

// Func supplies a body for List, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *StreamReaderListStub) Func(fn func(context.Context) iter.Seq2[streamreader.Value, error]) *StreamReaderListStub {
	s.fn = fn
	return s
}

// Yields makes List return a sequence over items.
//
// Building a range-over-func by hand is a closure with a break-handling
// branch in it, and a test that gets that branch wrong reports a bug in
// itself rather than in the subject.
func (s *StreamReaderListStub) Yields(items ...streamreader.Value) *StreamReaderListStub {
	s.fn = func(_ context.Context) iter.Seq2[streamreader.Value, error] {
		return func(yield func(streamreader.Value, error) bool) {
			for _, v := range items {
				if !yield(v, nil) {
					return
				}
			}
		}
	}
	return s
}

// YieldsError makes List return a sequence that produces items and
// then fails.
//
// Failing partway through is the case a consumer's loop most often gets
// wrong, and it cannot be reached by a sequence that only ever succeeds.
func (s *StreamReaderListStub) YieldsError(items []streamreader.Value, err error) *StreamReaderListStub {
	s.fn = func(_ context.Context) iter.Seq2[streamreader.Value, error] {
		return func(yield func(streamreader.Value, error) bool) {
			for _, v := range items {
				if !yield(v, nil) {
					return
				}
			}
			var zero streamreader.Value
			yield(zero, err)
		}
	}
	return s
}

// --- StreamReaderStub ---

// The double-runtime witness for the surface this double rides. A
// breaking change to the runtime renames it, and this file stops
// compiling with the skew named.
var _ = stub.CompatV1

// StreamReaderStubOption configures a [StreamReaderStub] at construction time.
type StreamReaderStubOption func(*StreamReaderStub)

// StreamReaderStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func StreamReaderStubStrict() StreamReaderStubOption {
	return func(s *StreamReaderStub) { s.strict = true }
}

// StreamReaderStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func StreamReaderStubDelegateTo(impl streamreader.StreamReader) StreamReaderStubOption {
	return func(s *StreamReaderStub) {
		s.OnAdd.Func(impl.Add)
		s.OnList.Func(impl.List)
	}
}

// StreamReaderStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func StreamReaderStubWithClock(clk clock.Clock) StreamReaderStubOption {
	return func(s *StreamReaderStub) { s.group.SetClock(clk) }
}

// StreamReaderStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func StreamReaderStubWithRandSource(src rand.Source) StreamReaderStubOption {
	return func(s *StreamReaderStub) { s.group.SetRandSource(src) }
}

// StreamReaderStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func StreamReaderStubBenchMode() StreamReaderStubOption {
	return func(s *StreamReaderStub) { s.group.BenchMode() }
}

// WithStreamReaderAdd sets Add's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithStreamReaderAdd(fn func(context.Context, streamreader.Value) error) StreamReaderStubOption {
	return func(s *StreamReaderStub) { s.OnAdd.Func(fn) }
}

// WithStreamReaderList sets List's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithStreamReaderList(fn func(context.Context) iter.Seq2[streamreader.Value, error]) StreamReaderStubOption {
	return func(s *StreamReaderStub) { s.OnList.Func(fn) }
}

// StreamReaderStub is a recording test double for StreamReader.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type StreamReaderStub struct {
	OnAdd  *StreamReaderAddStub
	OnList *StreamReaderListStub

	// group is every method stub above, viewed through the surface that
	// does not depend on a signature — one home for the settings and the
	// lifecycle that apply to the whole double.
	group  *stub.Group
	strict bool
}

// Compile-time proof that StreamReaderStub satisfies StreamReader.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ streamreader.StreamReader = (*StreamReaderStub)(nil)

// NewStreamReaderStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewStreamReaderStub(tb testing.TB, opts ...StreamReaderStubOption) *StreamReaderStub {
	s := &StreamReaderStub{
		OnAdd:  &StreamReaderAddStub{MethodStub: stub.NewMethodStub[StreamReaderAddCall](tb, "StreamReader.Add")},
		OnList: &StreamReaderListStub{MethodStub: stub.NewMethodStub[StreamReaderListCall](tb, "StreamReader.List")},
	}
	s.group = stub.NewGroup(
		s.OnAdd.MethodStub,
		s.OnList.MethodStub,
	)
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.group.StrictAll()
	}
	s.group.Bind(tb)
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *StreamReaderStub) ResetCalls() {
	s.group.Reset()
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *StreamReaderAddStub) invoke(ctx context.Context, v streamreader.Value) func() StreamReaderAddReturn {
	if s.fn == nil {
		return nil
	}
	return func() StreamReaderAddReturn {
		r0 := s.fn(ctx, v)
		return StreamReaderAddReturn{Err: r0}
	}
}

// Add records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *StreamReaderStub) Add(ctx context.Context, v streamreader.Value) error {
	call := StreamReaderAddCall{Ctx: ctx, V: v}
	r := stub.Answer(s.OnAdd.MethodStub, &call, stub.Arms[StreamReaderAddCall, StreamReaderAddReturn]{
		Invoke:   s.OnAdd.invoke(ctx, v),
		Fallback: s.OnAdd.fallback,
		Fault:    func(err error) StreamReaderAddReturn { return StreamReaderAddReturn{Err: err} },
		Stamp: func(c *StreamReaderAddCall, r StreamReaderAddReturn) {
			c.Err = r.Err
		},
	})
	return r.Err
}

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *StreamReaderListStub) invoke(ctx context.Context) func() StreamReaderListReturn {
	if s.fn == nil {
		return nil
	}
	return func() StreamReaderListReturn {
		r0 := s.fn(ctx)
		return StreamReaderListReturn{Result: r0}
	}
}

// List records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *StreamReaderStub) List(ctx context.Context) iter.Seq2[streamreader.Value, error] {
	call := StreamReaderListCall{Ctx: ctx}
	r := stub.Answer(s.OnList.MethodStub, &call, stub.Arms[StreamReaderListCall, StreamReaderListReturn]{
		Invoke:   s.OnList.invoke(ctx),
		Fallback: s.OnList.fallback,
		Stamp: func(c *StreamReaderListCall, r StreamReaderListReturn) {
			c.Result = r.Result
		},
	})
	return r.Result
}

// testkit: end of generated content.
// testkit:provenance 6433432dfa0482ba6d198a370fa993a3c95ce1a719f89c27c592ce9d7717b1bb
