// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/lookup/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package lookuptest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/lookup"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/lookup/lookuptest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// lookupStubInspectSubject binds Inspect into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func lookupStubInspectSubject(tb testing.TB) stub.Subject[lookuptest.LookupInspectCall, lookuptest.LookupInspectReturn] {
	tb.Helper()
	s := lookuptest.NewLookupStub(tb)
	return stub.Subject[lookuptest.LookupInspectCall, lookuptest.LookupInspectReturn]{
		Stub: s.OnInspect.MethodStub,
		Call: func() {
			var a0 string = "test-key"
			_, _, _ = s.Inspect(a0)
		},
		Result: func() lookuptest.LookupInspectReturn {
			var a0 string = "test-key"
			got0, got1, got2 := s.Inspect(a0)
			return lookuptest.LookupInspectReturn{Result0: got0, Result1: got1, Result2: got2}
		},
		Override: func(mark func()) {
			s.OnInspect.Func(func(_ string) (lookup.Value, lookup.Meta, bool) {
				mark()
				var z0 lookup.Value
				var z1 lookup.Meta
				var z2 bool
				return z0, z1, z2
			})
		},
	}
}

// TestLookupStubInspect pins how Inspect answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestLookupStubInspect(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Inspect", lookupStubInspectSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := lookuptest.NewLookupStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 lookup.Value
		var want1 lookup.Meta
		var want2 bool = true
		s.OnInspect.Returns(want0, want1, want2)
		var a0 string = "test-key"
		_, _, got2 := s.Inspect(a0)
		testkit.Equal(t, got2, want2, "Inspect must answer with what Returns pinned")
		// Blanked above rather than dropped in silence. No literal exists
		// for result 0 (go.thesmos.sh/testkit/conformance/corpus/iface/detector/lookup.Value), result 1 (go.thesmos.sh/testkit/conformance/corpus/iface/detector/lookup.Meta), so the only value to pin there
		// is the zero — and a double that ignored Returns answers the zero
		// too, which makes the comparison pass either way.
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := lookuptest.NewLookupStub(t)
		var a0 string = "test-key"
		_, _, _ = s.Inspect(a0)
		got := s.OnInspect.AssertCalledOnce(t, "Inspect must record the call")
		testkit.Equal(t, got.Key, a0, "the recorded call carries Key")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := lookuptest.NewLookupStub(t)
		var seen []lookuptest.LookupInspectCall
		s.OnInspect.OnRecord(func(c lookuptest.LookupInspectCall) { seen = append(seen, c) })
		var a0 string = "test-key"
		_, _, _ = s.Inspect(a0)
		_, _, _ = s.Inspect(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Inspect call")
	})

	t.Run("wires WithLookupInspect at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := lookuptest.NewLookupStub(t, lookuptest.WithLookupInspect(func(_ string) (lookup.Value, lookup.Meta, bool) {
			called = true
			var z0 lookup.Value
			var z1 lookup.Meta
			var z2 bool
			return z0, z1, z2
		}))
		var a0 string = "test-key"
		_, _, _ = s.Inspect(a0)
		testkit.True(t, called, "WithLookupInspect must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		//
		// Pinned to a literal for the reason the Returns check is: a reset
		// that lost the configuration answers the zero, and so does one that
		// kept a configured zero.
		s := lookuptest.NewLookupStub(t)
		var want0 lookup.Value
		var want1 lookup.Meta
		var want2 bool = true
		s.OnInspect.Returns(want0, want1, want2)
		var a0 string = "test-key"
		_, _, _ = s.Inspect(a0)
		s.ResetCalls()
		_, _, got2 := s.Inspect(a0)
		testkit.Equal(t, got2, want2, "a reset must keep what Returns pinned")
	})
}

// lookupStubDouble describes how to build a LookupStub under each
// option whose effect is the same whatever a method's signature.
//
// Inspect stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func lookupStubDouble() stub.Double[lookuptest.LookupInspectCall] {
	instance := func(s *lookuptest.LookupStub) stub.Instance[lookuptest.LookupInspectCall] {
		return stub.Instance[lookuptest.LookupInspectCall]{
			Stub: s.OnInspect.MethodStub,
			Call: func() {
				var a0 string = "test-key"
				_, _, _ = s.Inspect(a0)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[lookuptest.LookupInspectCall]{
		New: func(tb testing.TB) stub.Instance[lookuptest.LookupInspectCall] {
			return instance(lookuptest.NewLookupStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[lookuptest.LookupInspectCall] {
			return instance(lookuptest.NewLookupStub(tb, lookuptest.LookupStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[lookuptest.LookupInspectCall] {
			return instance(lookuptest.NewLookupStub(tb, lookuptest.LookupStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[lookuptest.LookupInspectCall] {
			return instance(lookuptest.NewLookupStub(tb, lookuptest.LookupStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[lookuptest.LookupInspectCall] {
			return instance(lookuptest.NewLookupStub(tb, lookuptest.LookupStubStrict()))
		},
	}
}

// TestLookupStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestLookupStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, lookupStubDouble())
}

// TestLookupStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestLookupStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := lookuptest.NewLookupStub(t)
	s := lookuptest.NewLookupStub(t, lookuptest.LookupStubDelegateTo(inner))

	t.Run("forwards Inspect to the wrapped implementation", func(t *testing.T) {
		var a0 string = "test-key"
		_, _, _ = s.Inspect(a0)
		inner.OnInspect.AssertCalledOnce(t, "Inspect must reach the wrapped implementation")
	})
}

// testkit: end of generated content.
// testkit:provenance 3e3278d80879a6809dd071306831b807ea829310a97fdda293ab266af0e7f00b
