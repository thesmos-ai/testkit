// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/readernoerror/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package readernoerrortest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/readernoerror"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/readernoerror/readernoerrortest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// readerNoErrorStubLookupSubject binds Lookup into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func readerNoErrorStubLookupSubject(tb testing.TB) stub.Subject[readernoerrortest.ReaderNoErrorLookupCall, readernoerrortest.ReaderNoErrorLookupReturn] {
	tb.Helper()
	s := readernoerrortest.NewReaderNoErrorStub(tb)
	return stub.Subject[readernoerrortest.ReaderNoErrorLookupCall, readernoerrortest.ReaderNoErrorLookupReturn]{
		Stub: s.OnLookup.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string = "test-key"
			_ = s.Lookup(a0, a1)
		},
		Result: func() readernoerrortest.ReaderNoErrorLookupReturn {
			var a0 context.Context
			var a1 string = "test-key"
			got0 := s.Lookup(a0, a1)
			return readernoerrortest.ReaderNoErrorLookupReturn{Result: got0}
		},
		Override: func(mark func()) {
			s.OnLookup.Func(func(_ context.Context, _ string) readernoerror.Value {
				mark()
				var z0 readernoerror.Value
				return z0
			})
		},
	}
}

// TestReaderNoErrorStubLookup pins how Lookup answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestReaderNoErrorStubLookup(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Lookup", readerNoErrorStubLookupSubject)

	// No check that Lookup answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := readernoerrortest.NewReaderNoErrorStub(t)
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Lookup(a0, a1)
		got := s.OnLookup.AssertCalledOnce(t, "Lookup must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Key, a1, "the recorded call carries Key")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := readernoerrortest.NewReaderNoErrorStub(t)
		var seen []readernoerrortest.ReaderNoErrorLookupCall
		s.OnLookup.OnRecord(func(c readernoerrortest.ReaderNoErrorLookupCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Lookup(a0, a1)
		_ = s.Lookup(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Lookup call")
	})

	t.Run("wires WithReaderNoErrorLookup at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := readernoerrortest.NewReaderNoErrorStub(t, readernoerrortest.WithReaderNoErrorLookup(func(_ context.Context, _ string) readernoerror.Value {
			called = true
			var z0 readernoerror.Value
			return z0
		}))
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Lookup(a0, a1)
		testkit.True(t, called, "WithReaderNoErrorLookup must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Lookup admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := readernoerrortest.NewReaderNoErrorStub(t)
		var want0 readernoerror.Value
		s.OnLookup.Returns(want0)
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Lookup(a0, a1)
		s.ResetCalls()
		_ = s.Lookup(a0, a1)
		s.OnLookup.AssertCalledOnce(t, "Lookup must record the call that followed the reset")
	})
}

// readerNoErrorStubDouble describes how to build a ReaderNoErrorStub under each
// option whose effect is the same whatever a method's signature.
//
// Lookup stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func readerNoErrorStubDouble() stub.Double[readernoerrortest.ReaderNoErrorLookupCall] {
	instance := func(s *readernoerrortest.ReaderNoErrorStub) stub.Instance[readernoerrortest.ReaderNoErrorLookupCall] {
		return stub.Instance[readernoerrortest.ReaderNoErrorLookupCall]{
			Stub: s.OnLookup.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 string = "test-key"
				_ = s.Lookup(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[readernoerrortest.ReaderNoErrorLookupCall]{
		New: func(tb testing.TB) stub.Instance[readernoerrortest.ReaderNoErrorLookupCall] {
			return instance(readernoerrortest.NewReaderNoErrorStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[readernoerrortest.ReaderNoErrorLookupCall] {
			return instance(readernoerrortest.NewReaderNoErrorStub(tb, readernoerrortest.ReaderNoErrorStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[readernoerrortest.ReaderNoErrorLookupCall] {
			return instance(readernoerrortest.NewReaderNoErrorStub(tb, readernoerrortest.ReaderNoErrorStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[readernoerrortest.ReaderNoErrorLookupCall] {
			return instance(readernoerrortest.NewReaderNoErrorStub(tb, readernoerrortest.ReaderNoErrorStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[readernoerrortest.ReaderNoErrorLookupCall] {
			return instance(readernoerrortest.NewReaderNoErrorStub(tb, readernoerrortest.ReaderNoErrorStubStrict()))
		},
	}
}

// TestReaderNoErrorStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestReaderNoErrorStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, readerNoErrorStubDouble())
}

// TestReaderNoErrorStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestReaderNoErrorStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := readernoerrortest.NewReaderNoErrorStub(t)
	s := readernoerrortest.NewReaderNoErrorStub(t, readernoerrortest.ReaderNoErrorStubDelegateTo(inner))

	t.Run("forwards Lookup to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Lookup(a0, a1)
		inner.OnLookup.AssertCalledOnce(t, "Lookup must reach the wrapped implementation")
	})
}

// testkit: end of generated content.
// testkit:provenance 108015e48a2d0e853031d30262b3fb37f1420522a36f586a6da957d3ddc3d5db
