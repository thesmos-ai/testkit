// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/readernoerror/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package readernoerrortest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/readernoerror"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// ReaderNoErrorLookupCall records one invocation of ReaderNoError.Lookup.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type ReaderNoErrorLookupCall struct {
	Ctx    context.Context
	Key    string
	Result readernoerror.Value
}

// --- Per-method configuration ---

// ReaderNoErrorLookupStub controls how the double answers Lookup and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type ReaderNoErrorLookupStub struct {
	*stub.MethodStub[ReaderNoErrorLookupCall]

	fn       func(context.Context, string) readernoerror.Value
	fallback *ReaderNoErrorLookupReturn
}

// ReaderNoErrorLookupReturn holds the fixed answer configured through Returns.
type ReaderNoErrorLookupReturn struct {
	Result readernoerror.Value
}

// Returns pins a fixed result for every call to Lookup. A Func
// override and an injected fault both take precedence over it.
func (s *ReaderNoErrorLookupStub) Returns(result readernoerror.Value) *ReaderNoErrorLookupStub {
	s.fallback = &ReaderNoErrorLookupReturn{Result: result}
	return s
}

// Func supplies a body for Lookup, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *ReaderNoErrorLookupStub) Func(fn func(context.Context, string) readernoerror.Value) *ReaderNoErrorLookupStub {
	s.fn = fn
	return s
}

// --- ReaderNoErrorStub ---

// The double-runtime witness for the surface this double rides. A
// breaking change to the runtime renames it, and this file stops
// compiling with the skew named.
var _ = stub.CompatV1

// ReaderNoErrorStubOption configures a [ReaderNoErrorStub] at construction time.
type ReaderNoErrorStubOption func(*ReaderNoErrorStub)

// ReaderNoErrorStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func ReaderNoErrorStubStrict() ReaderNoErrorStubOption {
	return func(s *ReaderNoErrorStub) { s.strict = true }
}

// ReaderNoErrorStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func ReaderNoErrorStubDelegateTo(impl readernoerror.ReaderNoError) ReaderNoErrorStubOption {
	return func(s *ReaderNoErrorStub) {
		s.OnLookup.Func(impl.Lookup)
	}
}

// ReaderNoErrorStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func ReaderNoErrorStubWithClock(clk clock.Clock) ReaderNoErrorStubOption {
	return func(s *ReaderNoErrorStub) { s.group.SetClock(clk) }
}

// ReaderNoErrorStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func ReaderNoErrorStubWithRandSource(src rand.Source) ReaderNoErrorStubOption {
	return func(s *ReaderNoErrorStub) { s.group.SetRandSource(src) }
}

// ReaderNoErrorStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func ReaderNoErrorStubBenchMode() ReaderNoErrorStubOption {
	return func(s *ReaderNoErrorStub) { s.group.BenchMode() }
}

// WithReaderNoErrorLookup sets Lookup's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithReaderNoErrorLookup(fn func(context.Context, string) readernoerror.Value) ReaderNoErrorStubOption {
	return func(s *ReaderNoErrorStub) { s.OnLookup.Func(fn) }
}

// ReaderNoErrorStub is a recording test double for ReaderNoError.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type ReaderNoErrorStub struct {
	OnLookup *ReaderNoErrorLookupStub

	// group is every method stub above, viewed through the surface that
	// does not depend on a signature — one home for the settings and the
	// lifecycle that apply to the whole double.
	group  *stub.Group
	strict bool
}

// Compile-time proof that ReaderNoErrorStub satisfies ReaderNoError.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ readernoerror.ReaderNoError = (*ReaderNoErrorStub)(nil)

// NewReaderNoErrorStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewReaderNoErrorStub(tb testing.TB, opts ...ReaderNoErrorStubOption) *ReaderNoErrorStub {
	s := &ReaderNoErrorStub{
		OnLookup: &ReaderNoErrorLookupStub{MethodStub: stub.NewMethodStub[ReaderNoErrorLookupCall](tb, "ReaderNoError.Lookup")},
	}
	s.group = stub.NewGroup(
		s.OnLookup.MethodStub,
	)
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.group.StrictAll()
	}
	s.group.Bind(tb)
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *ReaderNoErrorStub) ResetCalls() {
	s.group.Reset()
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *ReaderNoErrorLookupStub) invoke(ctx context.Context, key string) func() ReaderNoErrorLookupReturn {
	if s.fn == nil {
		return nil
	}
	return func() ReaderNoErrorLookupReturn {
		r0 := s.fn(ctx, key)
		return ReaderNoErrorLookupReturn{Result: r0}
	}
}

// Lookup records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *ReaderNoErrorStub) Lookup(ctx context.Context, key string) readernoerror.Value {
	call := ReaderNoErrorLookupCall{Ctx: ctx, Key: key}
	r := stub.Answer(s.OnLookup.MethodStub, &call, stub.Arms[ReaderNoErrorLookupCall, ReaderNoErrorLookupReturn]{
		Invoke:   s.OnLookup.invoke(ctx, key),
		Fallback: s.OnLookup.fallback,
		Stamp: func(c *ReaderNoErrorLookupCall, r ReaderNoErrorLookupReturn) {
			c.Result = r.Result
		},
	})
	return r.Result
}

// testkit: end of generated content.
// testkit:provenance 9621ca673f1fea195d507e93abf62debec2431a452ea628d88ffc4c423486bcb
