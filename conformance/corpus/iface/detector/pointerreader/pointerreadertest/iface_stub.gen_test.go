// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/pointerreader/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package pointerreadertest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/pointerreader"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/pointerreader/pointerreadertest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// pointerReaderStubFindSubject binds Find into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func pointerReaderStubFindSubject(tb testing.TB) stub.Subject[pointerreadertest.PointerReaderFindCall, pointerreadertest.PointerReaderFindReturn] {
	tb.Helper()
	s := pointerreadertest.NewPointerReaderStub(tb)
	return stub.Subject[pointerreadertest.PointerReaderFindCall, pointerreadertest.PointerReaderFindReturn]{
		Stub: s.OnFind.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string = "test-key"
			_ = s.Find(a0, a1)
		},
		Result: func() pointerreadertest.PointerReaderFindReturn {
			var a0 context.Context
			var a1 string = "test-key"
			got0 := s.Find(a0, a1)
			return pointerreadertest.PointerReaderFindReturn{Result: got0}
		},
		Override: func(mark func()) {
			s.OnFind.Func(func(_ context.Context, _ string) *pointerreader.Value {
				mark()
				var z0 *pointerreader.Value
				return z0
			})
		},
	}
}

// TestPointerReaderStubFind pins how Find answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestPointerReaderStubFind(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Find", pointerReaderStubFindSubject)

	// No check that Find answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := pointerreadertest.NewPointerReaderStub(t)
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Find(a0, a1)
		got := s.OnFind.AssertCalledOnce(t, "Find must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Key, a1, "the recorded call carries Key")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := pointerreadertest.NewPointerReaderStub(t)
		var seen []pointerreadertest.PointerReaderFindCall
		s.OnFind.OnRecord(func(c pointerreadertest.PointerReaderFindCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Find(a0, a1)
		_ = s.Find(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Find call")
	})

	t.Run("wires WithPointerReaderFind at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := pointerreadertest.NewPointerReaderStub(t, pointerreadertest.WithPointerReaderFind(func(_ context.Context, _ string) *pointerreader.Value {
			called = true
			var z0 *pointerreader.Value
			return z0
		}))
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Find(a0, a1)
		testkit.True(t, called, "WithPointerReaderFind must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Find admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := pointerreadertest.NewPointerReaderStub(t)
		var want0 *pointerreader.Value
		s.OnFind.Returns(want0)
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Find(a0, a1)
		s.ResetCalls()
		_ = s.Find(a0, a1)
		s.OnFind.AssertCalledOnce(t, "Find must record the call that followed the reset")
	})
}

// pointerReaderStubDouble describes how to build a PointerReaderStub under each
// option whose effect is the same whatever a method's signature.
//
// Find stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func pointerReaderStubDouble() stub.Double[pointerreadertest.PointerReaderFindCall] {
	instance := func(s *pointerreadertest.PointerReaderStub) stub.Instance[pointerreadertest.PointerReaderFindCall] {
		return stub.Instance[pointerreadertest.PointerReaderFindCall]{
			Stub: s.OnFind.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 string = "test-key"
				_ = s.Find(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[pointerreadertest.PointerReaderFindCall]{
		New: func(tb testing.TB) stub.Instance[pointerreadertest.PointerReaderFindCall] {
			return instance(pointerreadertest.NewPointerReaderStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[pointerreadertest.PointerReaderFindCall] {
			return instance(pointerreadertest.NewPointerReaderStub(tb, pointerreadertest.PointerReaderStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[pointerreadertest.PointerReaderFindCall] {
			return instance(pointerreadertest.NewPointerReaderStub(tb, pointerreadertest.PointerReaderStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[pointerreadertest.PointerReaderFindCall] {
			return instance(pointerreadertest.NewPointerReaderStub(tb, pointerreadertest.PointerReaderStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[pointerreadertest.PointerReaderFindCall] {
			return instance(pointerreadertest.NewPointerReaderStub(tb, pointerreadertest.PointerReaderStubStrict()))
		},
	}
}

// TestPointerReaderStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestPointerReaderStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, pointerReaderStubDouble())
}

// TestPointerReaderStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestPointerReaderStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := pointerreadertest.NewPointerReaderStub(t)
	s := pointerreadertest.NewPointerReaderStub(t, pointerreadertest.PointerReaderStubDelegateTo(inner))

	t.Run("forwards Find to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string = "test-key"
		_ = s.Find(a0, a1)
		inner.OnFind.AssertCalledOnce(t, "Find must reach the wrapped implementation")
	})
}

// testkit: end of generated content.
// testkit:provenance 45ff228886b7909e91c3774d0ca4d421eb48112d00cac4a4584712018df5dfa6
