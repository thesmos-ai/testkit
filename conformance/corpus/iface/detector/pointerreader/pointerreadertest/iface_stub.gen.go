// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/pointerreader/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package pointerreadertest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/pointerreader"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// PointerReaderFindCall records one invocation of PointerReader.Find.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type PointerReaderFindCall struct {
	Ctx    context.Context
	Key    string
	Result *pointerreader.Value
}

// --- Per-method configuration ---

// PointerReaderFindStub controls how the double answers Find and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type PointerReaderFindStub struct {
	*stub.MethodStub[PointerReaderFindCall]

	fn       func(context.Context, string) *pointerreader.Value
	fallback *PointerReaderFindReturn
}

// PointerReaderFindReturn holds the fixed answer configured through Returns.
type PointerReaderFindReturn struct {
	Result *pointerreader.Value
}

// Returns pins a fixed result for every call to Find. A Func
// override and an injected fault both take precedence over it.
func (s *PointerReaderFindStub) Returns(result *pointerreader.Value) *PointerReaderFindStub {
	s.fallback = &PointerReaderFindReturn{Result: result}
	return s
}

// Func supplies a body for Find, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *PointerReaderFindStub) Func(fn func(context.Context, string) *pointerreader.Value) *PointerReaderFindStub {
	s.fn = fn
	return s
}

// --- PointerReaderStub ---

// The double-runtime witness for the surface this double rides. A
// breaking change to the runtime renames it, and this file stops
// compiling with the skew named.
var _ = stub.CompatV1

// PointerReaderStubOption configures a [PointerReaderStub] at construction time.
type PointerReaderStubOption func(*PointerReaderStub)

// PointerReaderStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func PointerReaderStubStrict() PointerReaderStubOption {
	return func(s *PointerReaderStub) { s.strict = true }
}

// PointerReaderStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func PointerReaderStubDelegateTo(impl pointerreader.PointerReader) PointerReaderStubOption {
	return func(s *PointerReaderStub) {
		s.OnFind.Func(impl.Find)
	}
}

// PointerReaderStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func PointerReaderStubWithClock(clk clock.Clock) PointerReaderStubOption {
	return func(s *PointerReaderStub) { s.group.SetClock(clk) }
}

// PointerReaderStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func PointerReaderStubWithRandSource(src rand.Source) PointerReaderStubOption {
	return func(s *PointerReaderStub) { s.group.SetRandSource(src) }
}

// PointerReaderStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func PointerReaderStubBenchMode() PointerReaderStubOption {
	return func(s *PointerReaderStub) { s.group.BenchMode() }
}

// WithPointerReaderFind sets Find's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithPointerReaderFind(fn func(context.Context, string) *pointerreader.Value) PointerReaderStubOption {
	return func(s *PointerReaderStub) { s.OnFind.Func(fn) }
}

// PointerReaderStub is a recording test double for PointerReader.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type PointerReaderStub struct {
	OnFind *PointerReaderFindStub

	// group is every method stub above, viewed through the surface that
	// does not depend on a signature — one home for the settings and the
	// lifecycle that apply to the whole double.
	group  *stub.Group
	strict bool
}

// Compile-time proof that PointerReaderStub satisfies PointerReader.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ pointerreader.PointerReader = (*PointerReaderStub)(nil)

// NewPointerReaderStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewPointerReaderStub(tb testing.TB, opts ...PointerReaderStubOption) *PointerReaderStub {
	s := &PointerReaderStub{
		OnFind: &PointerReaderFindStub{MethodStub: stub.NewMethodStub[PointerReaderFindCall](tb, "PointerReader.Find")},
	}
	s.group = stub.NewGroup(
		s.OnFind.MethodStub,
	)
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.group.StrictAll()
	}
	s.group.Bind(tb)
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *PointerReaderStub) ResetCalls() {
	s.group.Reset()
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *PointerReaderFindStub) invoke(ctx context.Context, key string) func() PointerReaderFindReturn {
	if s.fn == nil {
		return nil
	}
	return func() PointerReaderFindReturn {
		r0 := s.fn(ctx, key)
		return PointerReaderFindReturn{Result: r0}
	}
}

// Find records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *PointerReaderStub) Find(ctx context.Context, key string) *pointerreader.Value {
	call := PointerReaderFindCall{Ctx: ctx, Key: key}
	r := stub.Answer(s.OnFind.MethodStub, &call, stub.Arms[PointerReaderFindCall, PointerReaderFindReturn]{
		Invoke:   s.OnFind.invoke(ctx, key),
		Fallback: s.OnFind.fallback,
		Stamp: func(c *PointerReaderFindCall, r PointerReaderFindReturn) {
			c.Result = r.Result
		},
	})
	return r.Result
}

// testkit: end of generated content.
// testkit:provenance 589a3811c35ae79415de2c0cab48efd3ed3f5c1fe44e454633f25288582ac291
