// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/pure/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package puretest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/pure/puretest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// pureStubDescribeSubject binds Describe into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func pureStubDescribeSubject(tb testing.TB) stub.Subject[puretest.PureDescribeCall, puretest.PureDescribeReturn] {
	tb.Helper()
	s := puretest.NewPureStub(tb)
	return stub.Subject[puretest.PureDescribeCall, puretest.PureDescribeReturn]{
		Stub: s.OnDescribe.MethodStub,
		Call: func() {
			_ = s.Describe()
		},
		Result: func() puretest.PureDescribeReturn {
			got0 := s.Describe()
			return puretest.PureDescribeReturn{Result: got0}
		},
		Override: func(mark func()) {
			s.OnDescribe.Func(func() string {
				mark()
				var z0 string
				return z0
			})
		},
	}
}

// TestPureStubDescribe pins how Describe answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestPureStubDescribe(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Describe", pureStubDescribeSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := puretest.NewPureStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 string = "test-describe"
		s.OnDescribe.Returns(want0)
		got0 := s.Describe()
		testkit.Equal(t, got0, want0, "Describe must answer with what Returns pinned")
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := puretest.NewPureStub(t)
		_ = s.Describe()
		s.OnDescribe.AssertCalledOnce(t, "Describe must record the call")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := puretest.NewPureStub(t)
		var seen []puretest.PureDescribeCall
		s.OnDescribe.OnRecord(func(c puretest.PureDescribeCall) { seen = append(seen, c) })
		_ = s.Describe()
		_ = s.Describe()
		testkit.Len(t, seen, 2, "OnRecord must fire once per Describe call")
	})

	t.Run("wires WithPureDescribe at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := puretest.NewPureStub(t, puretest.WithPureDescribe(func() string {
			called = true
			var z0 string
			return z0
		}))
		_ = s.Describe()
		testkit.True(t, called, "WithPureDescribe must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		//
		// Pinned to a literal for the reason the Returns check is: a reset
		// that lost the configuration answers the zero, and so does one that
		// kept a configured zero.
		s := puretest.NewPureStub(t)
		var want0 string = "test-describe"
		s.OnDescribe.Returns(want0)
		_ = s.Describe()
		s.ResetCalls()
		got0 := s.Describe()
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
	})
}

// pureStubDouble describes how to build a PureStub under each
// option whose effect is the same whatever a method's signature.
//
// Describe stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func pureStubDouble() stub.Double[puretest.PureDescribeCall] {
	instance := func(s *puretest.PureStub) stub.Instance[puretest.PureDescribeCall] {
		return stub.Instance[puretest.PureDescribeCall]{
			Stub: s.OnDescribe.MethodStub,
			Call: func() {
				_ = s.Describe()
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[puretest.PureDescribeCall]{
		New: func(tb testing.TB) stub.Instance[puretest.PureDescribeCall] {
			return instance(puretest.NewPureStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[puretest.PureDescribeCall] {
			return instance(puretest.NewPureStub(tb, puretest.PureStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[puretest.PureDescribeCall] {
			return instance(puretest.NewPureStub(tb, puretest.PureStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[puretest.PureDescribeCall] {
			return instance(puretest.NewPureStub(tb, puretest.PureStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[puretest.PureDescribeCall] {
			return instance(puretest.NewPureStub(tb, puretest.PureStubStrict()))
		},
	}
}

// TestPureStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestPureStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, pureStubDouble())
}

// TestPureStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestPureStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := puretest.NewPureStub(t)
	s := puretest.NewPureStub(t, puretest.PureStubDelegateTo(inner))

	t.Run("forwards Describe to the wrapped implementation", func(t *testing.T) {
		_ = s.Describe()
		inner.OnDescribe.AssertCalledOnce(t, "Describe must reach the wrapped implementation")
	})
}

// testkit: end of generated content.
// testkit:provenance 8c56a45b337d2448d222316628f2a8b4e29849825301f2177fb77cccce05d98f
