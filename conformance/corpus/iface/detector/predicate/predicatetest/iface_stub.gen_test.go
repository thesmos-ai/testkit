// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/predicate/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package predicatetest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/predicate/predicatetest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// predicateStubIsEmptySubject binds IsEmpty into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func predicateStubIsEmptySubject(tb testing.TB) stub.Subject[predicatetest.PredicateIsEmptyCall, predicatetest.PredicateIsEmptyReturn] {
	tb.Helper()
	s := predicatetest.NewPredicateStub(tb)
	return stub.Subject[predicatetest.PredicateIsEmptyCall, predicatetest.PredicateIsEmptyReturn]{
		Stub: s.OnIsEmpty.MethodStub,
		Call: func() {
			_ = s.IsEmpty()
		},
		Result: func() predicatetest.PredicateIsEmptyReturn {
			got0 := s.IsEmpty()
			return predicatetest.PredicateIsEmptyReturn{Result: got0}
		},
		Override: func(mark func()) {
			s.OnIsEmpty.Func(func() bool {
				mark()
				var z0 bool
				return z0
			})
		},
	}
}

// TestPredicateStubIsEmpty pins how IsEmpty answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestPredicateStubIsEmpty(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "IsEmpty", predicateStubIsEmptySubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := predicatetest.NewPredicateStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 bool = true
		s.OnIsEmpty.Returns(want0)
		got0 := s.IsEmpty()
		testkit.Equal(t, got0, want0, "IsEmpty must answer with what Returns pinned")
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := predicatetest.NewPredicateStub(t)
		_ = s.IsEmpty()
		s.OnIsEmpty.AssertCalledOnce(t, "IsEmpty must record the call")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := predicatetest.NewPredicateStub(t)
		var seen []predicatetest.PredicateIsEmptyCall
		s.OnIsEmpty.OnRecord(func(c predicatetest.PredicateIsEmptyCall) { seen = append(seen, c) })
		_ = s.IsEmpty()
		_ = s.IsEmpty()
		testkit.Len(t, seen, 2, "OnRecord must fire once per IsEmpty call")
	})

	t.Run("wires WithPredicateIsEmpty at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := predicatetest.NewPredicateStub(t, predicatetest.WithPredicateIsEmpty(func() bool {
			called = true
			var z0 bool
			return z0
		}))
		_ = s.IsEmpty()
		testkit.True(t, called, "WithPredicateIsEmpty must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		//
		// Pinned to a literal for the reason the Returns check is: a reset
		// that lost the configuration answers the zero, and so does one that
		// kept a configured zero.
		s := predicatetest.NewPredicateStub(t)
		var want0 bool = true
		s.OnIsEmpty.Returns(want0)
		_ = s.IsEmpty()
		s.ResetCalls()
		got0 := s.IsEmpty()
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
	})
}

// predicateStubDouble describes how to build a PredicateStub under each
// option whose effect is the same whatever a method's signature.
//
// IsEmpty stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func predicateStubDouble() stub.Double[predicatetest.PredicateIsEmptyCall] {
	instance := func(s *predicatetest.PredicateStub) stub.Instance[predicatetest.PredicateIsEmptyCall] {
		return stub.Instance[predicatetest.PredicateIsEmptyCall]{
			Stub: s.OnIsEmpty.MethodStub,
			Call: func() {
				_ = s.IsEmpty()
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[predicatetest.PredicateIsEmptyCall]{
		New: func(tb testing.TB) stub.Instance[predicatetest.PredicateIsEmptyCall] {
			return instance(predicatetest.NewPredicateStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[predicatetest.PredicateIsEmptyCall] {
			return instance(predicatetest.NewPredicateStub(tb, predicatetest.PredicateStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[predicatetest.PredicateIsEmptyCall] {
			return instance(predicatetest.NewPredicateStub(tb, predicatetest.PredicateStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[predicatetest.PredicateIsEmptyCall] {
			return instance(predicatetest.NewPredicateStub(tb, predicatetest.PredicateStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[predicatetest.PredicateIsEmptyCall] {
			return instance(predicatetest.NewPredicateStub(tb, predicatetest.PredicateStubStrict()))
		},
	}
}

// TestPredicateStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestPredicateStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, predicateStubDouble())
}

// TestPredicateStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestPredicateStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := predicatetest.NewPredicateStub(t)
	s := predicatetest.NewPredicateStub(t, predicatetest.PredicateStubDelegateTo(inner))

	t.Run("forwards IsEmpty to the wrapped implementation", func(t *testing.T) {
		_ = s.IsEmpty()
		inner.OnIsEmpty.AssertCalledOnce(t, "IsEmpty must reach the wrapped implementation")
	})
}

// testkit: end of generated content.
// testkit:provenance 8b556344ec96ead5f5fab0e46d39aa3ee45330cf6743f1404726a79975a02467
