// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/multireader/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package multireadertest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/multireader"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// MultiReaderGetWithMetaCall records one invocation of MultiReader.GetWithMeta.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type MultiReaderGetWithMetaCall struct {
	Ctx     context.Context
	Key     string
	Result0 multireader.Value
	Result1 multireader.Meta
	Err     error
}

// --- Per-method configuration ---

// MultiReaderGetWithMetaStub controls how the double answers GetWithMeta and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type MultiReaderGetWithMetaStub struct {
	*stub.MethodStub[MultiReaderGetWithMetaCall]

	fn       func(context.Context, string) (multireader.Value, multireader.Meta, error)
	fallback *MultiReaderGetWithMetaReturn
}

// MultiReaderGetWithMetaReturn holds the fixed answer configured through Returns.
type MultiReaderGetWithMetaReturn struct {
	Result0 multireader.Value
	Result1 multireader.Meta
	Err     error
}

// Returns pins a fixed result for every call to GetWithMeta. A Func
// override and an injected fault both take precedence over it.
func (s *MultiReaderGetWithMetaStub) Returns(result0 multireader.Value, result1 multireader.Meta, err error) *MultiReaderGetWithMetaStub {
	s.fallback = &MultiReaderGetWithMetaReturn{Result0: result0, Result1: result1, Err: err}
	return s
}

// Func supplies a body for GetWithMeta, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *MultiReaderGetWithMetaStub) Func(fn func(context.Context, string) (multireader.Value, multireader.Meta, error)) *MultiReaderGetWithMetaStub {
	s.fn = fn
	return s
}

// --- MultiReaderStub ---

// The double-runtime witness for the surface this double rides. A
// breaking change to the runtime renames it, and this file stops
// compiling with the skew named.
var _ = stub.CompatV1

// MultiReaderStubOption configures a [MultiReaderStub] at construction time.
type MultiReaderStubOption func(*MultiReaderStub)

// MultiReaderStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func MultiReaderStubStrict() MultiReaderStubOption {
	return func(s *MultiReaderStub) { s.strict = true }
}

// MultiReaderStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func MultiReaderStubDelegateTo(impl multireader.MultiReader) MultiReaderStubOption {
	return func(s *MultiReaderStub) {
		s.OnGetWithMeta.Func(impl.GetWithMeta)
	}
}

// MultiReaderStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func MultiReaderStubWithClock(clk clock.Clock) MultiReaderStubOption {
	return func(s *MultiReaderStub) { s.group.SetClock(clk) }
}

// MultiReaderStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func MultiReaderStubWithRandSource(src rand.Source) MultiReaderStubOption {
	return func(s *MultiReaderStub) { s.group.SetRandSource(src) }
}

// MultiReaderStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func MultiReaderStubBenchMode() MultiReaderStubOption {
	return func(s *MultiReaderStub) { s.group.BenchMode() }
}

// WithMultiReaderGetWithMeta sets GetWithMeta's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithMultiReaderGetWithMeta(fn func(context.Context, string) (multireader.Value, multireader.Meta, error)) MultiReaderStubOption {
	return func(s *MultiReaderStub) { s.OnGetWithMeta.Func(fn) }
}

// MultiReaderStub is a recording test double for MultiReader.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type MultiReaderStub struct {
	OnGetWithMeta *MultiReaderGetWithMetaStub

	// group is every method stub above, viewed through the surface that
	// does not depend on a signature — one home for the settings and the
	// lifecycle that apply to the whole double.
	group  *stub.Group
	strict bool
}

// Compile-time proof that MultiReaderStub satisfies MultiReader.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ multireader.MultiReader = (*MultiReaderStub)(nil)

// NewMultiReaderStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewMultiReaderStub(tb testing.TB, opts ...MultiReaderStubOption) *MultiReaderStub {
	s := &MultiReaderStub{
		OnGetWithMeta: &MultiReaderGetWithMetaStub{MethodStub: stub.NewMethodStub[MultiReaderGetWithMetaCall](tb, "MultiReader.GetWithMeta")},
	}
	s.group = stub.NewGroup(
		s.OnGetWithMeta.MethodStub,
	)
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.group.StrictAll()
	}
	s.group.Bind(tb)
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *MultiReaderStub) ResetCalls() {
	s.group.Reset()
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *MultiReaderGetWithMetaStub) invoke(ctx context.Context, key string) func() MultiReaderGetWithMetaReturn {
	if s.fn == nil {
		return nil
	}
	return func() MultiReaderGetWithMetaReturn {
		r0, r1, r2 := s.fn(ctx, key)
		return MultiReaderGetWithMetaReturn{Result0: r0, Result1: r1, Err: r2}
	}
}

// GetWithMeta records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *MultiReaderStub) GetWithMeta(ctx context.Context, key string) (multireader.Value, multireader.Meta, error) {
	call := MultiReaderGetWithMetaCall{Ctx: ctx, Key: key}
	r := stub.Answer(s.OnGetWithMeta.MethodStub, &call, stub.Arms[MultiReaderGetWithMetaCall, MultiReaderGetWithMetaReturn]{
		Invoke:   s.OnGetWithMeta.invoke(ctx, key),
		Fallback: s.OnGetWithMeta.fallback,
		Fault:    func(err error) MultiReaderGetWithMetaReturn { return MultiReaderGetWithMetaReturn{Err: err} },
		Stamp: func(c *MultiReaderGetWithMetaCall, r MultiReaderGetWithMetaReturn) {
			c.Result0 = r.Result0
			c.Result1 = r.Result1
			c.Err = r.Err
		},
	})
	return r.Result0, r.Result1, r.Err
}

// testkit: end of generated content.
// testkit:provenance 0b341144c574193cdbafae4582568a023b082a423dc51d6d081205fc0320f00e
