// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/poisonaccessor/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package poisonaccessortest_test

import (
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/poisonaccessor/poisonaccessortest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// poisonAccessorStubErrSubject binds Err into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func poisonAccessorStubErrSubject(tb testing.TB) stub.Subject[poisonaccessortest.PoisonAccessorErrCall, poisonaccessortest.PoisonAccessorErrReturn] {
	tb.Helper()
	s := poisonaccessortest.NewPoisonAccessorStub(tb)
	return stub.Subject[poisonaccessortest.PoisonAccessorErrCall, poisonaccessortest.PoisonAccessorErrReturn]{
		Stub: s.OnErr.MethodStub,
		Call: func() {
			_ = s.Err()
		},
		Result: func() poisonaccessortest.PoisonAccessorErrReturn {
			got0 := s.Err()
			return poisonaccessortest.PoisonAccessorErrReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnErr.Func(func() error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			r0 := s.Err()
			return r0
		},
	}
}

// TestPoisonAccessorStubErr pins how Err answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestPoisonAccessorStubErr(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Err", poisonAccessorStubErrSubject)

	// No check that Err answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := poisonaccessortest.NewPoisonAccessorStub(t)
		_ = s.Err()
		s.OnErr.AssertCalledOnce(t, "Err must record the call")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := poisonaccessortest.NewPoisonAccessorStub(t)
		var seen []poisonaccessortest.PoisonAccessorErrCall
		s.OnErr.OnRecord(func(c poisonaccessortest.PoisonAccessorErrCall) { seen = append(seen, c) })
		_ = s.Err()
		_ = s.Err()
		testkit.Len(t, seen, 2, "OnRecord must fire once per Err call")
	})

	t.Run("wires WithPoisonAccessorErr at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := poisonaccessortest.NewPoisonAccessorStub(t, poisonaccessortest.WithPoisonAccessorErr(func() error {
			called = true
			var z0 error
			return z0
		}))
		_ = s.Err()
		testkit.True(t, called, "WithPoisonAccessorErr must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Err admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := poisonaccessortest.NewPoisonAccessorStub(t)
		var want0 error
		s.OnErr.Returns(want0)
		_ = s.Err()
		s.ResetCalls()
		_ = s.Err()
		s.OnErr.AssertCalledOnce(t, "Err must record the call that followed the reset")
	})
}

// poisonAccessorStubDouble describes how to build a PoisonAccessorStub under each
// option whose effect is the same whatever a method's signature.
//
// Err stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func poisonAccessorStubDouble() stub.Double[poisonaccessortest.PoisonAccessorErrCall] {
	instance := func(s *poisonaccessortest.PoisonAccessorStub) stub.Instance[poisonaccessortest.PoisonAccessorErrCall] {
		return stub.Instance[poisonaccessortest.PoisonAccessorErrCall]{
			Stub: s.OnErr.MethodStub,
			Call: func() {
				_ = s.Err()
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[poisonaccessortest.PoisonAccessorErrCall]{
		New: func(tb testing.TB) stub.Instance[poisonaccessortest.PoisonAccessorErrCall] {
			return instance(poisonaccessortest.NewPoisonAccessorStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[poisonaccessortest.PoisonAccessorErrCall] {
			return instance(poisonaccessortest.NewPoisonAccessorStub(tb, poisonaccessortest.PoisonAccessorStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[poisonaccessortest.PoisonAccessorErrCall] {
			return instance(poisonaccessortest.NewPoisonAccessorStub(tb, poisonaccessortest.PoisonAccessorStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[poisonaccessortest.PoisonAccessorErrCall] {
			return instance(poisonaccessortest.NewPoisonAccessorStub(tb, poisonaccessortest.PoisonAccessorStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[poisonaccessortest.PoisonAccessorErrCall] {
			return instance(poisonaccessortest.NewPoisonAccessorStub(tb, poisonaccessortest.PoisonAccessorStubStrict()))
		},
	}
}

// TestPoisonAccessorStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestPoisonAccessorStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, poisonAccessorStubDouble())
}

// TestPoisonAccessorStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestPoisonAccessorStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := poisonaccessortest.NewPoisonAccessorStub(t)
	s := poisonaccessortest.NewPoisonAccessorStub(t, poisonaccessortest.PoisonAccessorStubDelegateTo(inner))

	t.Run("forwards Err to the wrapped implementation", func(t *testing.T) {
		_ = s.Err()
		inner.OnErr.AssertCalledOnce(t, "Err must reach the wrapped implementation")
	})

	t.Run("surfaces what Err answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Err-delegate")
		inner.OnErr.FaultsFor(time.Hour, want)
		r0 := s.Err()
		testkit.ErrorIs(t, r0, want, "Err must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 702a6e0d8cbaa36497b1c65f0f9cc615b632f7e0e235a6641e3909030defbac0
