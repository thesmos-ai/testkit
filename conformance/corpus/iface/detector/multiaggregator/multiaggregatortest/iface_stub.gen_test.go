// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/multiaggregator/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package multiaggregatortest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/multiaggregator/multiaggregatortest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// multiAggregatorStubStatsSubject binds Stats into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func multiAggregatorStubStatsSubject(tb testing.TB) stub.Subject[multiaggregatortest.MultiAggregatorStatsCall, multiaggregatortest.MultiAggregatorStatsReturn] {
	tb.Helper()
	s := multiaggregatortest.NewMultiAggregatorStub(tb)
	return stub.Subject[multiaggregatortest.MultiAggregatorStatsCall, multiaggregatortest.MultiAggregatorStatsReturn]{
		Stub: s.OnStats.MethodStub,
		Call: func() {
			var a0 context.Context
			_, _, _ = s.Stats(a0)
		},
		Result: func() multiaggregatortest.MultiAggregatorStatsReturn {
			var a0 context.Context
			got0, got1, got2 := s.Stats(a0)
			return multiaggregatortest.MultiAggregatorStatsReturn{Result0: got0, Result1: got1, Err: got2}
		},
		Override: func(mark func()) {
			s.OnStats.Func(func(_ context.Context) (int, int, error) {
				mark()
				var z0 int
				var z1 int
				var z2 error
				return z0, z1, z2
			})
		},
		Fails: func() error {
			var a0 context.Context
			_, _, r2 := s.Stats(a0)
			return r2
		},
	}
}

// TestMultiAggregatorStubStats pins how Stats answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMultiAggregatorStubStats(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Stats", multiAggregatorStubStatsSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := multiaggregatortest.NewMultiAggregatorStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 int = 42
		var want1 int = 42
		var want2 error
		s.OnStats.Returns(want0, want1, want2)
		var a0 context.Context
		got0, got1, _ := s.Stats(a0)
		testkit.Equal(t, got0, want0, "Stats must answer with what Returns pinned")
		testkit.Equal(t, got1, want1, "Stats must answer with what Returns pinned")
		// Blanked above rather than dropped in silence. No literal exists
		// for result 2 (error), so the only value to pin there
		// is the zero — and a double that ignored Returns answers the zero
		// too, which makes the comparison pass either way.
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := multiaggregatortest.NewMultiAggregatorStub(t)
		var a0 context.Context
		_, _, _ = s.Stats(a0)
		got := s.OnStats.AssertCalledOnce(t, "Stats must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := multiaggregatortest.NewMultiAggregatorStub(t)
		var seen []multiaggregatortest.MultiAggregatorStatsCall
		s.OnStats.OnRecord(func(c multiaggregatortest.MultiAggregatorStatsCall) { seen = append(seen, c) })
		var a0 context.Context
		_, _, _ = s.Stats(a0)
		_, _, _ = s.Stats(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Stats call")
	})

	t.Run("wires WithMultiAggregatorStats at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := multiaggregatortest.NewMultiAggregatorStub(t, multiaggregatortest.WithMultiAggregatorStats(func(_ context.Context) (int, int, error) {
			called = true
			var z0 int
			var z1 int
			var z2 error
			return z0, z1, z2
		}))
		var a0 context.Context
		_, _, _ = s.Stats(a0)
		testkit.True(t, called, "WithMultiAggregatorStats must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		//
		// Pinned to a literal for the reason the Returns check is: a reset
		// that lost the configuration answers the zero, and so does one that
		// kept a configured zero.
		s := multiaggregatortest.NewMultiAggregatorStub(t)
		var want0 int = 42
		var want1 int = 42
		var want2 error
		s.OnStats.Returns(want0, want1, want2)
		var a0 context.Context
		_, _, _ = s.Stats(a0)
		s.ResetCalls()
		got0, got1, _ := s.Stats(a0)
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
		testkit.Equal(t, got1, want1, "a reset must keep what Returns pinned")
	})
}

// multiAggregatorStubDouble describes how to build a MultiAggregatorStub under each
// option whose effect is the same whatever a method's signature.
//
// Stats stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func multiAggregatorStubDouble() stub.Double[multiaggregatortest.MultiAggregatorStatsCall] {
	instance := func(s *multiaggregatortest.MultiAggregatorStub) stub.Instance[multiaggregatortest.MultiAggregatorStatsCall] {
		return stub.Instance[multiaggregatortest.MultiAggregatorStatsCall]{
			Stub: s.OnStats.MethodStub,
			Call: func() {
				var a0 context.Context
				_, _, _ = s.Stats(a0)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[multiaggregatortest.MultiAggregatorStatsCall]{
		New: func(tb testing.TB) stub.Instance[multiaggregatortest.MultiAggregatorStatsCall] {
			return instance(multiaggregatortest.NewMultiAggregatorStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[multiaggregatortest.MultiAggregatorStatsCall] {
			return instance(multiaggregatortest.NewMultiAggregatorStub(tb, multiaggregatortest.MultiAggregatorStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[multiaggregatortest.MultiAggregatorStatsCall] {
			return instance(multiaggregatortest.NewMultiAggregatorStub(tb, multiaggregatortest.MultiAggregatorStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[multiaggregatortest.MultiAggregatorStatsCall] {
			return instance(multiaggregatortest.NewMultiAggregatorStub(tb, multiaggregatortest.MultiAggregatorStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[multiaggregatortest.MultiAggregatorStatsCall] {
			return instance(multiaggregatortest.NewMultiAggregatorStub(tb, multiaggregatortest.MultiAggregatorStubStrict()))
		},
	}
}

// TestMultiAggregatorStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestMultiAggregatorStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, multiAggregatorStubDouble())
}

// TestMultiAggregatorStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestMultiAggregatorStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := multiaggregatortest.NewMultiAggregatorStub(t)
	s := multiaggregatortest.NewMultiAggregatorStub(t, multiaggregatortest.MultiAggregatorStubDelegateTo(inner))

	t.Run("forwards Stats to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_, _, _ = s.Stats(a0)
		inner.OnStats.AssertCalledOnce(t, "Stats must reach the wrapped implementation")
	})

	t.Run("surfaces what Stats answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Stats-delegate")
		inner.OnStats.FaultsFor(time.Hour, want)
		var a0 context.Context
		_, _, r2 := s.Stats(a0)
		testkit.ErrorIs(t, r2, want, "Stats must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 2660d56c001437ca854da4854ebab3e29402a03952668e3f66e8ab457d0b9b21
