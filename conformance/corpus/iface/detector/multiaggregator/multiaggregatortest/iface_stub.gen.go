// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/multiaggregator/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

// Conformance checks worked out from the interfaces this package doubles.
//
// One call runs every check for an interface against one implementation.
// Describe the implementation in a literal and hand it over — each
// interface's own Run function is documented beside it, with the names
// to use.
//
// Nothing else is required to start. The rest is there when you need it:
// a harness field to add only when a check fails asking for it, checks of
// your own that run beside the generated ones, a Prove entry that drives
// each of yours against the broken implementation it names, and a typed
// index for dropping a check by identity rather than by string.
//
// Nothing here is written by hand. Regenerate rather than edit: an edit
// survives until the next run and no longer.
package multiaggregatortest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/multiaggregator"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// MultiAggregatorStatsCall records one invocation of MultiAggregator.Stats.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type MultiAggregatorStatsCall struct {
	Ctx     context.Context
	Result0 int
	Result1 int
	Err     error
}

// --- Per-method configuration ---

// MultiAggregatorStatsStub controls how the double answers Stats and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type MultiAggregatorStatsStub struct {
	*stub.MethodStub[MultiAggregatorStatsCall]

	fn       func(context.Context) (int, int, error)
	fallback *MultiAggregatorStatsReturn
}

// MultiAggregatorStatsReturn holds the fixed answer configured through Returns.
type MultiAggregatorStatsReturn struct {
	Result0 int
	Result1 int
	Err     error
}

// Returns pins a fixed result for every call to Stats. A Func
// override and an injected fault both take precedence over it.
func (s *MultiAggregatorStatsStub) Returns(result0 int, result1 int, err error) *MultiAggregatorStatsStub {
	s.fallback = &MultiAggregatorStatsReturn{Result0: result0, Result1: result1, Err: err}
	return s
}

// Func supplies a body for Stats, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *MultiAggregatorStatsStub) Func(fn func(context.Context) (int, int, error)) *MultiAggregatorStatsStub {
	s.fn = fn
	return s
}

// --- MultiAggregatorStub ---

// The double-runtime witness for the surface this double rides. A
// breaking change to the runtime renames it, and this file stops
// compiling with the skew named.
var _ = stub.CompatV1

// MultiAggregatorStubOption configures a [MultiAggregatorStub] at construction time.
type MultiAggregatorStubOption func(*MultiAggregatorStub)

// MultiAggregatorStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func MultiAggregatorStubStrict() MultiAggregatorStubOption {
	return func(s *MultiAggregatorStub) { s.strict = true }
}

// MultiAggregatorStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func MultiAggregatorStubDelegateTo(impl multiaggregator.MultiAggregator) MultiAggregatorStubOption {
	return func(s *MultiAggregatorStub) {
		s.OnStats.Func(impl.Stats)
	}
}

// MultiAggregatorStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func MultiAggregatorStubWithClock(clk clock.Clock) MultiAggregatorStubOption {
	return func(s *MultiAggregatorStub) { s.group.SetClock(clk) }
}

// MultiAggregatorStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func MultiAggregatorStubWithRandSource(src rand.Source) MultiAggregatorStubOption {
	return func(s *MultiAggregatorStub) { s.group.SetRandSource(src) }
}

// MultiAggregatorStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func MultiAggregatorStubBenchMode() MultiAggregatorStubOption {
	return func(s *MultiAggregatorStub) { s.group.BenchMode() }
}

// WithMultiAggregatorStats sets Stats's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithMultiAggregatorStats(fn func(context.Context) (int, int, error)) MultiAggregatorStubOption {
	return func(s *MultiAggregatorStub) { s.OnStats.Func(fn) }
}

// MultiAggregatorStub is a recording test double for MultiAggregator.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type MultiAggregatorStub struct {
	OnStats *MultiAggregatorStatsStub

	// group is every method stub above, viewed through the surface that
	// does not depend on a signature — one home for the settings and the
	// lifecycle that apply to the whole double.
	group  *stub.Group
	strict bool
}

// Compile-time proof that MultiAggregatorStub satisfies MultiAggregator.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ multiaggregator.MultiAggregator = (*MultiAggregatorStub)(nil)

// NewMultiAggregatorStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewMultiAggregatorStub(tb testing.TB, opts ...MultiAggregatorStubOption) *MultiAggregatorStub {
	s := &MultiAggregatorStub{
		OnStats: &MultiAggregatorStatsStub{MethodStub: stub.NewMethodStub[MultiAggregatorStatsCall](tb, "MultiAggregator.Stats")},
	}
	s.group = stub.NewGroup(
		s.OnStats.MethodStub,
	)
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.group.StrictAll()
	}
	s.group.Bind(tb)
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *MultiAggregatorStub) ResetCalls() {
	s.group.Reset()
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *MultiAggregatorStatsStub) invoke(ctx context.Context) func() MultiAggregatorStatsReturn {
	if s.fn == nil {
		return nil
	}
	return func() MultiAggregatorStatsReturn {
		r0, r1, r2 := s.fn(ctx)
		return MultiAggregatorStatsReturn{Result0: r0, Result1: r1, Err: r2}
	}
}

// Stats records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *MultiAggregatorStub) Stats(ctx context.Context) (int, int, error) {
	call := MultiAggregatorStatsCall{Ctx: ctx}
	r := stub.Answer(s.OnStats.MethodStub, &call, stub.Arms[MultiAggregatorStatsCall, MultiAggregatorStatsReturn]{
		Invoke:   s.OnStats.invoke(ctx),
		Fallback: s.OnStats.fallback,
		Fault:    func(err error) MultiAggregatorStatsReturn { return MultiAggregatorStatsReturn{Err: err} },
		Stamp: func(c *MultiAggregatorStatsCall, r MultiAggregatorStatsReturn) {
			c.Result0 = r.Result0
			c.Result1 = r.Result1
			c.Err = r.Err
		},
	})
	return r.Result0, r.Result1, r.Err
}

// testkit: end of generated content.
// testkit:provenance 664571e6b53d0181b7b187ff4623a847f094a2e0f8c9f271886a7ea9bc3f472a
