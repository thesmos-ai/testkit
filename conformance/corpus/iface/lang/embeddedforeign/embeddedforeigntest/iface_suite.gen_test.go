// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/embeddedforeign/iface.go
// Plugins:   golang 1.0.0, suite 1.24.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package embeddedforeigntest_test

import (
	"testing"

	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/embeddedforeign/embeddedforeigntest"
	"go.thesmos.sh/testkit/engine/suite"
)

// The planted defects live in the file beside this one, on ProveStream.
//
// They were here once, and could not stay: a check may need a capability,
// and only your harness answers it. A defect stands in for a real subject
// and borrows the same answer — which a test function in this package has
// no way to reach, because the harness is written in yours. So the map
// went where the entry point that takes it lives, and ProveStream
// drives every defect below alongside the ones your own rows name:
//
//	Read.Smoke — a Stream whose Read panics
//
//	Read.Cancels — a Stream whose Read ignores the context it is handed
//
//	Read.NilContext — a Stream whose Read forgives a nil context and answers
//
//	Read.Deadline — a Stream whose Read ignores the context it is handed
//
//	Read.ZeroOnError — a Stream whose Read answers a believable value beside its error
//
//	Close.Smoke — a Stream whose Close panics

// TestStreamInvariants holds this package to what it says about itself.
//
// Four claims, none of which a consumer writes and none of which any
// other gate makes. The manifest pins the assertion SET, so a check that
// stops being emitted reads as a removed row in review rather than as a
// suite that quietly got smaller. The index and the drop hints pin the
// two ways a check is NAMED — an index entry pointing at nothing
// compiles, and a drop hint that falls back to the string form forfeits
// exactly the compile-break the index exists to give. The distinct-ID
// check catches two interfaces in one package claiming one identity,
// which method-name scopes make possible.
func TestStreamInvariants(t *testing.T) {
	t.Parallel()

	s := embeddedforeigntest.StreamSuite.Suite(embeddedforeigntest.DefaultStreamFixture())

	rows, err := suite.LockLines(s)
	if err != nil {
		t.Fatalf("render the manifest rows: %v", err)
	}
	suite.VerifyLock(t, "stream.checks.lock", rows)
	suite.VerifyIndex(t, "Stream",
		embeddedforeigntest.StreamSuite.Checks.All(), s.IDs())
	suite.VerifyDropHints(t, "Stream", s.IDs(), s.DropHint)
	suite.VerifyDistinctIDs(t, s.IDs())
}

// testkit: end of generated content.
// testkit:provenance 96bb768e8978f4e25e404a800b81ec710bf324f268446d012f254ce860b07d62
