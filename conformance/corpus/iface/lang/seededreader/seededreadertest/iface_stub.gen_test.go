// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/seededreader/iface.go
// Plugins:   golang 1.0.0, stub 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package seededreadertest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/seededreader"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/seededreader/seededreadertest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// catalogStubLookupSubject binds Lookup into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func catalogStubLookupSubject(tb testing.TB) stub.Subject[seededreadertest.CatalogLookupCall, seededreadertest.CatalogLookupReturn] {
	tb.Helper()
	s := seededreadertest.NewCatalogStub(tb)
	return stub.Subject[seededreadertest.CatalogLookupCall, seededreadertest.CatalogLookupReturn]{
		Stub: s.OnLookup.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 seededreader.Key
			_, _ = s.Lookup(a0, a1)
		},
		Result: func() seededreadertest.CatalogLookupReturn {
			var a0 context.Context
			var a1 seededreader.Key
			got0, got1 := s.Lookup(a0, a1)
			return seededreadertest.CatalogLookupReturn{Result: got0, Err: got1}
		},
		Override: func(mark func()) {
			s.OnLookup.Func(func(_ context.Context, _ seededreader.Key) (seededreader.Body, error) {
				mark()
				var z0 seededreader.Body
				var z1 error
				return z0, z1
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 seededreader.Key
			_, r1 := s.Lookup(a0, a1)
			return r1
		},
	}
}

// TestCatalogStubLookup pins how Lookup answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestCatalogStubLookup(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Lookup", catalogStubLookupSubject)

	// No check that Lookup answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := seededreadertest.NewCatalogStub(t)
		var a0 context.Context
		var a1 seededreader.Key
		_, _ = s.Lookup(a0, a1)
		got := s.OnLookup.AssertCalledOnce(t, "Lookup must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Key, a1, "the recorded call carries Key")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := seededreadertest.NewCatalogStub(t)
		var seen []seededreadertest.CatalogLookupCall
		s.OnLookup.OnRecord(func(c seededreadertest.CatalogLookupCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 seededreader.Key
		_, _ = s.Lookup(a0, a1)
		_, _ = s.Lookup(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Lookup call")
	})

	t.Run("wires WithCatalogLookup at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := seededreadertest.NewCatalogStub(t, seededreadertest.WithCatalogLookup(func(_ context.Context, _ seededreader.Key) (seededreader.Body, error) {
			called = true
			var z0 seededreader.Body
			var z1 error
			return z0, z1
		}))
		var a0 context.Context
		var a1 seededreader.Key
		_, _ = s.Lookup(a0, a1)
		testkit.True(t, called, "WithCatalogLookup must install the override")
	})

	t.Run("keeps recording across a reset", func(t *testing.T) {
		t.Parallel()
		// The weaker claim, and the honest one for this signature. No result
		// of Lookup admits a literal, so what Returns pinned cannot be
		// told from the zero and comparing them would pass either way.
		//
		// What a reset can still be held to is that it clears the log without
		// taking the double with it: configure, call, reset, call again, and
		// exactly one call is recorded. A reset that dropped the whole
		// configuration fails that, which is the failure the value comparison
		// was reaching for and could not state.
		s := seededreadertest.NewCatalogStub(t)
		var want0 seededreader.Body
		var want1 error
		s.OnLookup.Returns(want0, want1)
		var a0 context.Context
		var a1 seededreader.Key
		_, _ = s.Lookup(a0, a1)
		s.ResetCalls()
		_, _ = s.Lookup(a0, a1)
		s.OnLookup.AssertCalledOnce(t, "Lookup must record the call that followed the reset")
	})
}

// catalogStubLenSubject binds Len into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func catalogStubLenSubject(tb testing.TB) stub.Subject[seededreadertest.CatalogLenCall, seededreadertest.CatalogLenReturn] {
	tb.Helper()
	s := seededreadertest.NewCatalogStub(tb)
	return stub.Subject[seededreadertest.CatalogLenCall, seededreadertest.CatalogLenReturn]{
		Stub: s.OnLen.MethodStub,
		Call: func() {
			var a0 context.Context
			_, _ = s.Len(a0)
		},
		Result: func() seededreadertest.CatalogLenReturn {
			var a0 context.Context
			got0, got1 := s.Len(a0)
			return seededreadertest.CatalogLenReturn{Result: got0, Err: got1}
		},
		Override: func(mark func()) {
			s.OnLen.Func(func(_ context.Context) (int, error) {
				mark()
				var z0 int
				var z1 error
				return z0, z1
			})
		},
		Fails: func() error {
			var a0 context.Context
			_, r1 := s.Len(a0)
			return r1
		},
	}
}

// TestCatalogStubLen pins how Len answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestCatalogStubLen(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Len", catalogStubLenSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := seededreadertest.NewCatalogStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 int = 42
		var want1 error
		s.OnLen.Returns(want0, want1)
		var a0 context.Context
		got0, _ := s.Len(a0)
		testkit.Equal(t, got0, want0, "Len must answer with what Returns pinned")
		// Blanked above rather than dropped in silence. No literal exists
		// for result 1 (error), so the only value to pin there
		// is the zero — and a double that ignored Returns answers the zero
		// too, which makes the comparison pass either way.
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := seededreadertest.NewCatalogStub(t)
		var a0 context.Context
		_, _ = s.Len(a0)
		got := s.OnLen.AssertCalledOnce(t, "Len must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := seededreadertest.NewCatalogStub(t)
		var seen []seededreadertest.CatalogLenCall
		s.OnLen.OnRecord(func(c seededreadertest.CatalogLenCall) { seen = append(seen, c) })
		var a0 context.Context
		_, _ = s.Len(a0)
		_, _ = s.Len(a0)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Len call")
	})

	t.Run("wires WithCatalogLen at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := seededreadertest.NewCatalogStub(t, seededreadertest.WithCatalogLen(func(_ context.Context) (int, error) {
			called = true
			var z0 int
			var z1 error
			return z0, z1
		}))
		var a0 context.Context
		_, _ = s.Len(a0)
		testkit.True(t, called, "WithCatalogLen must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		//
		// Pinned to a literal for the reason the Returns check is: a reset
		// that lost the configuration answers the zero, and so does one that
		// kept a configured zero.
		s := seededreadertest.NewCatalogStub(t)
		var want0 int = 42
		var want1 error
		s.OnLen.Returns(want0, want1)
		var a0 context.Context
		_, _ = s.Len(a0)
		s.ResetCalls()
		got0, _ := s.Len(a0)
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
	})
}

// catalogStubDouble describes how to build a CatalogStub under each
// option whose effect is the same whatever a method's signature.
//
// Lookup stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func catalogStubDouble() stub.Double[seededreadertest.CatalogLookupCall] {
	instance := func(s *seededreadertest.CatalogStub) stub.Instance[seededreadertest.CatalogLookupCall] {
		return stub.Instance[seededreadertest.CatalogLookupCall]{
			Stub: s.OnLookup.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 seededreader.Key
				_, _ = s.Lookup(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[seededreadertest.CatalogLookupCall]{
		New: func(tb testing.TB) stub.Instance[seededreadertest.CatalogLookupCall] {
			return instance(seededreadertest.NewCatalogStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[seededreadertest.CatalogLookupCall] {
			return instance(seededreadertest.NewCatalogStub(tb, seededreadertest.CatalogStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[seededreadertest.CatalogLookupCall] {
			return instance(seededreadertest.NewCatalogStub(tb, seededreadertest.CatalogStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[seededreadertest.CatalogLookupCall] {
			return instance(seededreadertest.NewCatalogStub(tb, seededreadertest.CatalogStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[seededreadertest.CatalogLookupCall] {
			return instance(seededreadertest.NewCatalogStub(tb, seededreadertest.CatalogStubStrict()))
		},
	}
}

// TestCatalogStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestCatalogStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, catalogStubDouble())
}

// TestCatalogStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestCatalogStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := seededreadertest.NewCatalogStub(t)
	s := seededreadertest.NewCatalogStub(t, seededreadertest.CatalogStubDelegateTo(inner))

	t.Run("forwards Lookup to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 seededreader.Key
		_, _ = s.Lookup(a0, a1)
		inner.OnLookup.AssertCalledOnce(t, "Lookup must reach the wrapped implementation")
	})

	t.Run("surfaces what Lookup answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Lookup-delegate")
		inner.OnLookup.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 seededreader.Key
		_, r1 := s.Lookup(a0, a1)
		testkit.ErrorIs(t, r1, want, "Lookup must surface the wrapped answer")
	})

	t.Run("forwards Len to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		_, _ = s.Len(a0)
		inner.OnLen.AssertCalledOnce(t, "Len must reach the wrapped implementation")
	})

	t.Run("surfaces what Len answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Len-delegate")
		inner.OnLen.FaultsFor(time.Hour, want)
		var a0 context.Context
		_, r1 := s.Len(a0)
		testkit.ErrorIs(t, r1, want, "Len must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance 958bf7c2788acc89a98325560ffffe01b46b72516ade8952e0a0d115b11595cd
