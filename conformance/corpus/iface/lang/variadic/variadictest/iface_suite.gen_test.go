// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/variadic/iface.go
// Plugins:   golang 1.0.0, suite 1.24.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package variadictest_test

import (
	"testing"

	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/variadic/variadictest"
	"go.thesmos.sh/testkit/engine/suite"
)

// The planted defects live in the file beside this one, on ProveFinder.
//
// They were here once, and could not stay: a check may need a capability,
// and only your harness answers it. A defect stands in for a real subject
// and borrows the same answer — which a test function in this package has
// no way to reach, because the harness is written in yours. So the map
// went where the entry point that takes it lives, and ProveFinder
// drives every defect below alongside the ones your own rows name:
//
//	Find.Smoke — a Finder whose Find panics
//
//	Find.Cancels — a Finder whose Find ignores the context it is handed
//
//	Find.NilContext — a Finder whose Find forgives a nil context and answers
//
//	Find.Deadline — a Finder whose Find ignores the context it is handed
//
//	Find.ZeroOnError — a Finder whose Find answers a believable value beside its error
//
//	FindWithLimit.Smoke — a Finder whose FindWithLimit panics
//
//	FindWithLimit.Cancels — a Finder whose FindWithLimit ignores the context it is handed
//
//	FindWithLimit.NilContext — a Finder whose FindWithLimit forgives a nil context and answers
//
//	FindWithLimit.Deadline — a Finder whose FindWithLimit ignores the context it is handed
//
//	FindWithLimit.ZeroOnError — a Finder whose FindWithLimit answers a believable value beside its error

// TestFinderInvariants holds this package to what it says about itself.
//
// Four claims, none of which a consumer writes and none of which any
// other gate makes. The manifest pins the assertion SET, so a check that
// stops being emitted reads as a removed row in review rather than as a
// suite that quietly got smaller. The index and the drop hints pin the
// two ways a check is NAMED — an index entry pointing at nothing
// compiles, and a drop hint that falls back to the string form forfeits
// exactly the compile-break the index exists to give. The distinct-ID
// check catches two interfaces in one package claiming one identity,
// which method-name scopes make possible.
func TestFinderInvariants(t *testing.T) {
	t.Parallel()

	s := variadictest.FinderSuite.Suite(variadictest.DefaultFinderFixture())

	rows, err := suite.LockLines(s)
	if err != nil {
		t.Fatalf("render the manifest rows: %v", err)
	}
	suite.VerifyLock(t, "finder.checks.lock", rows)
	suite.VerifyIndex(t, "Finder",
		variadictest.FinderSuite.Checks.All(), s.IDs())
	suite.VerifyDropHints(t, "Finder", s.IDs(), s.DropHint)
	suite.VerifyDistinctIDs(t, s.IDs())
}

// testkit: end of generated content.
// testkit:provenance e22183a58963c48bf9f415edf05e2e7681c7d642158d5b544698034477167da2
