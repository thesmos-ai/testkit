// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/struct/config/iface.go
// Plugins:   golang 1.0.0, builder 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package configtest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/struct/config"
	"go.thesmos.sh/testkit/conformance/corpus/struct/config/configtest"
)

// TestNewConfig pins what the constructor seeds.
func TestNewConfig(t *testing.T) {
	t.Parallel()

	t.Run("seeds Host with its declared default", func(t *testing.T) {
		t.Parallel()
		// A default the constructor drops reads as a field the author never
		// declared, and every test built on it starts from the wrong value.
		testkit.Equal(t, configtest.NewConfig().Build().Host, "localhost",
			"NewConfig must seed Host")
	})

	t.Run("seeds Port with its declared default", func(t *testing.T) {
		t.Parallel()
		// A default the constructor drops reads as a field the author never
		// declared, and every test built on it starts from the wrong value.
		testkit.Equal(t, configtest.NewConfig().Build().Port, 8080,
			"NewConfig must seed Port")
	})

	t.Run("seeds Verbose with its declared default", func(t *testing.T) {
		t.Parallel()
		// A default the constructor drops reads as a field the author never
		// declared, and every test built on it starts from the wrong value.
		testkit.Equal(t, configtest.NewConfig().Build().Verbose, true,
			"NewConfig must seed Verbose")
	})

	t.Run("seeds Ratio with its declared default", func(t *testing.T) {
		t.Parallel()
		// A default the constructor drops reads as a field the author never
		// declared, and every test built on it starts from the wrong value.
		testkit.Equal(t, configtest.NewConfig().Build().Ratio, 0.75,
			"NewConfig must seed Ratio")
	})

	// No check that NewConfig seeds Retries: its declared default is
	// the zero value, so the assertion would be `0 == 0`
	// and would pass for a constructor that ignored the directive. The
	// declaration still stands — the author is saying the zero is deliberate
	// — and nothing here can tell that apart from the zero arriving by
	// default.

	// No check that NewConfig seeds Fallback: its declared default is
	// the zero value, so the assertion would be `nil == nil`
	// and would pass for a constructor that ignored the directive. The
	// declaration still stands — the author is saying the zero is deliberate
	// — and nothing here can tell that apart from the zero arriving by
	// default.
}

// TestNewConfigFrom pins the seeding constructor, which is what a test
// uses to vary one field of a value it already holds.
func TestNewConfigFrom(t *testing.T) {
	t.Parallel()
	t.Run("carries the value through untouched", func(t *testing.T) {
		t.Parallel()
		// Anything it altered would be a field the caller did not ask to
		// change, which defeats the point of seeding from a value at all.
		//
		// Populated, not the zero value. `From(zero).Build() == zero` holds
		// for a constructor that drops every field it is handed, which is the
		// one defect this check exists to catch — so the seed is built
		// through the setters, each already proven above to reach its field.
		seed := configtest.NewConfig().WithHost("test-host").WithPort(42).WithVerbose(true).WithRatio(3.14).WithRetries(42).WithUndirected("test-undirected").Build()
		testkit.Equal(t, configtest.NewConfigFrom(seed).Build(), seed,
			"NewConfigFrom must not alter what it was given")
	})
}

// TestConfigBuilderWithHost pins the setter for Host.
func TestConfigBuilderWithHost(t *testing.T) {
	t.Parallel()

	t.Run("reaches Host", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, configtest.NewConfig().WithHost("test-host").Build().Host, "test-host",
			"WithHost must reach Host")
		testkit.Equal(t, configtest.NewConfig().WithHost("other-host").Build().Host, "other-host",
			"WithHost must reach Host")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v string
		b := configtest.NewConfig()
		testkit.True(t, b.WithHost(v) == b, "the setter returns its receiver")
	})
}

// TestConfigBuilderWithPort pins the setter for Port.
func TestConfigBuilderWithPort(t *testing.T) {
	t.Parallel()

	t.Run("reaches Port", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, configtest.NewConfig().WithPort(42).Build().Port, 42,
			"WithPort must reach Port")
		testkit.Equal(t, configtest.NewConfig().WithPort(7).Build().Port, 7,
			"WithPort must reach Port")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v int
		b := configtest.NewConfig()
		testkit.True(t, b.WithPort(v) == b, "the setter returns its receiver")
	})
}

// TestConfigBuilderWithVerbose pins the setter for Verbose.
func TestConfigBuilderWithVerbose(t *testing.T) {
	t.Parallel()

	t.Run("reaches Verbose", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, configtest.NewConfig().WithVerbose(true).Build().Verbose, true,
			"WithVerbose must reach Verbose")
		testkit.Equal(t, configtest.NewConfig().WithVerbose(false).Build().Verbose, false,
			"WithVerbose must reach Verbose")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v bool
		b := configtest.NewConfig()
		testkit.True(t, b.WithVerbose(v) == b, "the setter returns its receiver")
	})
}

// TestConfigBuilderWithRatio pins the setter for Ratio.
func TestConfigBuilderWithRatio(t *testing.T) {
	t.Parallel()

	t.Run("reaches Ratio", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, configtest.NewConfig().WithRatio(3.14).Build().Ratio, 3.14,
			"WithRatio must reach Ratio")
		testkit.Equal(t, configtest.NewConfig().WithRatio(2.72).Build().Ratio, 2.72,
			"WithRatio must reach Ratio")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v float64
		b := configtest.NewConfig()
		testkit.True(t, b.WithRatio(v) == b, "the setter returns its receiver")
	})
}

// TestConfigBuilderWithRetries pins the setter for Retries.
func TestConfigBuilderWithRetries(t *testing.T) {
	t.Parallel()

	t.Run("reaches Retries", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, configtest.NewConfig().WithRetries(42).Build().Retries, 42,
			"WithRetries must reach Retries")
		testkit.Equal(t, configtest.NewConfig().WithRetries(7).Build().Retries, 7,
			"WithRetries must reach Retries")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v int
		b := configtest.NewConfig()
		testkit.True(t, b.WithRetries(v) == b, "the setter returns its receiver")
	})
}

// TestConfigBuilderWithFallback pins the setter for Fallback.
func TestConfigBuilderWithFallback(t *testing.T) {
	t.Parallel()

	t.Run("reaches Fallback", func(t *testing.T) {
		t.Parallel()
		// Compared as pointers rather than dereferenced: a setter that assigned
		// nothing leaves nil, and dereferencing that panics instead of saying
		// which setter failed. Two values because whatever the constructor
		// seeded can equal at most one of them.
		want := string("test-fallback")
		testkit.Equal(t, configtest.NewConfig().WithFallback("test-fallback").Build().Fallback, &want,
			"WithFallback must reach Fallback")
		other := string("other-fallback")
		testkit.Equal(t, configtest.NewConfig().WithFallback("other-fallback").Build().Fallback, &other,
			"WithFallback must reach Fallback")
	})

	t.Run("addresses the value it was given", func(t *testing.T) {
		t.Parallel()
		// The setter takes a value, so a builder handing back nil never
		// allocated and the field stays indistinguishable from unset.
		var v string
		testkit.True(t, configtest.NewConfig().WithFallback(v).Build().Fallback != nil,
			"WithFallback must take an address")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v string
		b := configtest.NewConfig()
		testkit.True(t, b.WithFallback(v) == b, "the setter returns its receiver")
	})
}

// TestConfigBuilderWithUndirected pins the setter for Undirected.
func TestConfigBuilderWithUndirected(t *testing.T) {
	t.Parallel()

	t.Run("reaches Undirected", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, configtest.NewConfig().WithUndirected("test-undirected").Build().Undirected, "test-undirected",
			"WithUndirected must reach Undirected")
		testkit.Equal(t, configtest.NewConfig().WithUndirected("other-undirected").Build().Undirected, "other-undirected",
			"WithUndirected must reach Undirected")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v string
		b := configtest.NewConfig()
		testkit.True(t, b.WithUndirected(v) == b, "the setter returns its receiver")
	})
}

// TestConfigBuilderMutate pins the escape hatch for a field no setter reaches.
func TestConfigBuilderMutate(t *testing.T) {
	t.Parallel()

	t.Run("reaches the value under construction", func(t *testing.T) {
		t.Parallel()
		// Handed a copy, every mutation would be discarded and the caller
		// would see a builder that silently ignored them.
		reached := false
		configtest.NewConfig().Mutate(func(*config.Config) { reached = true })
		testkit.True(t, reached, "Mutate must be applied")
	})
}

// TestConfigBuilderClone pins that a clone can be configured on its own.
func TestConfigBuilderClone(t *testing.T) {
	t.Parallel()

	t.Run("carries the configuration across", func(t *testing.T) {
		t.Parallel()
		// A clone starting from nothing would be a second constructor under
		// another name, and a test cloning a configured builder would silently
		// lose everything it had set.
		b := configtest.NewConfig()
		testkit.Equal(t, b.Clone().Build(), b.Build(), "a clone starts where its original stood")
	})
}

// TestConfigBuilderBuild pins what the builder hands back.
func TestConfigBuilderBuild(t *testing.T) {
	t.Parallel()
	t.Run("returns every field that was configured", func(t *testing.T) {
		t.Parallel()
		// Every field set on the way, not a zero compared against itself:
		// a Build that answered the zero would satisfy the second form
		// however much had been configured.
		seed := configtest.NewConfig().WithHost("test-host").WithPort(42).WithVerbose(true).WithRatio(3.14).WithRetries(42).WithUndirected("test-undirected").Build()
		testkit.Equal(t, configtest.NewConfigFrom(seed).Build(), seed,
			"Build returns the value under construction")
	})
}

// testkit: end of generated content.
// testkit:provenance 95e13e94d4a2d91aa5931ab4b67c2527c942b30a72924c94621a807d0325d992
