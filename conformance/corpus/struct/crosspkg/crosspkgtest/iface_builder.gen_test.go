// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/struct/crosspkg/iface.go
// Plugins:   golang 1.0.0, builder 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package crosspkgtest_test

import (
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/struct/crosspkg"
	"go.thesmos.sh/testkit/conformance/corpus/struct/crosspkg/crosspkgtest"
	"go.thesmos.sh/testkit/conformance/corpus/struct/seed"
)

// TestNewSettings pins what the constructor seeds.
func TestNewSettings(t *testing.T) {
	t.Parallel()

	t.Run("seeds Timeout with its declared default", func(t *testing.T) {
		t.Parallel()
		// A default the constructor drops reads as a field the author never
		// declared, and every test built on it starts from the wrong value.
		testkit.Equal(t, crosspkgtest.NewSettings().Build().Timeout, time.Second,
			"NewSettings must seed Timeout")
	})

	t.Run("seeds Retries with its declared default", func(t *testing.T) {
		t.Parallel()
		// A default the constructor drops reads as a field the author never
		// declared, and every test built on it starts from the wrong value.
		testkit.Equal(t, crosspkgtest.NewSettings().Build().Retries, seed.Retries,
			"NewSettings must seed Retries")
	})

	t.Run("seeds Region with its declared default", func(t *testing.T) {
		t.Parallel()
		// A default the constructor drops reads as a field the author never
		// declared, and every test built on it starts from the wrong value.
		testkit.Equal(t, crosspkgtest.NewSettings().Build().Region, seed.Region,
			"NewSettings must seed Region")
	})
}

// TestNewSettingsFrom pins the seeding constructor, which is what a test
// uses to vary one field of a value it already holds.
func TestNewSettingsFrom(t *testing.T) {
	t.Parallel()
	t.Run("carries the value through untouched", func(t *testing.T) {
		t.Parallel()
		// Anything it altered would be a field the caller did not ask to
		// change, which defeats the point of seeding from a value at all.
		//
		// Populated, not the zero value. `From(zero).Build() == zero` holds
		// for a constructor that drops every field it is handed, which is the
		// one defect this check exists to catch — so the seed is built
		// through the setters, each already proven above to reach its field.
		seed := crosspkgtest.NewSettings().WithTimeout(time.Duration(42)).WithRetries(42).WithRegion("test-region").WithDeadline(time.Unix(42, 0)).Build()
		testkit.Equal(t, crosspkgtest.NewSettingsFrom(seed).Build(), seed,
			"NewSettingsFrom must not alter what it was given")
	})
}

// TestSettingsBuilderWithTimeout pins the setter for Timeout.
func TestSettingsBuilderWithTimeout(t *testing.T) {
	t.Parallel()

	t.Run("reaches Timeout", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, crosspkgtest.NewSettings().WithTimeout(time.Duration(42)).Build().Timeout, time.Duration(42),
			"WithTimeout must reach Timeout")
		testkit.Equal(t, crosspkgtest.NewSettings().WithTimeout(time.Duration(7)).Build().Timeout, time.Duration(7),
			"WithTimeout must reach Timeout")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v time.Duration
		b := crosspkgtest.NewSettings()
		testkit.True(t, b.WithTimeout(v) == b, "the setter returns its receiver")
	})
}

// TestSettingsBuilderWithRetries pins the setter for Retries.
func TestSettingsBuilderWithRetries(t *testing.T) {
	t.Parallel()

	t.Run("reaches Retries", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, crosspkgtest.NewSettings().WithRetries(42).Build().Retries, 42,
			"WithRetries must reach Retries")
		testkit.Equal(t, crosspkgtest.NewSettings().WithRetries(7).Build().Retries, 7,
			"WithRetries must reach Retries")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v int
		b := crosspkgtest.NewSettings()
		testkit.True(t, b.WithRetries(v) == b, "the setter returns its receiver")
	})
}

// TestSettingsBuilderWithRegion pins the setter for Region.
func TestSettingsBuilderWithRegion(t *testing.T) {
	t.Parallel()

	t.Run("reaches Region", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, crosspkgtest.NewSettings().WithRegion("test-region").Build().Region, "test-region",
			"WithRegion must reach Region")
		testkit.Equal(t, crosspkgtest.NewSettings().WithRegion("other-region").Build().Region, "other-region",
			"WithRegion must reach Region")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v string
		b := crosspkgtest.NewSettings()
		testkit.True(t, b.WithRegion(v) == b, "the setter returns its receiver")
	})
}

// TestSettingsBuilderWithDeadline pins the setter for Deadline.
func TestSettingsBuilderWithDeadline(t *testing.T) {
	t.Parallel()

	t.Run("reaches Deadline", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, crosspkgtest.NewSettings().WithDeadline(time.Unix(42, 0)).Build().Deadline, time.Unix(42, 0),
			"WithDeadline must reach Deadline")
		testkit.Equal(t, crosspkgtest.NewSettings().WithDeadline(time.Unix(7, 0)).Build().Deadline, time.Unix(7, 0),
			"WithDeadline must reach Deadline")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v time.Time
		b := crosspkgtest.NewSettings()
		testkit.True(t, b.WithDeadline(v) == b, "the setter returns its receiver")
	})
}

// TestSettingsBuilderMutate pins the escape hatch for a field no setter reaches.
func TestSettingsBuilderMutate(t *testing.T) {
	t.Parallel()

	t.Run("reaches the value under construction", func(t *testing.T) {
		t.Parallel()
		// Handed a copy, every mutation would be discarded and the caller
		// would see a builder that silently ignored them.
		reached := false
		crosspkgtest.NewSettings().Mutate(func(*crosspkg.Settings) { reached = true })
		testkit.True(t, reached, "Mutate must be applied")
	})
}

// TestSettingsBuilderClone pins that a clone can be configured on its own.
func TestSettingsBuilderClone(t *testing.T) {
	t.Parallel()

	t.Run("carries the configuration across", func(t *testing.T) {
		t.Parallel()
		// A clone starting from nothing would be a second constructor under
		// another name, and a test cloning a configured builder would silently
		// lose everything it had set.
		b := crosspkgtest.NewSettings()
		testkit.Equal(t, b.Clone().Build(), b.Build(), "a clone starts where its original stood")
	})
}

// TestSettingsBuilderBuild pins what the builder hands back.
func TestSettingsBuilderBuild(t *testing.T) {
	t.Parallel()
	t.Run("returns every field that was configured", func(t *testing.T) {
		t.Parallel()
		// Every field set on the way, not a zero compared against itself:
		// a Build that answered the zero would satisfy the second form
		// however much had been configured.
		seed := crosspkgtest.NewSettings().WithTimeout(time.Duration(42)).WithRetries(42).WithRegion("test-region").WithDeadline(time.Unix(42, 0)).Build()
		testkit.Equal(t, crosspkgtest.NewSettingsFrom(seed).Build(), seed,
			"Build returns the value under construction")
	})
}

// TestNewMirrored pins what the constructor seeds.
func TestNewMirrored(t *testing.T) {
	t.Parallel()

	t.Run("seeds from the package companion", func(t *testing.T) {
		t.Parallel()
		// Compared against the companion's own return rather than a value
		// restated here: anything weaker would pass against a constructor that
		// called something else, or nothing at all.
		testkit.Equal(t, crosspkgtest.NewMirrored().Build(), seed.ConfigDefaults(),
			"NewMirrored must seed from its companion")
	})
}

// TestNewMirroredFrom pins the seeding constructor, which is what a test
// uses to vary one field of a value it already holds.
func TestNewMirroredFrom(t *testing.T) {
	t.Parallel()
	t.Run("carries the value through untouched", func(t *testing.T) {
		t.Parallel()
		// Anything it altered would be a field the caller did not ask to
		// change, which defeats the point of seeding from a value at all.
		//
		// Populated, not the zero value. `From(zero).Build() == zero` holds
		// for a constructor that drops every field it is handed, which is the
		// one defect this check exists to catch — so the seed is built
		// through the setters, each already proven above to reach its field.
		seed := crosspkgtest.NewMirrored().WithRegion("test-region").WithRetries(42).WithLabel("test-label").Build()
		testkit.Equal(t, crosspkgtest.NewMirroredFrom(seed).Build(), seed,
			"NewMirroredFrom must not alter what it was given")
	})
}

// TestMirroredBuilderWithRegion pins the setter for Region.
func TestMirroredBuilderWithRegion(t *testing.T) {
	t.Parallel()

	t.Run("reaches Region", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, crosspkgtest.NewMirrored().WithRegion("test-region").Build().Region, "test-region",
			"WithRegion must reach Region")
		testkit.Equal(t, crosspkgtest.NewMirrored().WithRegion("other-region").Build().Region, "other-region",
			"WithRegion must reach Region")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v string
		b := crosspkgtest.NewMirrored()
		testkit.True(t, b.WithRegion(v) == b, "the setter returns its receiver")
	})
}

// TestMirroredBuilderWithRetries pins the setter for Retries.
func TestMirroredBuilderWithRetries(t *testing.T) {
	t.Parallel()

	t.Run("reaches Retries", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, crosspkgtest.NewMirrored().WithRetries(42).Build().Retries, 42,
			"WithRetries must reach Retries")
		testkit.Equal(t, crosspkgtest.NewMirrored().WithRetries(7).Build().Retries, 7,
			"WithRetries must reach Retries")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v int
		b := crosspkgtest.NewMirrored()
		testkit.True(t, b.WithRetries(v) == b, "the setter returns its receiver")
	})
}

// TestMirroredBuilderWithLabel pins the setter for Label.
func TestMirroredBuilderWithLabel(t *testing.T) {
	t.Parallel()

	t.Run("reaches Label", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, crosspkgtest.NewMirrored().WithLabel("test-label").Build().Label, "test-label",
			"WithLabel must reach Label")
		testkit.Equal(t, crosspkgtest.NewMirrored().WithLabel("other-label").Build().Label, "other-label",
			"WithLabel must reach Label")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v string
		b := crosspkgtest.NewMirrored()
		testkit.True(t, b.WithLabel(v) == b, "the setter returns its receiver")
	})
}

// TestMirroredBuilderMutate pins the escape hatch for a field no setter reaches.
func TestMirroredBuilderMutate(t *testing.T) {
	t.Parallel()

	t.Run("reaches the value under construction", func(t *testing.T) {
		t.Parallel()
		// Handed a copy, every mutation would be discarded and the caller
		// would see a builder that silently ignored them.
		reached := false
		crosspkgtest.NewMirrored().Mutate(func(*crosspkg.Mirrored) { reached = true })
		testkit.True(t, reached, "Mutate must be applied")
	})
}

// TestMirroredBuilderClone pins that a clone can be configured on its own.
func TestMirroredBuilderClone(t *testing.T) {
	t.Parallel()

	t.Run("carries the configuration across", func(t *testing.T) {
		t.Parallel()
		// A clone starting from nothing would be a second constructor under
		// another name, and a test cloning a configured builder would silently
		// lose everything it had set.
		b := crosspkgtest.NewMirrored()
		testkit.Equal(t, b.Clone().Build(), b.Build(), "a clone starts where its original stood")
	})
}

// TestMirroredBuilderBuild pins what the builder hands back.
func TestMirroredBuilderBuild(t *testing.T) {
	t.Parallel()
	t.Run("returns every field that was configured", func(t *testing.T) {
		t.Parallel()
		// Every field set on the way, not a zero compared against itself:
		// a Build that answered the zero would satisfy the second form
		// however much had been configured.
		seed := crosspkgtest.NewMirrored().WithRegion("test-region").WithRetries(42).WithLabel("test-label").Build()
		testkit.Equal(t, crosspkgtest.NewMirroredFrom(seed).Build(), seed,
			"Build returns the value under construction")
	})
}

// TestNewShorthand pins what the constructor seeds.
func TestNewShorthand(t *testing.T) {
	t.Parallel()

	t.Run("seeds from the package companion", func(t *testing.T) {
		t.Parallel()
		// Compared against the companion's own return rather than a value
		// restated here: anything weaker would pass against a constructor that
		// called something else, or nothing at all.
		testkit.Equal(t, crosspkgtest.NewShorthand().Build(), seed.ConfigDefaults(),
			"NewShorthand must seed from its companion")
	})
}

// TestNewShorthandFrom pins the seeding constructor, which is what a test
// uses to vary one field of a value it already holds.
func TestNewShorthandFrom(t *testing.T) {
	t.Parallel()
	t.Run("carries the value through untouched", func(t *testing.T) {
		t.Parallel()
		// Anything it altered would be a field the caller did not ask to
		// change, which defeats the point of seeding from a value at all.
		//
		// Populated, not the zero value. `From(zero).Build() == zero` holds
		// for a constructor that drops every field it is handed, which is the
		// one defect this check exists to catch — so the seed is built
		// through the setters, each already proven above to reach its field.
		seed := crosspkgtest.NewShorthand().WithRegion("test-region").WithRetries(42).WithLabel("test-label").Build()
		testkit.Equal(t, crosspkgtest.NewShorthandFrom(seed).Build(), seed,
			"NewShorthandFrom must not alter what it was given")
	})
}

// TestShorthandBuilderWithRegion pins the setter for Region.
func TestShorthandBuilderWithRegion(t *testing.T) {
	t.Parallel()

	t.Run("reaches Region", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, crosspkgtest.NewShorthand().WithRegion("test-region").Build().Region, "test-region",
			"WithRegion must reach Region")
		testkit.Equal(t, crosspkgtest.NewShorthand().WithRegion("other-region").Build().Region, "other-region",
			"WithRegion must reach Region")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v string
		b := crosspkgtest.NewShorthand()
		testkit.True(t, b.WithRegion(v) == b, "the setter returns its receiver")
	})
}

// TestShorthandBuilderWithRetries pins the setter for Retries.
func TestShorthandBuilderWithRetries(t *testing.T) {
	t.Parallel()

	t.Run("reaches Retries", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, crosspkgtest.NewShorthand().WithRetries(42).Build().Retries, 42,
			"WithRetries must reach Retries")
		testkit.Equal(t, crosspkgtest.NewShorthand().WithRetries(7).Build().Retries, 7,
			"WithRetries must reach Retries")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v int
		b := crosspkgtest.NewShorthand()
		testkit.True(t, b.WithRetries(v) == b, "the setter returns its receiver")
	})
}

// TestShorthandBuilderWithLabel pins the setter for Label.
func TestShorthandBuilderWithLabel(t *testing.T) {
	t.Parallel()

	t.Run("reaches Label", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, crosspkgtest.NewShorthand().WithLabel("test-label").Build().Label, "test-label",
			"WithLabel must reach Label")
		testkit.Equal(t, crosspkgtest.NewShorthand().WithLabel("other-label").Build().Label, "other-label",
			"WithLabel must reach Label")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v string
		b := crosspkgtest.NewShorthand()
		testkit.True(t, b.WithLabel(v) == b, "the setter returns its receiver")
	})
}

// TestShorthandBuilderMutate pins the escape hatch for a field no setter reaches.
func TestShorthandBuilderMutate(t *testing.T) {
	t.Parallel()

	t.Run("reaches the value under construction", func(t *testing.T) {
		t.Parallel()
		// Handed a copy, every mutation would be discarded and the caller
		// would see a builder that silently ignored them.
		reached := false
		crosspkgtest.NewShorthand().Mutate(func(*crosspkg.Shorthand) { reached = true })
		testkit.True(t, reached, "Mutate must be applied")
	})
}

// TestShorthandBuilderClone pins that a clone can be configured on its own.
func TestShorthandBuilderClone(t *testing.T) {
	t.Parallel()

	t.Run("carries the configuration across", func(t *testing.T) {
		t.Parallel()
		// A clone starting from nothing would be a second constructor under
		// another name, and a test cloning a configured builder would silently
		// lose everything it had set.
		b := crosspkgtest.NewShorthand()
		testkit.Equal(t, b.Clone().Build(), b.Build(), "a clone starts where its original stood")
	})
}

// TestShorthandBuilderBuild pins what the builder hands back.
func TestShorthandBuilderBuild(t *testing.T) {
	t.Parallel()
	t.Run("returns every field that was configured", func(t *testing.T) {
		t.Parallel()
		// Every field set on the way, not a zero compared against itself:
		// a Build that answered the zero would satisfy the second form
		// however much had been configured.
		seed := crosspkgtest.NewShorthand().WithRegion("test-region").WithRetries(42).WithLabel("test-label").Build()
		testkit.Equal(t, crosspkgtest.NewShorthandFrom(seed).Build(), seed,
			"Build returns the value under construction")
	})
}

// testkit: end of generated content.
// testkit:provenance 4d530264d13df739b77629641cd78681ff826283f8290440eb5349584c8a842b
