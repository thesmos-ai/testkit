// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/integration/validated/iface.go
// Plugins:   golang 1.0.0, builder 1.4.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package validatedtest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/integration/validated"
	"go.thesmos.sh/testkit/conformance/corpus/integration/validated/validatedtest"
)

// TestNewAccount pins what the constructor seeds.
func TestNewAccount(t *testing.T) {
	t.Parallel()

	t.Run("seeds from the package companion", func(t *testing.T) {
		t.Parallel()
		// Compared against the companion's own return rather than a value
		// restated here: anything weaker would pass against a constructor that
		// called something else, or nothing at all.
		testkit.Equal(t, validatedtest.NewAccount().Build(), validated.AccountDefaults(),
			"NewAccount must seed from its companion")
	})
}

// TestNewAccountFrom pins the seeding constructor, which is what a test
// uses to vary one field of a value it already holds.
func TestNewAccountFrom(t *testing.T) {
	t.Parallel()
	t.Run("carries the value through untouched", func(t *testing.T) {
		t.Parallel()
		// Anything it altered would be a field the caller did not ask to
		// change, which defeats the point of seeding from a value at all.
		//
		// Populated, not the zero value. `From(zero).Build() == zero` holds
		// for a constructor that drops every field it is handed, which is the
		// one defect this check exists to catch — so the seed is built
		// through the setters, each already proven above to reach its field.
		seed := validatedtest.NewAccount().WithID("test-id").WithEmail("test-email").Build()
		testkit.Equal(t, validatedtest.NewAccountFrom(seed).Build(), seed,
			"NewAccountFrom must not alter what it was given")
	})
}

// TestAccountBuilderWithID pins the setter for ID.
func TestAccountBuilderWithID(t *testing.T) {
	t.Parallel()

	t.Run("reaches ID", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, validatedtest.NewAccount().WithID("test-id").Build().ID, "test-id",
			"WithID must reach ID")
		testkit.Equal(t, validatedtest.NewAccount().WithID("other-id").Build().ID, "other-id",
			"WithID must reach ID")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v string
		b := validatedtest.NewAccount()
		testkit.True(t, b.WithID(v) == b, "the setter returns its receiver")
	})
}

// TestAccountBuilderWithEmail pins the setter for Email.
func TestAccountBuilderWithEmail(t *testing.T) {
	t.Parallel()

	t.Run("reaches Email", func(t *testing.T) {
		t.Parallel()
		// Two distinct values rather than one: whatever the constructor seeded
		// can equal at most one of them, so a setter assigning nothing fails
		// here instead of passing against its own seed.
		testkit.Equal(t, validatedtest.NewAccount().WithEmail("test-email").Build().Email, "test-email",
			"WithEmail must reach Email")
		testkit.Equal(t, validatedtest.NewAccount().WithEmail("other-email").Build().Email, "other-email",
			"WithEmail must reach Email")
	})

	t.Run("returns the builder so calls chain", func(t *testing.T) {
		t.Parallel()
		var v string
		b := validatedtest.NewAccount()
		testkit.True(t, b.WithEmail(v) == b, "the setter returns its receiver")
	})
}

// TestAccountBuilderMutate pins the escape hatch for a field no setter reaches.
func TestAccountBuilderMutate(t *testing.T) {
	t.Parallel()

	t.Run("reaches the value under construction", func(t *testing.T) {
		t.Parallel()
		// Handed a copy, every mutation would be discarded and the caller
		// would see a builder that silently ignored them.
		reached := false
		validatedtest.NewAccount().Mutate(func(*validated.Account) { reached = true })
		testkit.True(t, reached, "Mutate must be applied")
	})
}

// TestAccountBuilderClone pins that a clone can be configured on its own.
func TestAccountBuilderClone(t *testing.T) {
	t.Parallel()

	t.Run("carries the configuration across", func(t *testing.T) {
		t.Parallel()
		// A clone starting from nothing would be a second constructor under
		// another name, and a test cloning a configured builder would silently
		// lose everything it had set.
		b := validatedtest.NewAccount()
		testkit.Equal(t, b.Clone().Build(), b.Build(), "a clone starts where its original stood")
	})
}

// TestAccountBuilderBuild pins what the builder hands back.
func TestAccountBuilderBuild(t *testing.T) {
	t.Parallel()
	t.Run("returns every field that was configured", func(t *testing.T) {
		t.Parallel()
		// Every field set on the way, not a zero compared against itself:
		// a Build that answered the zero would satisfy the second form
		// however much had been configured.
		seed := validatedtest.NewAccount().WithID("test-id").WithEmail("test-email").Build()
		testkit.Equal(t, validatedtest.NewAccountFrom(seed).Build(), seed,
			"Build returns the value under construction")
	})
}

// testkit: end of generated content.
// testkit:provenance 7e473de89d9c2263901acb9d8f4323647a182174571cf2dacfdc119176372ade
