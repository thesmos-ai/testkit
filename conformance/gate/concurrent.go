// Copyright Thesmos 2026
// SPDX-License-Identifier: MIT

package gate

// UnrenderedLegs registers every concurrency family the model tier
// classifies and does not drive, with what the leg would take.
//
// Empty, and the census beside it is what keeps it that way. All five
// families render: the four the engine ships a Porcupine model for step
// their own histories, and session runs stepless with the per-client laws
// carrying the verdict — the arrangement the runner demands, since it
// refuses a stepless model with no law registered rather than pass a run
// that asserted nothing.
//
// The register stays because the next family will arrive before its leg
// does, and a row here is how that gap stays visible while it is written.
// Keys are the family; values are what the leg has to spell.
//
//nolint:gochecknoglobals // a debt register, read-only, test-facing.
var UnrenderedLegs = map[string]string{}
